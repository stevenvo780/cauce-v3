/** The two shipped breakpoints; `styles/responsive.css` declares exactly these widths. */
export const RAIL_VIEWPORT = '(max-width: 1100px)';
export const BOTTOM_BAR_VIEWPORT = '(max-width: 760px)';
