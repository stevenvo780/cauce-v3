# Historial de despliegues

Registro append-only. Una fila por intento de `deploy/deploy.sh` (la noche del 26-08 hubo 17 intentos que no dejaron ni una línea en ningún sitio — eso no se repite).

| Fecha UTC | Commit | Runtime digest | Console digest | Resultado |
|---|---|---|---|---|
