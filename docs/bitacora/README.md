# Bitácora (histórico congelado)

Material histórico: handoffs, informes de sesión y planes fechados. **No es estado actual del sistema** — no confiar en nada de aquí como verdad vigente. El estado vigente vive en el README raíz, `plan-reestructura/` y `ordenes/`.
