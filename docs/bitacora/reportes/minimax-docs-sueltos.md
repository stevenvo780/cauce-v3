# Veredicto sobre los 4 docs sueltos de `docs/` — ronda 3 minimax

Alcance: `docs/consola-roles-con-nombre.md`, `docs/terminal-pty.md`,
`docs/directiva-ficheros-del-agente.md`, `docs/threat-model.md`. NO se mueven;
el integrador decide. Misma mecánica que la verificación de runbooks: lectura
contra el sistema real.

| Doc | Veredicto | Evidencia |
|---|---|---|
| `docs/threat-model.md` | **Vigente** | Las 17 amenazas/controls del mapa contrastan con el código: `DevOnlyAuthProvider`+`NODE_ENV` (services/gateway/src/password.ts), lease+epoch+fencing (packages/store/src/repository/leases.ts), outbox `*_query_success=0` (services/dispatcher/src/metrics.ts:196–217), `system.database.probe` (services/dispatcher/src/handlers.ts:54), `pg_stat_ssl.ssl=true` en readiness (services/gateway/src/health.ts). Las dos refs a `ops/runbooks/ha.md` están vigentes. Los riesgos pendientes siguen siéndolo. **Mover a bitácora no aporta nada**: describe el modelo de ataque vivo. |
| `docs/terminal-pty.md` | **Desactualizado en 2 puntos menores** | (1) Línea 272 dice «los 14 alias»; la flota real son **15** (ver `ops/manifests/` — `zeus` falta en el conteo del doc). (2) Línea 251 referencia `ops/runbooks/rollback.md`, que **no existe** (sólo hay `docs/bitacora/rollback.md`, histórico); el autor probablemente pensaba en el runbook vivo que se menciona en `ha.md` y `alias-cutover.md`. El resto del runbook describe con precisión el canal PTY desplegado (puertos 8445/8446, `services/terminal-relay/src/config.ts:53–54`; nginx `apps/console/nginx.conf:15`; el bind de directorio en `grants.json` y `pty_agent_identities.json`; `ops/scripts/{compose.sh,release-candidate.py,validate.sh}` todos presentes). La sección §6 sobre `ultimate-terminal` coincide con la doc de bitácora (`docs/bitacora/hardening-2026-07-25.md:121+`). |
| `docs/directiva-ficheros-del-agente.md` | **Desactualizado en 2 puntos** | (1) Línea 145 dice «`registerAgentDocumentRoutes(app, deps)` **es una línea que falta, deliberadamente»**, pero `services/gateway/src/app.ts:41,1483` ya la importa y la llama. La afirmación «No está desplegado y no está enganchado a `main.ts`» es **falsa hoy**. Coherente con que el README de la consola` también lo dice: ambos docs describen el mismo gap, que ya se cerró. (2) Tabla de §2: lista 14 alias y 13 adaptadores vivos con `argos` muerto aparte; **falta `zeus`** (medido en su contenedor `ws-zeus`, arnés `claude`, directiva `/home/dev/.claude/CLAUDE.md`). El conteo de alias es del 23-ago; el doc es de hace 4 días y la flota creció. El resto (cinco alias en los que el registro miente sobre `harness_id`/`home_directory`/`container_name`, los bind-mounts de un solo fichero, `ws-isa`+`ws-isa-workspace` compartiendo `$HOME`, el modo `document` propuesto, los 7 controles del §6) **sigue vigente**. |
| `docs/consola-roles-con-nombre.md` | **A bitácora, o reescritura de cabecera** | El doc se autodeclara «PENDIENTE, no implementado» pero la migración **`packages/store/migrations/024_agent_role_templates.sql` ya está aplicada** (ver tabla `agent_role_templates`, columna `slug` PK, columna `role_template_slug` en `agents`, triggers `agents_role_template_*`, diario `agent_role_brief_history`; integridad verificada en `packages/store/src/migration-integrity.ts:6,29–84`). La cabecera miente sobre el estado. Además el plan describe detalles que NO se implementaron como el plan dice: la tabla real es `agent_role_templates` (no `role_templates`), la PK es `slug` (no `id`), y los apartados §3-§6 (schema Zod, `roleTemplate()` en `configuration.ts`, fachada de gateway, pestaña de consola con alta/edición/asignación) **no existen en main** (`grep -rn 'role_template\|RoleTemplate' packages/protocol/src packages/store/src/configuration.ts apps/console/src/features/config`: cero coincidencias fuera de `migration-integrity.ts`). Lo que sí está implementado y el doc ignora es el FALLBACK en consola (`apps/console/src/features/config/roles.ts` y `RolesPanel.tsx`) que agrupa por `role_brief` y bloquea por las dos unidades — eso es el «catálogo de roles en uso» del §1 del doc, ya vivo y desplegado. **Recomendación al integrador**: el doc describe un plan que se ejecutó por la mitad y por un camino distinto. O se reescribe la cabecera para reflejar «migración aplicada, plan original reemplazado por `agent_role_templates` con `slug`, las capas protocol/store/console planificadas en §3-§6 siguen pendientes», o se mueve a `docs/bitacora/` para que el plan original quede como referencia histórica sin inducir a error. |

## Resumen ejecutivo

- **1 vigente** (`threat-model.md`).
- **2 desactualizados en 2-3 puntos menores** (`terminal-pty.md`, `directiva-ficheros-del-agente.md`) — flota creció a 15 y el conteo dice 14; dos refs a `ops/runbooks/rollback.md` y a `registerAgentDocumentRoutes` ya enganchado.
- **1 a mover a bitácora o reescribir** (`consola-roles-con-nombre.md`) — cabecera PENDIENTE/no-implementado falsa; el plan original diverge de lo que se construyó.

No se mueven ficheros en este reporte (lo prohíbe el encargo); el integrador decide.