global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    service: cauce-v3
    environment: cauce-v3

alerting:
  alertmanagers:
    - static_configs:
        - targets: [alertmanager:9093]

rule_files:
  - /etc/prometheus/alerts.yaml

scrape_configs:
  # Self-scrape is required for notification errors/drops. Without it Prometheus
  # could spend days failing to reach Alertmanager while every application
  # target remained green and no alert exposed the broken delivery path.
  - job_name: cauce-prometheus
    static_configs:
      - targets: [127.0.0.1:9090]
  - job_name: cauce-alertmanager
    static_configs:
      - targets: [alertmanager:9093]
  - job_name: cauce-dispatcher
    metrics_path: /metrics
    static_configs:
      - targets: [dispatcher:8082]
  - job_name: cauce-gateway
    metrics_path: /metrics
    static_configs:
      - targets: [gateway:8081]
  - job_name: otel-collector
    static_configs:
      - targets: [otel-collector:8888, otel-collector:9464]
  - job_name: cauce-telegram
    metrics_path: /metrics
    # Profile-aware discovery: sin registro DNS no hay target rojo fantasma. telegram-bridge binds only its internal backend
    # interface; no host port is published.
    dns_sd_configs:
      - names: [telegram-bridge]
        type: A
        port: 8086
        refresh_interval: 15s
  - job_name: cauce-outbox
    metrics_path: /metrics
    static_configs:
      - targets: [outbox-metrics:8084]
