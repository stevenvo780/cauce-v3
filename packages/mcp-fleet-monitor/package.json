{
  "name": "@cauce/mcp-fleet-monitor",
  "version": "1.0.0",
  "description": "MCP server for fleet monitoring: read-only tools for alias state, deliveries, chains, and health",
  "type": "module",
  "main": "dist/server.js",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "pnpm typecheck && pnpm exec esbuild src/server.ts --bundle --platform=node --format=esm --external:pg --outfile=dist/server.js && pnpm exec esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cauce/protocol": "workspace:*",
    "@cauce/store": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "@types/pg": "^8.15.4",
    "esbuild": "0.28.1",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=22"
  }
}
