# Plano objetivo y medición de contaminación (27-08)

Investigación de 5 agentes: coste de arranque, coste por tarea-tipo, coherencia de nombres, árbol objetivo por zona, y backlog final de purga.

## arranque

TAREA A1 — Coste de arranque de una sesión fresca. Datos crudos + auditoría, cita línea a línea.

## 1. Peso bruto de los 9 ficheros pedidos

| Fichero | Líneas | ~Tokens (líneas×12, fórmula pedida) | Tokens reales aprox (chars/4, más fiable) |
|---|---|---|---|
| AGENTS.md | 36 | 432 | 891 |
| CLAUDE.md | 36 | 432 | 894 |
| GEMINI.md | 36 | 432 | 893 |
| ordenes/00-PROTOCOLO.md | 57 | 684 | 1.441 |
| ordenes/codex.md | 16 | 192 | 481 |
| ordenes/gemini.md | 26 | 312 | 822 |
| ordenes/opencode-minimax.md | 20 | 240 | 609 |
| README.md | 54 | 648 | 1.107 |
| docs/arquitectura.md | 103 | 1.236 | 2.582 |
| **TOTAL (los 9)** | **384** | **4.608** | **9.720** |

**Hallazgo metodológico:** la fórmula líneas×12 pedida SUBESTIMA el coste real ~2,1x en este corpus (4.608 vs 9.720 tokens por chars/4). Causa: son párrafos largos sin envolver (una "línea" Markdown = 60-90 palabras de prosa), no líneas de código cortas — el propio AGENTS.md tiene 523 palabras en solo 36 líneas. Si se va a fijar un techo de "líneas", hay que fijarlo sabiendo que el coste en tokens no es lineal con ese proxy.

## 2. Qué lee REALMENTE una sesión fresca (no los 9 juntos)

Ni ordenes/00-PROTOCOLO.md ni AGENTS.md/CLAUDE.md/GEMINI.md ni ninguna orden referencian README.md o docs/arquitectura.md como lectura obligatoria (grep confirmado: cero referencias en los ficheros de arranque, salvo una mención incidental de "documentación (README/CLAUDE.md/AGENTS.md)" como nombre de sector en la tabla del protocolo). docs/arquitectura.md además se autodefine "guía para HUMANOS" (línea 1) y README.md dice "¿Primera vez aquí (humano)?" (línea 5). Es decir: el corpus de arranque OBLIGATORIO real ya es mucho menor que 384 líneas:

| Instancia | Arranque obligatorio (identidad + protocolo + su orden) | Líneas | Tokens (líneas×12) |
|---|---|---|---|
| Gemini | GEMINI.md + 00-PROTOCOLO.md + ordenes/gemini.md | 119 | 1.428 |
| MiniMax | AGENTS.md (genérico, no hay MINIMAX.md) + 00-PROTOCOLO.md + ordenes/opencode-minimax.md | 113 | 1.356 |
| Codex (paga esto UNA vez; sesión persistente) | AGENTS.md + 00-PROTOCOLO.md + ordenes/codex.md | 109 | 1.308 |
| Claude (sin orden fija — ver hallazgo abajo) | CLAUDE.md + 00-PROTOCOLO.md + orden ad-hoc no persistida | ≥93 | ≥1.116 |

**Conclusión honesta:** el objetivo "<400 líneas TODO incluido" YA SE CUMPLE hoy por instancia (93-119 líneas). El problema real que reporta el dueño no es volumen bruto — es (a) tres copias del mismo fichero con riesgo/hecho de divergencia, (b) contradicciones reales entre el fichero auto-cargado y el protocolo que dice resumir, (c) datos caducados dentro de las órdenes de tarea que obligan a re-verificar todo desde cero, y (d) huecos estructurales (Claude no tiene orden propia). Corregir eso reduce la "contaminación" mucho más que recortar líneas.

## 3. (a) REDUNDANCIAS — citadas línea a línea

1. **AGENTS.md, CLAUDE.md y GEMINI.md son BYTE-IDÉNTICOS** salvo el título de la línea 1 (verificado con `diff`: única diferencia en las 3 comparaciones). Las líneas 3-36 (33 de 36 líneas, ~92% del fichero) están triplicadas letra por letra. Esto no es "compartir contexto", es mantenimiento por triplicado con cero mecanismo que garantice que sigan sincronizados (de hecho ya no lo están del todo, ver contradicciones).

2. El hecho "PostgreSQL es la única fuente / entrega pull por WS / el dispatcher NO reparte, es el segador" se repite con redacciones distintas en **5 ficheros / 6 sitios**: AGENTS.md:3, CLAUDE.md:3, GEMINI.md:3 (idénticas), README.md:3+9-10, docs/arquitectura.md:21-23 ("Tres ideas que lo explican casi todo").

3. La regla "código muerto se borra con `git rm` + evidencia; git es el archivo; no hay carpetas de cuarentena" aparece en **4 sitios**: AGENTS.md:20, CLAUDE.md:20, GEMINI.md:20, README.md:19. Además, DENTRO de la propia línea 20 (en los 3 ficheros de identidad) el mismo hecho se dice DOS VECES seguidas en la misma línea: "No existen carpetas de cuarentena. [...] No existen carpetas de archivo: ni cuarentenas ni bitácoras." — redundancia intra-línea.

4. "Reportes/reporte final ≤5 líneas" aparece **7 veces**: AGENTS.md:27, CLAUDE.md:27, GEMINI.md:27, 00-PROTOCOLO.md:57, ordenes/codex.md:16, ordenes/gemini.md:26, ordenes/opencode-minimax.md:20.

5. "Mapa del árbol" (AGENTS/CLAUDE/GEMINI.md líneas 7-16, 9 bullets) duplica en sustancia la tabla "Componentes vivos" de README.md líneas 21-34 (con enlaces, más completa) y el diagrama+intro de docs/arquitectura.md líneas 5-25. Tres descripciones del mismo árbol de componentes.

6. "Despliegue exclusivamente FASE 3 con el dueño" aparece en AGENTS/CLAUDE/GEMINI.md:5 (x3) y de nuevo, más extenso, en README.md líneas 46-48.

7. **Redundancia INTRA-documento en 00-PROTOCOLO.md**: la sección "Convivir en main sin pisarse" (reglas 1-7, líneas 14-20) y la sección separada "Reglas de todo commit" (reglas 1-5, líneas 31-37) tratan el mismo terreno (gate, comentarios, `git mv`, declarar "hecho") en dos listas numeradas distintas — son fusionables en una sola lista de reglas de commit.

8. Los tres ficheros de orden (codex.md, gemini.md, opencode-minimax.md) repiten cada uno, en su línea 3-4, un resumen de las reglas del protocolo (directo a main, `git add` solo rutas propias, prohibido clean/reset/stash) que YA es lectura obligatoria del paso anterior del arranque (00-PROTOCOLO.md, ya leído justo antes según sus propios pasos "ARRANQUE (1)(2)(3)").

## 4. (b) CONTRADICCIONES / DESACTUALIZACIONES — el hallazgo más grave

**CONTRADICCIÓN 1 — el comando de gate no coincide entre el fichero auto-cargado y el protocolo que dice resumir:**
- AGENTS.md:23 / CLAUDE.md:23 / GEMINI.md:23: *"Gate por commit: `pnpm typecheck && pnpm lint` en verde"* (2 comandos).
- ordenes/00-PROTOCOLO.md:33: *"Gate antes de commit: `pnpm typecheck && pnpm lint && pnpm test:unit` en verde — test:unit es GLOBAL desde el 27-08"* (3 comandos, obligatorio).
- ordenes/gemini.md:3 confirma la versión de 3 comandos.
Los ficheros de identidad dicen literalmente "Reglas (las completas: `ordenes/00-PROTOCOLO.md`)" (AGENTS.md:18) pero el resumen que dan es MENOS estricto que el original — un agente que confíe solo en el fichero auto-cargado puede commitear código sin correr `test:unit`, violando la política real.

**CONTRADICCIÓN 2 — tope de subagentes, universal vs escalonado por instancia:**
- AGENTS.md:28 / CLAUDE.md:28 / GEMINI.md:28: *"tope 4"* (sin distinción de instancia).
- ordenes/00-PROTOCOLO.md:44: *"MiniMax 6 subagentes [...]; Gemini y Codex 4"*.
- ordenes/opencode-minimax.md:3: *"tu tope es 6, úsalo"* — el propio autor de la orden tuvo que parchear a mano el "4" incorrecto de AGENTS.md.
El fichero auto-cargado por MiniMax (no existe MINIMAX.md, usa AGENTS.md genérico) da el número equivocado para su propia instancia.

**CONTRADICCIÓN 3 — ordenes/codex.md: el 50% de "la lista" ya está resuelto, verificado con `wc -l` contra el repo real y contra `scripts/calidad-base.json`:**
De los 16 ficheros que codex.md:6-8 presenta como pendientes de partir (>800 líneas), **8 ya fueron partidos** (ya no están ni en el baseline de excepciones de `scripts/calidad-base.json`, que solo lista 36 ficheros y NINGUNO de estos 8):
- `durable-store.ts`: citado 2.060 líneas → real **90** (ahora barril de imports)
- `paste-runner.ts`: citado 1.900 → real **8** (ahora solo re-exports)
- `tmux.ts`: citado 1.529 → real **59**
- `engine.ts`: citado 1.322 → real **786** (ya bajo el umbral de 800)
- `output-parser.ts`: citado 1.189 → real **27**
- `harnesses/shared.ts`: citado 1.133 → real **29**
- `deliveries.ts`: citado 1.493 → real **27**
- `agents/fanin.ts`: citado 1.438 → real **289**
Los otros 8 (observability.ts, chain-control.ts, outbox.ts, messages.ts, configuration.ts, core.ts, relay-proxy.ts, agent-documents.ts) sí siguen >800 y coinciden con el baseline real. Además la lista **omite 2 ficheros que SÍ están hoy en el baseline >800** y no aparecen citados: `packages/adapter-sdk/src/shared-session/session.ts` (950 líneas) y `packages/store/src/repository/config.ts` (897 líneas). La orden mitiga esto pidiendo "mídela primero con wc -l" (codex.md:5), pero eso significa que la mitad del contenido de la orden es trabajo de re-verificación que el propio fichero debería haber ahorrado.

**CONTRADICCIÓN/DESACTUALIZACIÓN 4 — docs/arquitectura.md, tablas "Flujo 1/2" con cifras de líneas: 9 de 19 citas verificadas (47%) están desactualizadas**, algunas por un orden de magnitud:
- `paste-runner.ts` (línea 36): citado 1.900 → real 8 (237x de diferencia)
- `tmux.ts` (línea 36): citado 1.529 → real 59 (26x)
- `agent-leg.ts` (línea 46): citado 1.065 → real 206 (5x)
- `sessions.ts` (línea 47): citado 1.235 → real 248 (5x)
- `gateway-client.ts` (línea 47): citado 1.244 → real 498 (2,5x)
- `client.ts` (línea 55): citado 840 → real 337 (2,5x)
- `engine.ts` (línea 35): citado 1.322 → real 786
- dispatcher (línea 37): citado "822 líneas" (161+69) → real 230 (161→161 ok, pero handlers.ts real 69... total real es 161+69=230, no 822 — la propia suma citada en el texto no cuadra con los componentes)
- `repository.ts` (línea 34): citado 42 → real 43 (menor, ruido)
Este es el documento que README.md recomienda como PRIMERA lectura ("Si solo lees un documento, que sea este", docs/arquitectura.md:3) — su función es ayudar a calibrar antes de abrir un fichero, y casi la mitad de sus cifras concretas inducen a error (p.ej. hace pensar que `paste-runner.ts` es un monolito de 1.900 líneas cuando hoy es un barril de 8).

**DESACTUALIZACIÓN 5 — estado fechado dentro de una orden persistente:** ordenes/gemini.md:5 fija "ESTADO VERIFICADO al escribir esto (27-08): NADA de lo siguiente está hecho [...]" como un hecho puntual de hoy dentro de un fichero que sigue siendo la referencia después de hoy — en cuanto una tarea se complete y el fichero no se actualice, ese enunciado pasa a ser falso (mitigado parcialmente por la instrucción "si algo YA está verde, sáltalo y dilo en el reporte", pero la caducidad ya se demostró real en codex.md).

**HUECO ESTRUCTURAL 6 — Claude no tiene orden propia:** no existe `ordenes/claude.md`; en la tabla de sectores (00-PROTOCOLO.md:28) el sector de Claude es "plan-reestructura/, ordenes/, documentación [...], integración de merges, FASE 3" con dueño "Claude + dueño" — es el único sector sin fichero de orden autocontenido, rompiendo el principio "sesión nueva: NO necesitas historial; todo está aquí" que sí se aplica a Gemini/Codex/MiniMax. El coste de arranque de una sesión Claude fresca es indeterminado (depende de lo que el dueño le escriba a mano ese turno).

## 5. (c) % accionable vs relleno por fichero

| Fichero | Accionable (reglas/comandos que cambian conducta) | Relleno (narrativa/redundante/desactualizado) |
|---|---|---|
| AGENTS/CLAUDE/GEMINI.md | ~55% (líneas 5,18-28,30-32) | ~45%: mapa del árbol (líneas 7-16, redundante con README), historia (líneas 34-36, narrativa pura, redundante con 00-PROTOCOLO), y de las reglas "accionables" 2 de 8 son contradictorias con la fuente que dicen resumir |
| ordenes/00-PROTOCOLO.md | ~85% | ~15%: la narrativa del "cementerio de ramas" (líneas 7-8) y la redundancia intra-documento entre las dos listas de reglas de commit |
| ordenes/codex.md | ~90% en forma, pero **50% del contenido informativo (la lista) es dato caducado** — más que "relleno" es directamente incorrecto | L3 ("Excelente cierre anterior...") es elogio/narrativa, 0% accionable |
| ordenes/gemini.md | ~85% (5 tareas concretas y verificables) | ~15%: L3 resume protocolo ya leído; L5 es estado fechado |
| ordenes/opencode-minimax.md | ~85% | ~15%: L3 idem resumen redundante |
| README.md | ~40% accionable de verdad (única sección irreemplazable: "Desarrollo" líneas 38-42, comandos de instalación/gate que NO están en ningún otro de los 9 ficheros) | ~60%: intro/arquitectura redundante con identidad+arquitectura.md, tabla de componentes redundante con mapa del árbol, estado de despliegue redundante con identidad |
| docs/arquitectura.md | ~50% alto valor real (tablas Flujo 1/2/3, mapa de ops/, qué ignorar — nada de esto existe en otro sitio) | ~50% relleno o dañado: "tres ideas" redundante (5ª repetición del mismo hecho arquitectónico del corpus), y las cifras de líneas citadas están desactualizadas en casi la mitad de los casos verificados |

## 6. Conjunto mínimo propuesto

**Ya cabe hoy bajo 400 líneas por instancia** (93-119 líneas de arranque obligatorio real). La propuesta no es "recortar más volumen" sino eliminar las fuentes de deriva y de dato falso:

1. Un solo AGENTS.md CANÓNICO (~28 líneas): quitar mapa del árbol completo (apuntar a README §Componentes vivos), quitar "Historia mínima" (fusionar en la única narrativa que ya existe en 00-PROTOCOLO.md líneas 7-8), dejar solo las reglas que un agente necesita ANTES de haber leído el protocolo (no-ramas, `git add` propio, NO TOCAR, comentarios) y para el resto un puntero: "resto de reglas, tope de subagentes por instancia y gate exacto: `ordenes/00-PROTOCOLO.md`" — sin repetir cifras que puedan quedar desincronizadas (ahí nació la contradicción del gate y del tope 4/6).
2. CLAUDE.md y GEMINI.md pasan a ser STUBS de 3-4 líneas ("mismo contenido que AGENTS.md, ver ese fichero") — mantiene la convención de nombre que cada harness exige, elimina la triplicación real y el riesgo de que la próxima edición solo toque 2 de los 3.
3. 00-PROTOCOLO.md: fusionar "Convivir en main" (líneas 14-20) y "Reglas de todo commit" (líneas 31-37) en una sola lista (de 57 a ~48 líneas), sin perder contenido, solo sin repetirlo en dos sitios del mismo documento.
4. Corregir ordenes/codex.md: recalcular "la lista" a los 8 ficheros reales que siguen >800 (con sus cifras actuales) más los 2 omitidos (`shared-session/session.ts` 950, `repository/config.ts` 897); esto hace el fichero MÁS corto (de 16 a ~14 líneas) y 100% correcto en vez de 50% correcto.
5. README.md y docs/arquitectura.md quedan FUERA del arranque obligatorio de agentes (ya lo están de facto) — se documenta explícitamente en 00-PROTOCOLO.md como "lectura opcional/humana, no parte del ARRANQUE", para que ninguna orden futura los añada por inercia. Si se quiere que un agente tenga contexto arquitectónico, que sea el sector "Higiene de disco/docs" (MiniMax) quien lo pida puntualmente, no un paso automático de cada sesión.
6. Antes de dar por buena cualquier cifra de líneas dentro de docs/arquitectura.md, re-verificar con `wc -l` (9 de 19 están mal hoy) — o mejor, quitar las cifras absolutas de las tablas de Flujo 1/2/3 y dejar solo rutas + "qué buscar" (las cifras son las que caducan, la ruta y el propósito del fichero no).

Con (1)+(2)+(3) el arranque obligatorio por instancia baja de 119→~99 líneas (Gemini) y de 113→~93 (MiniMax), pero el beneficio real no es ese ahorro marginal de líneas: es pasar de 3 copias con 2 contradicciones ya confirmadas a 1 fuente única, y de una orden con 50% de datos falsos a una con 100% verificado.

- AGENTS.md, CLAUDE.md y GEMINI.md son BYTE-IDÉNTICOS salvo el título de la línea 1 (diff confirmado en las 3 combinaciones) — 33 de 36 líneas triplicadas letra por letra, sin mecanismo que garantice sincronía.
- CONTRADICCIÓN — comando de gate: AGENTS.md:23/CLAUDE.md:23/GEMINI.md:23 dicen 'pnpm typecheck && pnpm lint' (2 comandos); ordenes/00-PROTOCOLO.md:33 y ordenes/gemini.md:3 exigen además 'pnpm test:unit' (GLOBAL, obligatorio). Los ficheros auto-cargados dan un gate más laxo que el protocolo que dicen resumir (AGENTS.md:18).
- CONTRADICCIÓN — tope de subagentes: AGENTS.md:28/CLAUDE.md:28/GEMINI.md:28 fijan 'tope 4' universal; ordenes/00-PROTOCOLO.md:44 dice 'MiniMax 6 [...] Gemini y Codex 4'; ordenes/opencode-minimax.md:3 tiene que corregir a mano 'tu tope es 6, úsalo'. El fichero genérico que lee MiniMax (no existe MINIMAX.md) le da el número equivocado para su propia instancia.
- DESACTUALIZACIÓN GRAVE en ordenes/codex.md líneas 6-8: de los 16 ficheros listados como pendientes de partir (>800 líneas), 8 YA fueron partidos (verificado con wc -l y contra scripts/calidad-base.json, que ya no los incluye entre sus 36 excepciones): durable-store.ts citado 2.060→real 90; paste-runner.ts citado 1.900→real 8; tmux.ts citado 1.529→real 59; engine.ts citado 1.322→real 786; output-parser.ts citado 1.189→real 27; harnesses/shared.ts citado 1.133→real 29; deliveries.ts citado 1.493→real 27; agents/fanin.ts citado 1.438→real 289. Además la lista omite 2 ficheros que SÍ están hoy >800 en el baseline real: shared-session/session.ts (950) y repository/config.ts (897).
- DESACTUALIZACIÓN en docs/arquitectura.md (recomendado como primera lectura, línea 3): 9 de 19 cifras de líneas verificadas con wc -l están desactualizadas, hasta 237x de diferencia — paste-runner.ts citado 1.900 (línea 36) real 8; tmux.ts citado 1.529 (línea 36) real 59; agent-leg.ts citado 1.065 (línea 46) real 206; sessions.ts citado 1.235 (línea 47) real 248; gateway-client.ts citado 1.244 (línea 47) real 498; client.ts citado 840 (línea 55) real 337; engine.ts citado 1.322 (línea 35) real 786; la suma del dispatcher citada '822 líneas' (línea 37) no cuadra con el real 230.
- Redundancia x5-6: el hecho 'PostgreSQL única fuente / entrega pull por WS / dispatcher no reparte, es el segador' se repite casi igual en AGENTS.md:3, CLAUDE.md:3, GEMINI.md:3, README.md:3+9-10 y docs/arquitectura.md:21-23.
- Redundancia x4: 'código muerto se borra con git rm, git es el archivo, sin carpetas de cuarentena' en AGENTS.md:20, CLAUDE.md:20, GEMINI.md:20 y README.md:19; dentro de la misma línea 20 (en los 3 ficheros) el hecho 'no hay carpetas de cuarentena/archivo' se dice DOS veces seguidas en la misma frase.
- Redundancia x7 de la regla 'reporte ≤5 líneas': AGENTS.md:27, CLAUDE.md:27, GEMINI.md:27, 00-PROTOCOLO.md:57, ordenes/codex.md:16, ordenes/gemini.md:26, ordenes/opencode-minimax.md:20.
- Redundancia intra-documento en ordenes/00-PROTOCOLO.md: la lista 'Convivir en main sin pisarse' (reglas 1-7, líneas 14-20) y la lista separada 'Reglas de todo commit' (reglas 1-5, líneas 31-37) cubren el mismo terreno (gate, git mv, comentarios, declarar 'hecho') en dos sitios distintos del mismo fichero — fusionables.
- 'Mapa del árbol' de AGENTS/CLAUDE/GEMINI.md (líneas 7-16, 9 bullets) duplica en sustancia la tabla 'Componentes vivos' de README.md (líneas 21-34, más completa, con enlaces) y la intro de docs/arquitectura.md (líneas 5-25).
- Ni ordenes/00-PROTOCOLO.md ni AGENTS.md/CLAUDE.md/GEMINI.md ni las 3 órdenes de instancia referencian README.md o docs/arquitectura.md como lectura obligatoria (grep confirmado) — ambos se autodefinen para HUMANOS (docs/arquitectura.md:1, README.md:5). El arranque obligatorio real de una sesión fresca ya es de 93-119 líneas por instancia, muy por debajo del objetivo de 400.
- Hueco estructural: no existe ordenes/claude.md — Claude es el único de los 4 sectores (tabla 00-PROTOCOLO.md:22-29) sin fichero de orden autocontenido; su coste de arranque real es indeterminado, rompiendo el principio 'sesión nueva: todo está aquí' que sí cumplen Gemini/Codex/MiniMax.
- ordenes/gemini.md:5 fija un estado puntual ('ESTADO VERIFICADO al escribir esto (27-08): NADA de lo siguiente está hecho') dentro de un fichero pensado para persistir más allá de hoy — el mismo patrón de caducidad ya confirmado en ordenes/codex.md.
- La fórmula pedida líneas×12 SUBESTIMA el coste real ~2,1x en este corpus (4.608 tokens estimados vs ~9.720 por chars/4) porque son párrafos largos sin envolver, no líneas de código cortas — a tener en cuenta si se fija un techo de 'líneas' como proxy de coste.
- Única sección de los 9 ficheros sin equivalente en ningún otro sitio y 100% accionable: README.md líneas 38-42 ('Desarrollo': pnpm install/typecheck/lint/test:unit) — candidata clara a conservar en el conjunto mínimo pase lo que pase con el resto de README.md.

## tareas

Tracé el conjunto mínimo real (objetivo + imports directos + tipos usados + test que lo cubre) para las 5 tareas canónicas, usando docs/grafo.md como mapa de hubs e imports reales verificados fichero a fichero. Coste total: 26.737 líneas repartidas en 73 ficheros distintos para 5 arreglos puntuales — de 3.898 líneas (ruta de gateway) a 7.782 líneas (SQL de una operación del store). El acaparador más caro es la cadena de herencia simple de 19 clases en packages/store/src/repository/ (~11.235 líneas encadenadas por 'extends', ninguna con test colocado). El de mayor amplificación relativa es apps/console: arreglar un bug en MessagesPage.tsx (226 líneas) obliga a leer 5.696 líneas en 29 ficheros (25x) porque la vista de 'mensajería' importa directamente de la feature 'terminal' (746 líneas por un solo tipo). Hallazgo crítico de gobernanza: confirmé que el censo de 'candidatos huérfanos' de docs/grafo.md tiene falsos positivos verificados en apps/console — incluyendo ficheros con import ESTÁTICO normal (no solo dinámico) que SÍ están en uso en producción (App.tsx, TerminalPage.tsx, OperatorWorkspace.tsx, SessionStage.tsx, PtySessionDialog.tsx). Es el mayor riesgo de contaminación de los 5: una sesión fresca que confíe en esa lista para podar apps/console (70+ de los 105 huérfanos son de ahí) puede borrar rutas vivas.

- METODOLOGÍA: para cada tarea abrí el fichero objetivo, seguí sus imports directos con grep -n '^import', y por cada tipo importado desde un barrel (api/types.ts, packages/protocol/src/index.ts, packages/store/src/index.ts) localicé el fichero real que lo define con grep de la firma (interface/type/class). Añadí el test que cubre el objetivo cuando existe uno colocado; cuando no existe (3 de 5 tareas), reporto los ficheros de test más cercanos encontrados por grep y lo marco como 'disperso/ausente'. Todas las cifras de líneas son wc -l reales, no estimaciones.
- TABLA RESUMEN (tarea → nº ficheros del conjunto mínimo → líneas totales):
1) Bug en vista de consola (apps/console/src/features/messages/MessagesPage.tsx) → 29 ficheros → 5.696 líneas
2) Ruta nueva en el gateway (services/gateway/src/routes/console.ts) → 12 ficheros → 3.898 líneas
3) SQL de una operación del store (packages/store/src/repository/messages.ts, método publish()) → 12 ficheros → 7.782 líneas
4) Flujo del pty-agent (ops/pty-agent/cauce_pty_agent.py) → 4 ficheros → 3.942 líneas
5) Poller de telegram (services/telegram-bridge/src/poller.ts) → 16 ficheros → 5.419 líneas
TOTAL: 73 ficheros (con solapes reales entre tareas), 26.737 líneas sumadas. Factor de amplificación (líneas del conjunto / líneas del fichero objetivo): tarea 1 = 25,2x; tarea 2 = 4,2x; tarea 3 = 6,6x; tarea 4 = 1,5x; tarea 5 = 10,4x. La tarea 4 amplifica menos porque el objetivo YA es un monolito que contiene todo el flujo; la tarea 1 amplifica más porque el objetivo es delgado pero arrastra barrels y una feature vecina completa.
- TAREA 1 detalle (vista consola, apps/console/src/features/messages/MessagesPage.tsx, 226 líneas) — conjunto mínimo real: MessagesPage.tsx(226) + MessagesPage.test.tsx(557) + api/context.tsx(13) + api/use-resource.ts(143) + components/ui.tsx(277) + lib.ts(144) + navigation.ts(76) + features/terminal/fleet.ts(370) + features/terminal/session.ts(207) + features/messages/AgentRoster.tsx(149) + features/messages/ConversationPane.tsx(507) + features/messages/messages.css(394) + features/messages/queue-health.ts(145) + features/messages/roster.ts(195) + features/terminal/api.ts(746, solo para el tipo TerminalTarget de ~15 líneas) + api/types.ts(10, barrel) + api/types/deliveries.ts(242) + api/types/system.ts(42) + api/types/topology.ts(44) + api/types/auth.ts(33) + api/types/activity.ts(142) + features/terminal/cuerpo-del-mensaje.ts(58) + features/terminal/TerminalTranscript.tsx(111) + features/messages/desplazamiento.ts(41) + features/messages/durable-publish.ts(196) + features/messages/publish-receipt.ts(106) + features/messages/MessageTimeline.tsx(30) + api/client.ts(337) + api/client/core.ts(155). Verifiqué con grep '^interface|^type' en los 10 ficheros de api/types/ cuál define cada tipo realmente usado: todo el árbol solo necesita 5 de los 10 ficheros de api/types/ (deliveries, system, topology, auth, activity) — los otros 5 (agent-directives 372, chains 66, config 73, dlq 60, quotas 120 = 691 líneas) no hacen falta para esta tarea pero un agente cauteloso que no sepa el mapeo type→fichero suele leer el directorio entero.
- TAREA 2 detalle (ruta de gateway, services/gateway/src/routes/console.ts, 920 líneas) — conjunto mínimo real: routes/console.ts(920) + app.ts(408, wiring y fases de registro) + routes/shared.ts(179, helpers principal/replyError/CONNECTION_TOKEN_PATTERN usados por toda ruta) + auth.ts(547, AuthProvider) + facades.ts(261, capa de negocio que las rutas invocan) + console-security.ts(62) + packages/protocol/src/index.ts(6, barrel) + packages/protocol/src/schemas.ts(1115, de donde salen los tipos de request/response) + packages/store/src/index.ts(9, barrel) + packages/store/src/repository.ts(43, barrel de tipos de GatewayRepository) + console-audit.test.ts(117, patrón de test de ruta de consola existente) + tests/gateway-hardening/console-api-contract.test.ts(231, el test más cercano a 'contrato de todas las rutas de consola'). NO existe un routes/console.test.ts colocado: confirmé con find que services/gateway/src/routes/ no tiene ningún *.test.ts propio — la cobertura de rutas de consola vive repartida en 5 ficheros sueltos en la raíz de gateway/src (console-*.test.ts) más 18 ficheros en tests/gateway-hardening/ (5.479 líneas en total ahí).
- TAREA 3 detalle (SQL de operación del store, packages/store/src/repository/messages.ts método publish(), 1176 líneas) — conjunto mínimo real: messages.ts(1176) + config.ts(896) + outbox.ts(1280) + jobs.ts(177) + observability.ts(1415) + base.ts(12) + db.ts(454, DatabasePool) + packages/protocol/src/index.ts(6) + packages/protocol/src/schemas.ts(1115, de donde vienen PublishMessage/PublishResult) + tests/helpers/postgres.ts(279, arnés de test) + tests/gateway-hardening/publish-receipt-restart-postgres.test.ts(115) + tests/store-hardening/adversarial-postgres.test.ts(857). Los dos últimos son los MÁS CERCANOS que encontré con grep '.publish(' en tests/ — no hay un messages.test.ts colocado (confirmé: 0 ficheros *.test.ts dentro de packages/store/src/repository/). Esta cifra ya es solo la mitad del problema real: mapeé con grep 'extends.*Repository' TODA la cadena de herencia simple del store y es de 19 clases: BaseRepository → ObservabilityRepository → JobsRepository → OutboxRepository → ConfigRepository → MessagesRepository → DeliveryClaimsRepository → DeliveryAcksRepository → DeliveriesRepository → AgentsRepository → AgentProgressRepository → AgentResponseRepository → AgentFaninMaterializationRepository → AgentFaninRepository → AgentChainControlRepository → AgentNotificationsRepository → DeliveryControlRepository → QuotasRepository → CauceRepository, sumando 11.235 líneas en repository/. Como en JS/TS una clase solo ve a sus ANCESTROS (no a las subclases que vienen después en la cadena), el mínimo real para tocar messages.ts es 'solo' hacia BaseRepository (los 4.956 líneas que ya conté), pero un agente fresco no conoce de antemano la dirección de la cadena ni su forma — reconstruir ese mapa (que tuve que hacer yo con grep en 3 pasadas) es en sí mismo un coste de contexto no contado en las líneas.
- TAREA 4 detalle (flujo pty-agent, ops/pty-agent/cauce_pty_agent.py, 2667 líneas) — conjunto mínimo real: cauce_pty_agent.py(2667, target) + tests/test_read_governance.py(852, el test más grande y central) + tests/test_framing.py(189, protocolo de framing que sostiene todo el I/O) + tests/test_ticket.py(234, autenticación que abre la sesión) = 3.942 líneas en 4 ficheros. Confirmé con grep '^class |^def ' que dentro del propio fichero existe la clase PtyAgent entre las líneas 896 y 2641 — 1.745 líneas en UNA sola clase que mezcla framing, verificación de tickets, lanzamiento de tmux/openclaw y lectura/escritura de gobierno. Es el único de los 5 objetivos donde NO hay ninguna importación a otros ficheros del repo (confirmé el bloque de imports: solo librería estándar de Python) — es standalone a propósito, porque cauce-pty-launcher.sh lo copia como código efímero al host/contenedor remoto (mismo patrón documentado en la cabecera de rollout-pty.py). 11 de los 15 ficheros de test en ops/pty-agent/tests/ importan cauce_pty_agent.py directamente (confirmé con grep -rl); solo elegí 3 como 'mínimo real' para esta tarea concreta, pero cualquier cambio que toque estado compartido de la clase (self.*) puede en la práctica invalidar cobertura en los 11.
- TAREA 5 detalle (poller de telegram, services/telegram-bridge/src/poller.ts, 520 líneas) — conjunto mínimo real: poller.ts(520) + activity.ts(283) + addressing.ts(366) + config.ts(411) + ingress-body.ts(333) + progress.ts(276) + telegram.ts(384) + transcription.ts(124) + types.ts(356) + untrusted.ts(213) + attachments.ts(284) + redaction.ts(212) + packages/protocol/src/index.ts(6) + packages/protocol/src/schemas.ts(1115, de donde sale el tipo Origin) + services/telegram-bridge/test/bridge.test.ts(275) + services/telegram-bridge/test/bridge-lifecycle.test.ts(261) = 16 ficheros, 5.419 líneas. Confirmé con find que NO existe poller.test.ts: los tests viven en un directorio separado del código fuente (services/telegram-bridge/test/, sin 's', 18 ficheros) con nombres orientados a conceptos downstream (ingress, voice, fragments, redaction) en vez de al fichero que orquesta el flujo; localicé por grep 7 ficheros de test que tocan el poller indirectamente y elegí los 2 más centrales.
- ACAPARADOR 1 — packages/protocol/src/schemas.ts (1115/1116 líneas, hub #1 del grafo con 146 referencias entrantes al barrel index.ts que lo re-exporta en 6 líneas). Aparece en 3 de las 5 tareas (gateway, store, telegram) porque mezcla en un solo fichero Zod-schema los tipos de auth, deliveries, publish, terminal, quotas, config y chains sin separación por dominio. Cada tarea solo necesita entre 1 y 3 tipos concretos (verificado con grep de las firmas exactas usadas) pero se ve forzada a considerar el fichero completo. Parte EVITABLE: alta — es un solo fichero plano, sin razón operativa (a diferencia del caso 3) para no partirlo por dominio detrás del mismo barrel.
- ACAPARADOR 2 — packages/store/src/repository/* (hub #2 del grafo, packages/store/src/index.ts con 125 refs a un barrel de 9 líneas). Es el más caro en términos absolutos: 11.235 líneas repartidas en 19 clases encadenadas por herencia simple (extends), CERO ficheros de test colocados dentro de repository/. Parte evitable: alta en el diseño (herencia simple donde cabría composición — cada 'FooRepository' podría ser una propiedad inyectada en vez de un eslabón obligatorio de la cadena de prototipos), pero la ejecución del cambio pertenece al sector de Codex según ordenes/00-PROTOCOLO.md (packages/store/src/** es su sector exclusivo) — no es terreno para que cualquier sesión fresca toque de paso.
- ACAPARADOR 3 — apps/console/src/api/types.ts (hub #3 del grafo, 120 refs a un barrel de 10 líneas que hace 'export * from' de 10 ficheros en api/types/, 1204 líneas). A diferencia de los dos anteriores, este SÍ está bien seccionado por dominio internamente (auth.ts, deliveries.ts, activity.ts, etc. — verifiqué que cada tipo usado en la tarea 1 vive en exactamente uno de esos 10 ficheros, sin mezcla). Lo evitable aquí es menor y distinto: no es el contenido sino el patrón de barrel puro (export *) que oculta el mapeo tipo→fichero y obliga a grep o a leer los 10 ficheros para saber cuál abrir. apps/console/src/api/client.ts (337 líneas) tiene el mismo patrón pero mezclado con lógica real (la clase CauceApi), lo que lo hace algo más defendible.
- ACAPARADOR 4 — fuga cruzada de features dentro de la consola: apps/console/src/features/messages importa directamente de apps/console/src/features/terminal (fleet.ts, session.ts, cuerpo-del-mensaje.ts, TerminalTranscript.tsx, y el tipo TerminalTarget de terminal/api.ts, 746 líneas). Es la causa principal de que la tarea 1 tenga el factor de amplificación más alto (25x) de las 5 tareas: una vista de 226 líneas arrastra una feature vecina completa por no tener un módulo neutral compartido (domain/fleet o shared/) entre 'mensajería' y 'terminal'. El propio nombre del directorio 'features/' sugiere aislamiento que en la práctica no existe.
- ACAPARADOR 5 — ausencia o dispersión de test colocado: confirmé con find que NINGUNA de las 3 tareas de backend (ruta de gateway, SQL de store, poller de telegram) tiene un *.test.ts que comparta nombre con su fichero objetivo. La cobertura real está dispersa en directorios de integración separados (tests/gateway-hardening/ 18 ficheros/5.479 líneas, tests/store-hardening/ 9 ficheros/3.305 líneas, services/telegram-bridge/test/ 18 ficheros) con nombres orientados a features de negocio, no a los ficheros fuente. El coste no está en las líneas que reporté (ya son las más cercanas por grep) sino en el paso previo invisible: decidir CUÁL de 18-26 ficheros de test es 'el que cubre esto', que hoy exige una búsqueda abierta por cada tarea.
- ALERTA DE GOBERNANZA — docs/grafo.md tiene falsos positivos verificados en su censo de 105 'candidatos huérfanos', concentrados en apps/console (más de 70 de los 105 son de ahí). Confirmé que apps/console/src/App.tsx importa dinámicamente (await import(...)) a LiveFleetPage, AccountsPage, MessagesPage, QueuesPage, ObservabilityPage, ConfigPage y TerminalPage — las 7 vistas principales de la consola, TODAS listadas como huérfanas. Peor aún: seguí la cadena TerminalPage.tsx → OperatorWorkspace.tsx → SessionStage.tsx → PtySessionDialog.tsx y las 4 se importan con 'import' ESTÁTICO normal (no solo dinámico) y las 4 aparecen igualmente en la lista de huérfanos — así que el fallo de detección no es solo el code-splitting dinámico, es más amplio dentro de apps/console. El documento ya advierte 'verificar antes de tocar', pero dado que el dueño reporta que los modelos 'se contaminaban' reparando cosas aquí, y que este censo es precisamente la herramienta que ordenes/00-PROTOCOLO.md dice usar, el riesgo concreto es que una sesión fresca use esta lista para justificar borrar código 'muerto' y elimine rutas de producción en vivo.
- LOS 5 CAMBIOS ESTRUCTURALES QUE MÁS REDUCIRÍAN EL COSTE (priorizados por impacto medido / esfuerzo):
1. Partir packages/store/src/repository/* de herencia simple (19 clases, 11.235 líneas encadenadas, 0 tests colocados) a composición (repositorios inyectados como propiedades de CauceRepository). Es el que más líneas quita por tarea tocada (tarea 3 pasaría de ~5.000 a ~1.200-1.600 líneas de ascendencia obligatoria) y el que más riesgo de 'contaminación' elimina, porque hoy nadie puede saber sin grep en qué dirección mira la cadena. Pertenece al sector de Codex (packages/store/src/**) — requiere su ejecución explícita, no la de cualquier sesión de paso.
2. Partir packages/protocol/src/schemas.ts (1115 líneas, usado en 3 de 5 tareas) en módulos por dominio detrás del index.ts actual, igual que ya está hecho (bien) en apps/console/src/api/types/. Coste bajo, beneficio alto y transversal.
3. Dar test colocado a cada operación de packages/store/src/repository/*.ts y a cada grupo de rutas de services/gateway/src/routes/*.ts, en vez de depender solo de tests/gateway-hardening y tests/store-hardening. Convierte una búsqueda abierta ('¿cuál de 18 ficheros cubre esto?') en abrir un fichero con el mismo nombre.
4. Extraer a un módulo neutral (p. ej. apps/console/src/domain/fleet.ts) lo que hoy comparten features/messages y features/terminal (FleetAgent, OperatorSession, truncado de cuerpo de mensaje, TerminalTranscript), para que una vista de mensajería no arrastre 746 líneas de terminal/api.ts por un solo tipo. Es el cambio que más bajaría el factor de amplificación de la tarea con peor ratio (25x).
5. Corregir el generador de docs/grafo.md (pnpm grafo) para resolver import() dinámico y las importaciones intra-directorio dentro de apps/console, o en su defecto retirar/marcar explícitamente como 'no fiable dentro de apps/console' la sección de candidatos huérfanos hasta corregirlo — dado que confirmé huérfanos falsos incluso con import estático normal, no es un matiz menor sino un censo que hoy puede inducir a borrar código en producción.

## nombres

Auditoría de coherencia de nombres e idioma sobre todo el repo (apps/console, packages/{protocol,adapter-sdk,store,mcp-fleet-monitor}, services/{gateway,telegram-bridge,terminal-relay,dispatcher}, ops/*, tests/*, scripts/, deploy/), leyendo listados completos de directorio (no solo 20 ficheros) más grep de exports e import-paths para medir coste de renombrar contra el grafo. docs/mapa-de-ficheros.md NO EXISTE en este repo (verificado con find; solo existen docs/grafo.md, arquitectura.md, directiva-ficheros-del-agente.md, terminal-pty.md, threat-model.md) — no hay nada que cruzar con ⚠ en ese fichero. HALLAZGO PRINCIPAL: hay dos convenciones de facto distintas y bien respetadas — (1) casing: kebab-case para todo fichero .ts/.tsx/.mjs/.py/.sh no-componente (CERO camelCase encontrado en todo el repo), PascalCase reservado a componentes React de exportación única, snake_case en Python SOLO cuando el fichero se importa como módulo (obligatorio por sintaxis, no arbitrario); sufijo de test 100% '.test.*' (CERO '.spec.*' en 364 ficheros de test) — en estos dos ejes el repo es sorprendentemente disciplinado. (2) idioma: los comentarios son mayoritariamente español en TODAS las zonas por igual (~50-75% ficheros con al menos una línea de comentario en español, incluso en zonas con 0% de identificadores en español) — eso no es contaminación, es consistente. La contaminación real de idioma está en NOMBRES DE FICHERO e IDENTIFICADORES EXPORTADOS, y no sigue una frontera de paquete sino que aparece como 'islas' español dentro de ficheros predominantemente ingleses, incluida la peor forma posible: funciones en español y en inglés DENTRO DEL MISMO fichero (packages/adapter-sdk/src/harnesses/shared/errors.ts, packages/adapter-sdk/src/sdk/client.ts). El caso más caro/relevante es packages/protocol/src/agent-profile.ts: fichero con nombre inglés cuyos tipos exportados (ArnesDelAlias, HechosDelAlias, ContextoDeAlias) son 100% español y viajan re-exportados por el barril packages/protocol/src/index.ts (el hub nº1 del repo, 146 refs entrantes); HechosDelAlias solo se usa en 9 ficheros de 4 zonas distintas, así que el coste de traducir es bajo en términos absolutos pero cruza fronteras de sector. NO se proponen renombrados — se entrega el inventario con coste medido para que el dueño decida.

- CONVENCIÓN DE FACTO — CASING (global, limpio): en TODO el repo, ficheros lógicos (.ts/.tsx no-componente, .mjs, .py, .sh) son kebab-case sin excepción — verificado con `find ... | xargs basename | grep -E '^[a-z][a-zA-Z0-9]*[A-Z]'` sobre .ts/.mjs/.py/.sh: CERO coincidencias camelCase en todo el árbol (excluyendo node_modules/dist). Componentes React (.tsx con export por defecto de un componente) son PascalCase (AccountsPage.tsx, DeliveryTable.tsx, etc.). Esta es la única regla verdaderamente universal del repo — no hay que tocarla, solo documentarla en el protocolo para que las sesiones frescas dejen de dudar.
- CONVENCIÓN DE FACTO — SUFIJO DE TEST (global, limpio): 364 ficheros '*.test.*' en todo el repo, CERO '*.spec.*' (`find . -name '*.spec.*'` → vacío). El ejemplo del enunciado ('.test vs .spec') es un FALSO POSITIVO en este repo: no hay mezcla, es 100% '.test.'.
- FALSO POSITIVO A NO TOCAR — snake_case Python NO es inconsistencia sino requisito de import: ops/scripts/container_alias_lib.py, container_ops_digest.py, manifest_lib.py, dlq_cli.py son snake_case porque se importan con `from X import Y` (Python no admite guiones en identificadores de módulo) — confirmado por grep: container_alias_lib se importa en 6 ficheros (generate-units.py, fleet-watchdog.py, physical-fleet-gate.py, generate-container-units.py, pin-container-release.py, container-alias-query.py, generate-telegram-config.py), manifest_lib en 3 más, dlq_cli en 5 más. Coste de 'arreglar' esto a kebab: ROMPE 14+ imports, beneficio CERO (no es un bug). Recomendación: no tocar, y si acaso documentar la regla ('snake_case solo si se importa como módulo Python') en el protocolo para que nadie lo marque como hallazgo falso otra vez.
- FALSO POSITIVO A NO TOCAR — prefijo test_*.py en ops/tests/ y ops/pty-agent/tests/ (test_alias_lock_exec.py, test_fleet_watchdog.py, etc., ~28 ficheros) frente a *.test.mjs en el mismo directorio ops/tests/ (alias-runner.test.mjs, container-cutover.test.mjs, etc.): no es incoherencia editorial sino requisito de framework — pytest solo autodescubre `test_*.py`/`*_test.py`, vitest usa `*.test.ts/.mjs`. Cada convención vive dentro de su propio lenguaje/runner y no se cruzan.
- FALSO POSITIVO A NO TOCAR — sufijo '-postgres.test.ts' (58 ficheros en packages/store/test, services/telegram-bridge/test, tests/gateway-hardening, tests/store-hardening, tests/unit) es 100% consistente: siempre guion, siempre sufijo, nunca punto. El ejemplo del enunciado ('-postgres vs .postgres') NO se materializa en este repo — no hay variante '.postgres'.
- PATRÓN LEGÍTIMO A NO TOCAR — pares '<nombre>-core.mjs' + '<nombre>-core.d.mts' (deploy/outbox-metrics-core.{mjs,d.mts}, deploy/reconcile-stale-console-outbox-core.{mjs,d.mts}, deploy/release-state-metrics.{mjs,d.mts}) y '<nombre>.mjs' + '<nombre>.d.mts' en tests/terminal-pty/ (certs, fake-gateway, fake-pty-agent, protocol): es tipado a mano para módulos consumidos sin build completo, ya confirmado como 'en uso, sin import literal por nombre' por ordenes/reportes/claude-censo-comentarios-basura.md líneas 306 y 311. No es un sufijo inconsistente, es una convención deliberada de dos ficheros por módulo.
- DESVIACIÓN DE SUFIJO REAL (única encontrada) — services/gateway/src/health-schema037.pg.test.ts usa '.pg.test.ts' en vez del '-postgres.test.ts' que usan los otros 57 ficheros equivalentes del repo, y además pega el número de esquema sin guion ('schema037'). Coste de renombrar a 'health-schema-037-postgres.test.ts' (o similar): CERO referencias de código encontradas (`grep -rl 'schema037\|health-schema037\|\.pg\.test'` solo devuelve scripts/calidad-base.json, que es el baseline mecánico del trinquete de scripts/calidad.mjs y se regenera solo con `--update`). Es el candidato más barato de todo el informe si el dueño quiere una sola corrección de sufijo.
- IDIOMA — CONVENCIÓN DE FACTO en comentarios (global, consistente): heurística de palabras vacías españolas en comentarios por zona — apps/console 207/280 ficheros, packages/adapter-sdk 83/138, packages/store 46/85, services/gateway 44/75, services/telegram-bridge 21/40, services/terminal-relay 15/33, ops/scripts 8/60 (más bajo por ser scripts de shell/Python con muchos comentarios de shebang/licencia), scripts/ 4/6, deploy 3/20. Conclusión: comentar en español es la norma en TODO el repo por igual, no varía por zona ni por dueño de sector — no es la fuente de la 'duda' que reporta el dueño.
- IDIOMA — IDENTIFICADORES EXPORTADOS, zona por zona limpia: services/telegram-bridge/src (20 ficheros), services/terminal-relay/src (23 fuente), services/dispatcher/src (6), packages/store/src (34) y deploy/ (20) tienen el 100% de sus nombres de fichero e identificadores exportados en inglés — cero excepciones encontradas en listado completo. Son las zonas 'de referencia' para lo que debería ser el estándar.
- IDIOMA — ISLA ESPAÑOLA en apps/console (zona de Gemini, 285 ficheros): es la única zona con una convención de facto propia y coherente en sí misma, aunque bilingüe: vocabulario de DOMINIO (lo que ve el operador humano en la UI) va en español — directiva, perfil, ficheros, historial-rol, plazas, colas, arneses, capas, alta-rapida, interruptores, campos-inertes, fecha-relativa, menu-movil, desplazamiento, redimensionado, densidad-observacion, cuerpo-del-mensaje, foco-de-entrega, ultimo-error, veredicto-vocabulario — mientras que vocabulario TÉCNICO/infra va en inglés: session, api, types, plugin, fleet, roster, activity, auth, config, collection, use-resource, pty-*, layout-*, agent-state-*. Los componentes .tsx siguen el mismo patrón (AltaDeEspacios.tsx, ArnesesPanel.tsx, DirectivaModal.tsx, FicherosTab.tsx, HistorialRol.tsx, PerfilTab.tsx, PlazasColgadas.tsx en español; AccountsPage.tsx, DeliveryTable.tsx, MessagesPage.tsx, ObservabilityPage.tsx, AuthGate.tsx en inglés). Es una convención DEFENDIBLE (dominio de negocio en el idioma del negocio) pero NO está escrita en ningún sitio, así que una sesión fresca no puede inferir la regla sin releer decenas de ficheros — coste de formalizarla en el protocolo: una línea de documentación, coste cero de código.
- IDIOMA — CONTAMINACIÓN GRAVE (fichero mixto español+inglés en la MISMA lista de exports): packages/adapter-sdk/src/harnesses/shared/errors.ts mezcla esInterrupcionDelDuenio, esDiagnosticoDeArranque, nuncaEmpezoElTurno, elTestigoDiceQueNoEmpezo, abortadoPorApagado, sinMarcaDeArranque (español) con abortReason, cancellationMessage, executionError, sanitizeProcessOutput (inglés) en el mismo fichero de nombre inglés. Es el peor patrón para una sesión fresca: no puede aplicar una regla por fichero, tiene que mirar función por función. Coste: esInterrupcionDelDuenio usada en 3 ficheros, nuncaEmpezoElTurno en 4 — barato de traducir si se decide.
- IDIOMA — CONTAMINACIÓN GRAVE 2: packages/adapter-sdk/src/sdk/client.ts (fichero predominantemente inglés, es un hub de adapter-sdk) tiene dos funciones exportadas en español clavadas en medio: siembraAplicada, siembraHabilitada (usadas cada una en 2 ficheros). Mismo patrón de 'parche español' dentro de fichero inglés.
- IDIOMA — CASO MÁS CARO (nombre inglés, contenido 100% español, en el hub nº1): packages/protocol/src/agent-profile.ts declara ArnesDelAlias, HechosDelAlias, ContextoDeAlias, lineasDeArnes, componerBloqueDePerfil — todo en español — pero el propio fichero se llama en inglés y se re-exporta con `export *` desde packages/protocol/src/index.ts (146 refs entrantes al índice, hub nº1 del grafo). Coste de renombrar medido por identificador (grep de uso real, no solo import-path): ArnesDelAlias 3 ficheros, HechosDelAlias 9 ficheros (packages/protocol, packages/adapter-sdk ×2, packages/store, services/gateway/console ×2, tests/unit), ContextoDeAlias 11 ficheros (añade services/gateway/console/agent-profile.routes.ts y agent-profile-runtime.ts), componerBloqueDePerfil 4, lineasDeArnes 2. Es barato en términos absolutos (2-11 sitios cada uno) pero cruza 4 sectores de propiedad distintos (protocol es zona compartida; adapter-sdk y console pertenecen a Gemini; store/gateway a Codex) — cualquier renombrado necesita coordinación entre dueños, no es un `git mv` de una sola instancia.
- IDIOMA — mismo patrón repetido en 3 ficheros más de packages/protocol/src, el paquete MÁS compartido del repo (13 ficheros fuente, hub nº1): marcas-de-bloque.ts (bloqueEntreMarcas 2 refs, conBloqueGestionado 6 refs, bloqueDePerfil, sinBloqueDePerfil — todo español) y ficheros-del-arnes.ts (ficherosDelArnes 7 refs, nombresDelArnes 6 refs, ErrorDeTopeDelArnes, FicheroGenerado, PoliticaDeFichero — todo español). Ya señalados independientemente por ordenes/reportes/claude-censo-comentarios-basura.md como los peores ficheros por comentario ceremonial (marcas-de-bloque.ts: 10/17 líneas borrables; agent-profile.ts: 35/51 borrables) — dos auditorías distintas convergen en los mismos 2 ficheros por motivos distintos (comentarios vs idioma), lo que refuerza que son un punto de fricción real, no ruido de metodología.
- IDIOMA — packages/adapter-sdk/src/context/ es un subdirectorio ÍNTEGRAMENTE español dentro de un paquete 97% inglés (146 ficheros fuente): perfil-a-contexto.ts (serializarEstable, proyeccionOpenclaw, rolBreveDelPerfil, compilarContexto, CLAVES_PROHIBIDAS_OPENCLAW) y siembra-del-perfil.ts (sembrarPerfilDelArnes, resumenDeLaSiembra, marcaDeTiempo, directorioDelArnes, DiscoDelArnes, EscrituraDelArnes) — 3 refs de import-path cada uno. Además harnesses/contexto-fijo.ts (mismo paquete, fuera de context/) es 100% español: resumirContextoFijo, elFicheroYaLoDice, renglonDeContextoFijo, rutaDelContextoFijo, sembrarContextoFijo, motivoDeReenvio — 9 ficheros lo importan por ruta.
- IDIOMA — services/gateway/src (zona de Codex, 75 ficheros fuente, 97% inglés) tiene 4 islas puntuales: routes/legado-candidato.ts (nombre de fichero en español 'legado-candidato' pero contenido 100% inglés — LegacyCandidateRouteOptions, registerLegacyCandidatePublishIntentRoutes; 1 sola referencia de import, coste trivial de traducir el NOMBRE del fichero a legacy-candidate.ts), console/sonda-compartida.ts (SondaCompartida, sondaDiferida — 2-3 refs), terminal/hechos-del-registro.ts (hechosDelRegistro — 3-4 refs) y console/agent-profile.routes.ts (fichero inglés con interfaces internas RespuestaDelPerfil 2 refs, PerfilAplicado 1 ref). Todos de coste bajo (1-4 ficheros) por ser específicos de gateway y no cruzar a otros sectores.
- CONVENCIÓN DE FACTO POR ZONA — resumen para que una sesión fresca no dude: services/telegram-bridge, services/terminal-relay, services/dispatcher, packages/store, deploy/ → 100% inglés, kebab-case, sin excepciones (zonas 'de oro'). services/gateway, packages/adapter-sdk → ~95% inglés kebab-case con islas puntuales españolas de bajo coste (listadas arriba), tratables fichero a fichero sin tocar el resto. packages/protocol → mayoritariamente inglés PERO sus 2-3 ficheros con más contenido español (agent-profile.ts, marcas-de-bloque.ts, ficheros-del-arnes.ts) son justo los que viajan por el export* del índice del hub nº1 — este es el paquete donde vale más la pena intervenir primero porque lo lee todo el mundo. apps/console → bilingüe por diseño (dominio=español, infra=inglés), consistente en sí mismo pero no documentado. ops/scripts, ops/guardias, ops/console-legibilidad, scripts/ → mezcla real sin patrón claro (ver hallazgo siguiente).
- NOMBRES DE DIRECTORIO también mezclan idioma, no solo ficheros: dentro de ops/ conviven subdirectorios en español (ops/guardias, ops/console-legibilidad) con subdirectorios en inglés (ops/harness, ops/observability, ops/schemas, ops/container-runtime, ops/patches, ops/manifests, ops/openclaw-gateway) — la elección no sigue ningún criterio de dominio-vs-infra como en apps/console, parece simplemente quién escribió cada carpeta primero. Dentro de ops/guardias (nombre español) conviven ficheros ingleses (cred-guard.py, cred-guard.sh) con españoles (cauce-cuentas.py, cauce-envoltorio-local.sh, cauce-huerfanas.sh, hegel-ventas-checkin.py). Dentro de ops/console-legibilidad (nombre español) conviven medir-terminal.mjs/medir-tipografia.mjs/medir.mjs/servir-con-csp.mjs (español) con probe.mjs y cdp.mjs (inglés). Coste de renombrar cualquiera de estos: bajo (son scripts de operación, tocados por pocos consumidores, fuera del grafo de imports de TypeScript), pero el BENEFICIO de arreglar los NOMBRES DE CARPETA es mayor que el de un fichero suelto porque una carpeta es lo primero que ve/predice una sesión fresca al navegar.
- scripts/ (7 ficheros, tooling raíz compartido por todos los sectores) es mayoritariamente español (calidad.mjs, gancho-de-paquetes.mjs, grafo.mjs, paquetes-de-este-arbol.mjs) con 2 excepciones inglesas (test-all.mjs, test.sh). Bajo impacto por tamaño (6 ficheros) pero alta VISIBILIDAD porque toda sesión fresca ejecuta `pnpm grafo`/`pnpm calidad` desde el protocolo — vale la pena, si se documenta algo, documentar que esta carpeta es 'español por convención' explícitamente en 00-PROTOCOLO.md en vez de dejar que se infiera.
- NOMBRES QUE MIENTEN — no existe docs/mapa-de-ficheros.md en el repo (confirmado con find recursivo; solo aparece citado por el enunciado de la tarea, el fichero no existe). No hay ⚠ que cruzar. El fichero packages/protocol/test/ficheros-que-no-mienten.test.ts existe pero es sobre otra cosa: verifica que los ficheros GENERADOS para el arnés (SOUL.md, etc.) no queden con contenido rancio tras editar el perfil — no es un censo de nombres de fichero fuente engañosos, para no confundir a quien lea este informe.
- NOMBRES QUE MIENTEN — único caso ya documentado y verificado (no derivado por mí, citado con su evidencia): ordenes/reportes/claude-revision-ola3.md líneas 77-81, sobre ops/pty-agent/tests/test_write_governance_batch.py:167 — el test se llama test_allowlist_is_exact_and_never_accepts_openclaw_json ('la lista blanca es exacta') pero solo prueba que openclaw.json es rechazado por estar en NEVER_SERVE_BASENAMES (una lista de bloqueo distinta, comprobada ANTES que la lista blanca) — nunca prueba la exactitud de la propia allowlist. Probado en copia por esa revisión: metiendo openclaw.json DENTRO de `allowed` el test sigue en verde. Es un nombre que promete una garantía de seguridad (exactitud de la lista blanca) que el cuerpo del test no ofrece — clasificado [crítica] en esa revisión, no solo cosmético. Coste de arreglar: no es un rename, es escribir el test que falta; fuera del alcance de este censo de nombres pero merece mención porque es la instancia más grave de (d) encontrada en todo el repo.
- COBERTURA DE MUESTREO: se leyeron los listados COMPLETOS (no solo 20 ficheros) de apps/console/src (280), packages/protocol/src (7), packages/store/src (34), packages/adapter-sdk/src (83 de 138, resto son manifests/bin repetitivos ya cubiertos por patrón), services/gateway/src (75), services/telegram-bridge/src (20), services/terminal-relay/src (23), services/dispatcher/src (6), ops/scripts (60), ops/tests (24), ops/pty-agent (17 no-cache), tests/unit (40), tests/gateway-hardening (18), tests/store-hardening (9), tests/terminal-pty/integration/e2e (14), scripts/ (6), deploy/ (27) y subcarpetas menores de ops/ (guardias, harness, observability, schemas, container-runtime, patches, openclaw-gateway, console-legibilidad, manifests) — cobertura muy por encima del mínimo de 20/paquete pedido, así que las cifras de arriba no son extrapolación sino censo casi completo (excepción: no se leyó línea a línea el interior de los 731 ficheros de packages/adapter-sdk incluyendo manifests/scripts internos repetitivos, que no aportan señal de idioma nueva sobre el patrón ya identificado).

## plano

A4 (DISEÑO) — árbol objetivo por zona con checklist de mudanza derivado del grafo + git grep. Raíz del repo: /datos/workspaces/zeus/cauce-v3 (rutas dentro de los checklists son relativas a esa raíz, que es lo que `git mv` necesita). Veredicto por zona: (1) deploy/ SÍ merece reorganización y es la más barata de las cuatro, porque la ruta EN LA IMAGEN se puede dejar intacta (`/app/deploy/`) aunque la ruta EN EL ÁRBOL cambie — eso reduce ~60 referencias externas a 11 líneas reales; pero deploy/** es fila NADIE hasta FASE 3, así que el plan es para ejecutar CON el dueño, no ahora. (2) tests/ NO merece fusión: el split unit/integration/e2e/hardening-* está justificado por la matriz de `test:*`, por los timeouts (120-180s con postgres vs unit rápido) y porque tests/helpers/postgres.ts está anclado por 46 ficheros a `../../../`; el defecto real de tests/ no es el número de carpetas sino que `tests/unit` no contiene tests unitarios (25 de 40 leen ficheros de deploy/, ops/ o scripts/ desde disco) y que hay DOS hogares para tests de ops (`ops/tests/` .mjs y `tests/unit/` .ts) — eso es un split de RUNNER, no de carpeta, y no se arregla con git mv. (3) packages/adapter-sdk/src: los cuatro límites (sdk/shared-session/harnesses/bin) son los CORRECTOS y no hay que moverlos; lo que está mal es un ciclo sdk↔harnesses solo-de-tipos, una quinta carpeta sin nombrar (context/, 2 ficheros) y 4 ficheros "fake" repartidos en 3 niveles — se arregla con una edición de ~10 líneas de import, no con una mudanza (mover esta zona rompe el trinquete en 39 ficheros). (4) ops/: NO se reordena nada operativo antes de FASE 3 — 6 ficheros de ops/scripts/ son ExecStart de units instaladas VIVAS en /opt y %h/.local, 28 rutas están escritas a mano en container_ops_digest.py, y ops/generated/** está verificado byte a byte contra SHA256SUMS/OPERATIONS.sha256; lo único movible hoy son 3 .md de la raíz de ops/ sin una sola referencia entrante. Hallazgo transversal que explica la "contaminación": tres mecanismos (trinquete por ruta, digests que hashean la ruta, globs planos) convierten un `git mv` puro en gate rojo o en cobertura silenciosamente perdida, y el modelo que lo sufre "repara" editando contenido en el mismo commit — exactamente lo que el protocolo prohíbe.

- === LEY TRANSVERSAL: POR QUÉ UN `git mv` PURO CONTAMINA AQUÍ (leer antes de las 4 zonas) ===

Tres mecanismos indexan por RUTA, no por contenido. Ninguno está documentado en AGENTS.md ni en 00-PROTOCOLO.md. Un modelo fresco que hace `git mv` limpio ve el gate ponerse rojo (o peor: verde y más flojo) por razones que no entiende, y lo 'arregla' editando comentarios/código en el mismo commit — que es justo la mezcla que el protocolo regla 3 prohíbe.

(A) TRINQUETE POR RUTA — /datos/workspaces/zeus/cauce-v3/scripts/calidad.mjs
  Línea 45: `const tope = base.lineas[f] ?? MAX` (800)
  Línea 47: `const topeF = base.fechas[f] ?? 0`
  Línea 50: `const topeC = base.comentarios?.[f] ?? Math.ceil(v.lineas * 0.15)`
  Al mover un fichero desaparece su clave y CAMBIA su tope. Simulación ejecutada sobre el árbol actual (ficheros que ponen ROJO el trinquete si se mueven sin re-clavar la clave):
    deploy/            2 ficheros  (liveness-probe.mjs 33 com > 28; smoke.sh 10 > 7)
    tests/            14 ficheros  (incl. tests/helpers/postgres.ts 73 > 42; gateway-hardening/delivery-admission.test.ts 924 líneas > 800; store-hardening/adversarial-postgres.test.ts 858 > 800)
    packages/adapter-sdk/  39 ficheros (incl. test/shared-session.test.ts 5455 líneas; test/engine.test.ts 2795 + 3 fechas; src/shared-session/session.ts 950)
    ops/              25 ficheros  (incl. pty-agent/cauce_pty_agent.py 2668; container-runtime/cauce-container-runtime.py 1665; tests/container-supervisor.test.mjs 1762)
  Corolario MENOS obvio y más peligroso: cuando el fichero movido NO supera el 15%, el gate queda VERDE pero el trinquete se AFLOJA en silencio (p.ej. deploy/migrate.mjs pasa de tope 1 a tope ceil(líneas*0.15)). Los 13 ficheros de deploy/ que propongo mover caen todos en este caso: mover sin re-clavar no rompe nada visible y relaja 13 trinquetes.
  Además calidad.mjs línea 53 sólo poda `base.lineas` de ficheros desaparecidos; `fechas` y `comentarios` acumulan claves muertas para siempre. YA HAY UNA: `"deploy/shadow-guard.mjs": 1` en la sección `comentarios` de scripts/calidad-base.json, fichero que no existe en `git ls-files`.
  NO usar `node scripts/calidad.mjs --update` como remedio en el checkout compartido: regenera el baseline COMPLETO desde el estado actual del árbol, y con otras instancias trabajando a medias en main hornearía sus números en curso. El remedio correcto es re-clavar las claves a mano, en el MISMO commit del mv.

(B) DIGESTS QUE HASHEAN LA RUTA — ops/scripts/source-digest.py, función `compute()`:
    `digest.update(len(relative)...); digest.update(relative); digest.update(content)`
  El digest incluye la ruta relativa. `DOMAIN_INPUTS` (línea 144-153) mete `"deploy"` ENTERO en los dominios runtime y console (por rglob), así que ningún fichero se escapa de la cobertura al moverlo dentro de deploy/ — pero el VALOR del digest cambia. Ese digest es evidencia de release (ops/scripts/validate-testcontainers-evidence.py:83-84, tests/e2e/real-qa.test.ts:99, ops/harness/runner.mjs:42). Consecuencia: cualquier mudanza dentro de deploy/, packages/, services/ o apps/console/ invalida el digest anotado para la imagen que corre HOY en producción. Es un hecho de FASE 3, no un bug.

(C) GLOBS PLANOS (no recursivos) — ops/scripts/validate.sh:
    línea 6:   `for file in "$ROOT"/scripts/*.sh "$PROJECT"/deploy/*.sh; do bash -n "$file"; done`
    línea 7:   `for file in "$ROOT"/scripts/*.mjs "$ROOT"/harness/*.mjs "$ROOT"/tests/*.mjs "$PROJECT"/deploy/*.mjs; do node --check "$file"; done`
    línea 197: `shellcheck "$ROOT"/scripts/*.sh "$PROJECT"/deploy/*.sh`
  Esto importa MUCHO porque eslint.config.js:5 y package.json `lint:core` NO cubren deploy/, ops/ ni scripts/: `eslint packages/protocol packages/store services tests vitest.config.ts`. Es decir, estos tres globs planos son la ÚNICA verificación sintáctica que tienen los ~35 .mjs/.sh de deploy/ y ops/scripts/. Un subdirectorio nuevo se cae del glob. Si se mueve TODO (glob vacío) el fallo es ruidoso (bash -n sobre una ruta literal inexistente); si se mueve PARTE, la pérdida de cobertura es silenciosa. Es el peor de los tres casos.
- === ZONA 1 · deploy/ — ÁRBOL OBJETIVO ===

Estado: 31 ficheros tracked, todos planos (docs/grafo.md dice 27 porque grafo.mjs filtra por `esFuente`).

LA IDEA QUE HACE BARATA ESTA MUDANZA (y que ninguna sesión ha usado todavía): la ruta EN EL ÁRBOL FUENTE y la ruta DENTRO DE LA IMAGEN son variables independientes, unidas sólo por el destino de tres líneas COPY. Si el destino se deja en `./deploy/`, TODA referencia in-image sigue válida sin tocarla. Verificado sobre los consumidores reales:
  - deploy/compose.yaml:18 `entrypoint: ["/app/deploy/runtime-entrypoint.sh"]` — NO cambia
  - deploy/compose.yaml:36 `command: ["node","deploy/migrate.mjs"]`; :219,:355,:395,:442 healthchecks `node deploy/readiness-probe.mjs`; :330 `deploy/local-readiness-probe.mjs`; :367 `deploy/outbox-metrics.mjs` — NO cambian
  - deploy/compose.dev.yaml:281 `deploy/unix-readiness-probe.mjs` — NO cambia
  - deploy/Dockerfile:97 `RUN node deploy/runtime-package-smoke.mjs` y :101 HEALTHCHECK — NO cambian
  - deploy/smoke.sh:10 `docker exec cauce-v3-prod-gateway-1 node /app/deploy/readiness-probe.mjs` — NO cambia
  - deploy/smoke-runtime-packaging.sh:73,77 `docker run ... node deploy/runtime-package-smoke.mjs` / `node deploy/migrate.mjs` — NO cambian
  - ops/compose.test.yaml:28,50,75 — NO cambian
  - ops/scripts/stack-health.sh:44,48,50,54 `node deploy/readiness-probe.mjs` (se ejecuta dentro del contenedor) — NO cambia
  - deploy/fleet-snapshot.mjs:7 `import {...} from 'file:///app/deploy/postgres-tls.mjs'` (URL ABSOLUTA in-image) — NO cambia
  - deploy/runtime-package-smoke.mjs:12-39,155-156,223 usa `new URL('../packages/...', import.meta.url)` — depende de estar en /app/deploy/, y sigue estándolo — NO cambia

ÁRBOL OBJETIVO (regla verificable mecánicamente: el subdirectorio nombra QUÉ ETAPA/CONTENEDOR consume el fichero; la raíz es lo que ejecuta el OPERADOR en el host):

deploy/
  Dockerfile                              (etapas build / production-dependencies / runtime / console)
  compose.yaml  compose.dev.yaml  compose.postgres.yaml  compose.alertmanager.yaml
  deploy.sh                               (único entrypoint del operador)
  smoke.sh  smoke-runtime-packaging.sh
  HISTORIAL.md                            (append-only; lo escribe deploy.sh:55)
  runtime/     <- ENTRA en la imagen `runtime` (Dockerfile:92-94). Mapea 1:1 sobre /app/deploy/
    runtime-entrypoint.sh  runtime-store.package.json
    migrate.mjs  migration-integrity.mjs  schema-version.mjs  postgres-tls.mjs
    readiness-probe.mjs  local-readiness-probe.mjs  unix-readiness-probe.mjs
    fleet-snapshot.mjs
    outbox-metrics.mjs  outbox-metrics-core.mjs  outbox-metrics-core.d.mts
    release-state-metrics.mjs  release-state-metrics.d.mts
    reconcile-stale-console-outbox.mjs  reconcile-stale-console-outbox-core.mjs  reconcile-stale-console-outbox-core.d.mts
    runtime-package-smoke.mjs
  console/     <- ENTRA en la imagen `console` (Dockerfile:140)
    nginx-console-tls.conf
  postgres/    <- bind-mount del contenedor postgres (compose.postgres.yaml:7 y :40)
    postgres-tls-entrypoint.sh

Cuentas: 19 en runtime/ + 1 console/ + 1 postgres/ + 9 en raíz + liveness-probe.mjs (pendiente de decisión, ver hallazgo aparte) = 31. ✓

DECISIÓN DE DISEÑO A DEFENDER: NO sub-dividir runtime/ en probes/ metrics/ migracion/. El destino del COPY es un único directorio plano `/app/deploy/`, así que un segundo nivel en el fuente rompería el 1:1 `deploy/runtime/X` ⇒ `/app/deploy/X`. Una jerarquía más bonita que MIENTE sobre lo que hay dentro del contenedor es exactamente el tipo de estructura que envenena al siguiente modelo. La invariante plana es verificable de un vistazo contra Dockerfile:93.

Los 3 .d.mts no se COPYan a la imagen (sólo sirven al typecheck) pero DEBEN viajar junto a su .mjs hermano: TypeScript resuelve `foo.d.mts` por adyacencia a `foo.mjs`.
- === ZONA 1 · deploy/ — CHECKLIST DE MUDANZA: LAS 11 LÍNEAS QUE SÍ CAMBIAN ===

Barrido hecho con `git grep -nE "(new URL\(|resolve\(|readFile|readFileSync|join\()[^)]*deploy/"` y `git grep -nE "from ['\"][^'\"]*deploy/"` sobre todo el árbol excluyendo deploy/ y *.md. Ésta es la lista COMPLETA de lecturas contra el ÁRBOL FUENTE (todo lo demás son rutas in-image y no se toca):

A) Dentro de deploy/ (3 líneas):
  1. deploy/Dockerfile:92  `COPY --chown=node:node deploy/runtime-store.package.json ./packages/store/package.json`
                            -> origen pasa a `deploy/runtime/runtime-store.package.json`; destino IGUAL
  2. deploy/Dockerfile:93  los 14 orígenes `deploy/X.mjs` pasan a `deploy/runtime/X.mjs`; destino `./deploy/` IGUAL
  3. deploy/Dockerfile:94  `COPY ... deploy/runtime-entrypoint.sh ./deploy/runtime-entrypoint.sh` -> origen `deploy/runtime/runtime-entrypoint.sh`; destino IGUAL (y :95 `RUN chmod 0555 ./deploy/runtime-entrypoint.sh` NO cambia)
  4. deploy/Dockerfile:140 `COPY deploy/nginx-console-tls.conf /etc/nginx/conf.d/default.conf` -> origen `deploy/console/nginx-console-tls.conf`; destino IGUAL
  5. deploy/compose.postgres.yaml:40 `- ./postgres-tls-entrypoint.sh:/opt/cauce/postgres-tls-entrypoint.sh:ro` -> `./postgres/postgres-tls-entrypoint.sh:...` (el `./` resuelve contra el PROJECT DIRECTORY, que deploy.sh:10 fija con `--project-directory "$REPO/deploy"` y que validate.sh:178 hereda del dir del primer -f; en ambos casos = deploy/, así que la corrección es la misma). compose.postgres.yaml:7 `entrypoint: ["/opt/cauce/postgres-tls-entrypoint.sh"] ` NO cambia (ruta in-container).

B) Fuera de deploy/ (6 líneas, todas verificadas con número de línea):
  6. packages/store/src/migrate-cli.ts:12  `new URL('../../../deploy/migrate.mjs', import.meta.url)` -> `'../../../deploy/runtime/migrate.mjs'`
     (cubierto por tests/unit/migrate-cli-production.test.ts)
  7. packages/store/test/legacy-console-outbox-reconciliation-postgres.test.ts:6  `from '../../../deploy/reconcile-stale-console-outbox-core.mjs'` -> `.../deploy/runtime/...`
  8. tests/unit/outbox-metrics.test.ts:2  `import { collectOutboxMetrics } from '../../deploy/outbox-metrics-core.mjs'` -> `'../../deploy/runtime/outbox-metrics-core.mjs'`
  9. tests/unit/release-state-metrics.test.ts:8  `} from '../../deploy/release-state-metrics.mjs'` -> `'../../deploy/runtime/release-state-metrics.mjs'`
 10. tests/unit/readiness-probe.test.ts:14  `join(repositoryRoot,'deploy/readiness-probe.mjs')` -> `'deploy/runtime/readiness-probe.mjs'`
 11. tests/unit/runtime-package-smoke.test.ts:10  `new URL('../../deploy/runtime-package-smoke.mjs', import.meta.url).href` -> `'../../deploy/runtime/runtime-package-smoke.mjs'`
 12. tests/unit/terminal-relay-operability.test.ts:8 `new URL('../../deploy/nginx-console-tls.conf', ...)` -> `'../../deploy/console/nginx-console-tls.conf'`
     tests/unit/terminal-relay-operability.test.ts:9 `new URL('../../deploy/local-readiness-probe.mjs', ...)` -> `'../../deploy/runtime/local-readiness-probe.mjs'`
     (línea :6 compose.yaml y :7 Dockerfile NO cambian; y su aserción de texto sobre `"deploy/local-readiness-probe.mjs"` dentro del healthcheck TAMPOCO cambia porque es la ruta in-image)

C) Índices y guardas que hay que re-clavar en el MISMO commit:
 13. ops/scripts/validate.sh:6, :7, :197 — añadir `"$PROJECT"/deploy/runtime/*.sh` y `"$PROJECT"/deploy/runtime/*.mjs` a los tres globs planos, y `"$PROJECT"/deploy/postgres/*.sh` al de shell. SIN ESTO se pierde en silencio la única verificación sintáctica de 17 ficheros.
 14. ops/tests/source-digest-domains.test.mjs:138 `'deploy/runtime-entrypoint.sh'` y :139 `'deploy/runtime-store.package.json'` (lista de centinelas del dominio runtime) -> rutas nuevas. También los fixtures que ESCRIBEN esa ruta: :209 y :328 `await write('deploy/runtime-entrypoint.sh', ...)`.
 15. scripts/calidad-base.json — re-clavar 14 claves de la sección `comentarios`: deploy/{fleet-snapshot,local-readiness-probe,migrate,migration-integrity,outbox-metrics-core,outbox-metrics,readiness-probe,reconcile-stale-console-outbox,release-state-metrics,runtime-entrypoint.sh,runtime-package-smoke,schema-version,unix-readiness-probe} -> `deploy/runtime/...` y `deploy/postgres-tls-entrypoint.sh` -> `deploy/postgres/...`. Aprovechar para BORRAR la clave huérfana `deploy/shadow-guard.mjs`. (deploy/{deploy.sh,smoke.sh,liveness-probe.mjs,smoke-runtime-packaging.sh} se quedan donde están: no tocar sus claves.) NINGUNA de las 14 se pone roja si no se re-clava — se AFLOJA. Por eso hay que hacerlo aunque el gate no obligue.
 16. docs/grafo.md — regenerar con `pnpm grafo`. scripts/grafo.mjs:19 hace `nodo()` a segundo nivel para `deploy`, así que el nodo único `deploy` se parte en `deploy` + `deploy/runtime` + `deploy/console` + `deploy/postgres`. Es esperado; el fichero es generado, no editar a mano.

NO SE TOCAN (comprobado uno a uno): deploy/compose.yaml (ninguna línea), deploy/compose.dev.yaml, deploy/compose.alertmanager.yaml, deploy/deploy.sh, deploy/smoke.sh, deploy/smoke-runtime-packaging.sh, ops/compose.test.yaml, ops/scripts/stack-health.sh, ops/scripts/source-digest.py (mete `"deploy"` entero por rglob), apps/console/nginx.conf:14 y ops/console-legibilidad/servir-con-csp.mjs:6,:28 (son COMENTARIOS que citan la ruta — actualizarlos es una edición de contenido, va en commit aparte o se deja).
FALSO POSITIVO detectado y descartado: `ops/scripts/backup.sh:16` referencia `"$ROOT/scripts/check-postgres-tls.mjs"` (de ops/), no `deploy/postgres-tls.mjs`.
- === ZONA 1 · deploy/ — ORDEN DE OPERACIONES SEGURO ===

BLOQUEO PREVIO, INNEGOCIABLE: `deploy/**` está en la fila NADIE de la tabla de sectores (ordenes/00-PROTOCOLO.md:29) y en el NO TOCAR de AGENTS.md:32. NINGUNA instancia ejecuta esto hoy. Es un ítem de FASE 3 con el dueño (plan-reestructura/31). Lo que sigue es el guion para esa ventana.

PASO 0 — foto antes (todo lectura, no toca producción):
  pnpm typecheck && pnpm lint && pnpm test:unit          # verde de partida, pegar salida
  python3 ops/scripts/source-digest.py --domain runtime   # anotar
  python3 ops/scripts/source-digest.py --domain console   # anotar
  ./ops/scripts/validate.sh                              # verde de partida
  docker inspect --format '{{.Image}}' cauce-v3-prod-gateway-1   # digest de lo que corre AHORA

PASO 1 — COMMIT ÚNICO «mv mecánico + reapunte de rutas» (21 mv + las 11 líneas del checklist + calidad-base.json).
  RESOLUCIÓN EXPLÍCITA DE LA TENSIÓN CON EL PROTOCOLO: la regla 3 pide `git mv` en commit separado de las ediciones de CONTENIDO, y la regla 4 pide que `main` nunca quede en rojo. Aquí las dos no se pueden cumplir por separado: un commit de sólo-mv deja typecheck y test:unit ROJOS (imports 6-12 rotos). Un reapunte de ruta NO es una edición de contenido: es la segunda mitad del propio mv. Por eso van juntos, y el mensaje del commit lo dice para que el siguiente lector no lo lea como una violación:
    `deploy: agrupar por consumidor (runtime/ console/ postgres/)`
    `mv mecanico + reapunte de rutas de origen. Cero cambios de logica: el destino`
    `de los COPY sigue siendo ./deploy/, asi que /app/deploy/* no cambia.`
    `Verificable: git show --stat y git log --follow -- <fichero>.`
  Excede el tope de 20 ficheros del protocolo, permitido por la excepción explícita «salvo mv mecánico» (00-PROTOCOLO.md:34).
  Secuencia exacta:
    mkdir -p deploy/runtime deploy/console deploy/postgres
    git mv deploy/{runtime-entrypoint.sh,runtime-store.package.json,migrate.mjs,migration-integrity.mjs,schema-version.mjs,postgres-tls.mjs,readiness-probe.mjs,local-readiness-probe.mjs,unix-readiness-probe.mjs,fleet-snapshot.mjs,outbox-metrics.mjs,outbox-metrics-core.mjs,outbox-metrics-core.d.mts,release-state-metrics.mjs,release-state-metrics.d.mts,reconcile-stale-console-outbox.mjs,reconcile-stale-console-outbox-core.mjs,reconcile-stale-console-outbox-core.d.mts,runtime-package-smoke.mjs} deploy/runtime/
    git mv deploy/nginx-console-tls.conf deploy/console/
    git mv deploy/postgres-tls-entrypoint.sh deploy/postgres/
    (los 9 imports relativos INTERNOS —deploy/{migrate,migration-integrity,readiness-probe,schema-version,outbox-metrics,reconcile-stale-console-outbox}.mjs importando `./postgres-tls.mjs`, `./outbox-metrics-core.mjs`, `./release-state-metrics.mjs`, `./reconcile-stale-console-outbox-core.mjs`— NO se tocan: los 19 ficheros viajan juntos y siguen siendo hermanos)
    ... aplicar las 11 líneas del checklist + validate.sh + source-digest-domains.test.mjs + calidad-base.json ...
    git commit deploy/ packages/store/src/migrate-cli.ts packages/store/test/legacy-console-outbox-reconciliation-postgres.test.ts tests/unit/ ops/scripts/validate.sh ops/tests/source-digest-domains.test.mjs scripts/calidad-base.json -m "..."
  PROHIBIDO como atajo: dejar un shim/re-export en la ruta vieja. Crearía dos rutas vivas para el mismo fichero — el patrón exacto que produjo 10 versiones paralelas en agosto.

PASO 2 — GATE, en este orden (los tres primeros son baratos, el cuarto es el que de verdad prueba la mudanza):
  a) pnpm typecheck && pnpm lint && pnpm test:unit
  b) ./ops/scripts/validate.sh    (ejercita `docker compose config` de las 3 combinaciones -> valida el bind-mount de postgres/)
  c) node ops/tests/source-digest-domains.test.mjs
  d) pnpm qa:runtime-packaging   ==> ESTA ES LA PRUEBA DEL EFECTO REAL. deploy/smoke-runtime-packaging.sh:18 construye `--target runtime` y :73/:77 corren `node deploy/runtime-package-smoke.mjs` y `node deploy/migrate.mjs` DENTRO del contenedor. Si el destino del COPY quedó bien, pasa sin haber tocado una sola línea de ese script. Si falla, el destino se movió y hay que volver a `./deploy/`. Correr como usuario normal, NUNCA root (runtime-package-smoke valida ownership y da falso rojo bajo root — 00-PROTOCOLO.md:33).

PASO 3 — COMMIT SEPARADO, sólo .md, sin gate: `pnpm grafo` y commitear docs/grafo.md.

PASO 4 — anotar en el dossier de FASE 3 (plan-reestructura/fase3/00-DOSSIER.md) los digests runtime/console NUEVOS frente a los del paso 0, y por qué difieren: la mudanza cambió las rutas relativas que source-digest.py hashea. Sin esa nota, el siguiente que compare el digest del árbol contra el de la imagen en producción concluirá que hay deriva de código donde sólo hubo una mudanza.

ROLLBACK: `git revert` del commit del paso 1. Como no se tocó ningún destino in-image ni ninguna unidad systemd, y no se desplegó nada, el revert es total y no deja rastro en el host.
- === ZONA 1 · deploy/ — DOS RESIDUOS QUE ENCONTRÉ DE PASO (decisión del dueño, no míos) ===

(1) deploy/liveness-probe.mjs — 7302 bytes, 33 líneas de comentario, NO ESTÁ CABLEADO A NADA.
  Comprobado con `git grep -n liveness-probe` sobre deploy/compose*.yaml, deploy/Dockerfile, deploy/deploy.sh, deploy/smoke.sh, ops/compose.test.yaml, ops/systemd/ y ops/generated/: CERO resultados. No entra en la imagen (no está en el COPY de Dockerfile:93), no es healthcheck de ningún servicio, no lo invoca ninguna unit.
  Sus dos únicos consumidores son tests que lo ejecutan por ruta de disco:
    tests/unit/liveness-probe.test.ts:10       `join(repositoryRoot,'deploy/liveness-probe.mjs')`
    services/dispatcher/test/liveness.test.ts:13  `join(repositoryRoot,'deploy/liveness-probe.mjs')`
  Su propia cabecera (línea 16) documenta el uso `node deploy/liveness-probe.mjs <url> <campo> [stallMs]` — o sea, se escribió para que alguien lo invoque, y nadie lo invoca.
  Por eso NO tiene sitio en el árbol objetivo: no es `runtime/` (no está en la imagen), no es `console/`, no es `postgres/`, y no es una herramienta del operador cableada como deploy.sh/smoke.sh. Tres salidas, todas del dueño: (a) `git rm` + los dos tests, por la regla 0 (código muerto se borra, git es el archivo); (b) si el operador lo usa a mano desde el host, va a `ops/scripts/` con el resto de utilidades de host; (c) si se pretendía cablear como healthcheck y se olvidó, es un cabo suelto de FASE 3, no una mudanza. Dejarlo en la raíz de deploy/ sin decidir es lo único que NO hay que hacer: es la carpeta de la que estamos sacando el desorden.
  Nota: es uno de los 2 ficheros de deploy/ que rompen el trinquete al moverse (33 comentarios > tope 15% = 28), así que la opción (b) exige re-clavar su clave.

(2) scripts/calidad-base.json contiene `"deploy/shadow-guard.mjs": 1` en la sección `comentarios`, y ese fichero NO existe en `git ls-files`. Es la única clave huérfana de las 811 del baseline (36 en `lineas`: 0 huérfanas; 33 en `fechas`: 0; 742 en `comentarios`: 1). Causa: calidad.mjs:53 sólo poda `base.lineas`, nunca `fechas` ni `comentarios`. Borrarla en el commit del paso 1.
- === ZONA 2 · tests/ — VEREDICTO: NO FUSIONAR. La estructura es correcta; el contenido de tests/unit no lo es ===

Estado: unit(40) · gateway-hardening(17 tests + helpers.ts) · store-hardening(9) · terminal-pty(5 tests + 9 dobles/vectores + README) · integration(4) · e2e(2) · helpers(1).

TRES RAZONES DURAS PARA NO FUSIONAR:
(a) tests/helpers/postgres.ts ESTÁ CLAVADO. 46 ficheros lo importan con la forma literal `'../../../tests/helpers/postgres.js'` — profundidad 3 fija. Reparto: 42 en packages/store/test/, 2 en services/telegram-bridge/test/, 2 en services/gateway/src/, 1 en tests/e2e/, 1 en ops/tests/. Es el 5º hub del repo (67 refs entrantes en docs/grafo.md). Mover `tests/helpers/` un solo nivel = 46 ficheros editados en un commit, y el fichero rompe además el trinquete (73 comentarios > tope 15% = 42). Coste altísimo, beneficio cero.
(b) Los directorios corresponden 1:1 con scripts de package.json que son la matriz de CI, y esa matriz se auto-verifica: scripts/test-all.mjs:36-48 (`assertMatrixIsComplete`) falla si existe un script `test:*` que no está ni en SUITES ni en SEPARATELY_GATED. Renombrar un directorio obliga a tocar package.json:30-36 (6 scripts) Y test-all.mjs. La estructura de carpetas no es decorativa: es el índice de la matriz.
(c) Los tiempos de ejecución son incompatibles. `test:gateway-hardening` corre con `--testTimeout=120000`, `test:store-hardening` y `test:e2e` con `180000`, y ambos necesitan postgres (vía tests/helpers). `test:unit` es el gate por commit y corre en CI (.github/workflows/ci.yml:69 sólo ejecuta `pnpm test:unit`). Fusionar hardening dentro de unit metería postgres y 3 minutos en el gate de cada commit de las cuatro instancias.

EL DEFECTO REAL, MEDIDO: `tests/unit` no contiene tests unitarios. De sus 40 ficheros, 25 leen ficheros de deploy/, ops/ o scripts/ DESDE DISCO y afirman sobre su texto — son tests de POLÍTICA DEL ÁRBOL, no de código:
  contra ops/ o scripts/ (13): canary-gate, console-browser-storage-policy, gate-roundtrip-probe, host-backup-monitor, inactive-override-manifest, migration-gate, physical-fleet-gate, postgres-tls-policy, privacy-identities, provision-terminal-client, relay-telegram-observability, stack-health-arguments, testcontainers-evidence
  contra deploy/ (12): compose-files, compose-healthcheck, dockerfile-runtime-policy, liveness-probe, migrate-cli-production, observability-alerting, outbox-metrics, readiness-probe, release-state-metrics, runtime-package-smoke, terminal-relay-operability, source-digest-closure
  unitarios de verdad (15): agent-profile, agent-profile-mutacion, artifact-egress, auth, base-de-pruebas-guarda, composicion-del-perfil, gate-probe-authority, harness-observability, message-timeout, paquetes-de-este-arbol, perfil-espejo-sql, protocol, protocol-runtime, scheduler, topes-de-delegacion-editables
Esto lo confirma el grafo sin ambigüedad: tests/unit -> ops/scripts pesa 31 y tests/unit -> deploy pesa 14, contra tests/unit -> packages/store que pesa 4.

Y HAY DOS HOGARES PARA LOS TESTS DE ops/: `ops/tests/` (18 ficheros .mjs, ejecutados por ops/scripts/validate.sh:67-76 y por `test:container-supervisor`/`test:container-cutover`) y esos 13 .ts de `tests/unit/` (ejecutados por vitest en `test:unit`). Mismo sujeto, dos carpetas, dos runners, dos gates.
  AVISO EXPLÍCITO PARA LA INSTANCIA QUE EJECUTE: esto NO se arregla con `git mv`. Son runners distintos — `ops/tests/*.mjs` son ficheros node planos con `assert`, `tests/unit/*.ts` son suites de vitest con `describe/it/expect`. Mover uno al otro lado es una REESCRITURA, no una mudanza, y además `ops/tests/` está dentro de OPERATIONAL_TREES de container_ops_digest.py (ver zona 4). Se documenta la duplicidad y se deja para después de FASE 3.
- === ZONA 2 · tests/ — ÁRBOL OBJETIVO Y LA ÚNICA MUDANZA QUE PAGA HOY ===

ÁRBOL OBJETIVO (idéntico al actual salvo un subdirectorio; el resto es una decisión de NO tocar, tomada con evidencia):

tests/
  helpers/postgres.ts          INTOCABLE — 46 importadores a `../../../` fijo
  unit/                        el gate por commit (CI sólo corre esto)
  gateway-hardening/           contrato HTTP/WS del gateway (17 tests + helpers.ts, 11 lo importan con `./helpers`)
  store-hardening/             semántica postgres del store (9)
  terminal-pty/
    *.test.ts                  los 5 tests de verdad
    dobles/                    <- ÚNICA MUDANZA PROPUESTA: certs.mjs+.d.mts, fake-gateway.mjs+.d.mts,
                                  fake-pty-agent.mjs+.d.mts, protocol.mjs+.d.mts, vectors.json (9 ficheros)
    README.md
  integration/                 (4)
  e2e/                         (2)

LA MUDANZA (tests/terminal-pty/dobles/): hoy 9 de los 15 ficheros del directorio no son tests sino dobles y vectores, mezclados con los 5 tests. Superficie de referencia, comprobada fichero a fichero:
  certs.mjs        <- tests/terminal-pty/{certs.d.mts, fake-gateway.mjs, relay-contract.test.ts, relay-contract-agent.test.ts, relay-contract-lifecycle.test.ts} + services/terminal-relay/src/health.test.ts (CRUCE FUERA DE tests/ — es el arista `services/terminal-relay -> tests/terminal-pty` de peso 1 del grafo) + scripts/calidad-base.json
  fake-gateway.mjs <- tests/terminal-pty/{fake-gateway.d.mts, relay-contract.test.ts, relay-contract-lifecycle.test.ts, README.md} + scripts/calidad-base.json
  fake-pty-agent.mjs <- tests/terminal-pty/{fake-pty-agent.d.mts, relay-contract-agent.test.ts, relay-contract-lifecycle.test.ts, README.md} + scripts/calidad-base.json
  protocol.mjs     <- tests/terminal-pty/{protocol.d.mts, fake-gateway.mjs, fake-pty-agent.mjs, relay-contract.test.ts, relay-contract-agent.test.ts, relay-contract-lifecycle.test.ts, vectors.test.ts, README.md} + scripts/calidad-base.json
  vectors.json     <- tests/terminal-pty/{vectors.test.ts, README.md} + (sólo menciones en prosa: ops/pty-agent/README.md, services/terminal-relay/README.md, plan-reestructura/21-correcciones-mapeadas.md)

ORDEN DE OPERACIONES:
  0. Sector: `tests/**` no tiene dueño explícito en la tabla de 00-PROTOCOLO.md:22-29, pero tests/terminal-pty toca el contrato de services/terminal-relay, que es de Gemini. Pedir el turno al integrador ANTES de tocar; no es una carpeta libre.
  1. `pnpm test:terminal-pty` verde de partida (pegar salida) + `pnpm --filter @cauce/terminal-relay test` o el subconjunto que cubra services/terminal-relay/src/health.test.ts.
  2. Commit único (mv + reapunte, misma justificación que en deploy/: el reapunte es la segunda mitad del mv):
       mkdir -p tests/terminal-pty/dobles && git mv tests/terminal-pty/{certs,fake-gateway,fake-pty-agent,protocol}.{mjs,d.mts} tests/terminal-pty/vectors.json tests/terminal-pty/dobles/
     Reapuntes: los 5 *.test.ts de tests/terminal-pty (`./certs.mjs` -> `./dobles/certs.mjs`, etc.), los imports internos entre dobles (fake-gateway.mjs -> ./protocol.mjs y ./certs.mjs; fake-pty-agent.mjs -> ./protocol.mjs) NO cambian porque viajan juntos, y services/terminal-relay/src/health.test.ts (su ruta a certs.mjs gana un nivel).
  3. scripts/calidad-base.json: re-clavar `tests/terminal-pty/certs.mjs` (comentarios=9; tope nuevo por defecto sería 6 -> SE PONDRÍA ROJO si no se re-clava). Los otros 4 no rompen pero se re-clavan igual para no aflojar el trinquete.
  4. ops/scripts/validate.sh:7 incluye `"$ROOT"/tests/*.mjs` — ése es ops/tests, NO tests/terminal-pty, así que no hay que tocarlo. Verificado.
  5. Gate: `pnpm typecheck && pnpm lint && pnpm test:unit` + `pnpm test:terminal-pty`. Commit aparte para `pnpm grafo`.

LO QUE NO SE HACE Y POR QUÉ (escribirlo en el commit para que la siguiente sesión no lo re-proponga): no se fusiona gateway-hardening con store-hardening (sujetos distintos: uno prueba rutas HTTP/WS, otro semántica SQL; comparten sólo tests/helpers); no se fusiona hardening dentro de unit (metería postgres en el gate por commit); no se mueve tests/helpers (46 importadores); no se parte tests/unit en unit/+politica/ todavía — es la reorganización correcta a medio plazo pero cuesta tocar package.json, test-all.mjs y CI, y compite en la misma ventana con la mudanza de deploy/, que da más y arriesga menos.
- === ZONA 3 · packages/adapter-sdk/src — VEREDICTO: LOS LÍMITES SON LOS CORRECTOS. NO MOVER CARPETAS ===

Medición del grafo INTERNO de packages/adapter-sdk/src (83 ficheros .ts), arista = import relativo que cruza el primer nivel, calculada resolviendo cada `from './...'`:
   25  harnesses -> sdk
   14  (src raíz) -> sdk
   12  bin -> shared-session
    9  shared-session -> sdk
    8  bin -> sdk
    4  harnesses -> shared-session
    4  sdk -> harnesses          <== VIOLACIÓN
    2  bin -> harnesses
    2  (src raíz) -> context
    2  (src raíz) -> harnesses
    1  context -> harnesses      <== VIOLACIÓN
    1  sdk -> context            <== VIOLACIÓN
    1  (src raíz) -> shared-session

LECTURA: descontando 6 aristas, el resto dibuja un apilado limpio y sin ciclos — bin -> harnesses -> shared-session -> sdk. Es decir: la partición en sdk/ shared-session/ harnesses/ bin/ NO es arbitraria, es una estratificación real, y responde que SÍ a la pregunta del dueño: los límites son los correctos tras las particiones. Lo que hay que arreglar no es dónde están las carpetas, sino tres cosas que las hacen mentir:

(1) EL CICLO sdk <-> harnesses, y es SOLO DE TIPOS. Las 4 aristas son `import type`, verificadas una a una:
      packages/adapter-sdk/src/sdk/engine.ts:8-10        `import type { HarnessAdapter, HarnessSessionReservation, RuntimeProfileMeasurement, SessionLane } from "../harnesses/shared.js"`
      packages/adapter-sdk/src/sdk/client.ts:9            `import type { HarnessAdapter } from '../harnesses/shared.js'`
      packages/adapter-sdk/src/sdk/engine/contracts.ts:2  `import type { HarnessAdapter, RuntimeProfileMeasurement } from "../../harnesses/shared.js"`
      packages/adapter-sdk/src/sdk/engine/delivery-context.ts:3 `import type { SessionLane } from "../../harnesses/shared.js"`
    Las cuatro pasan por el barril packages/adapter-sdk/src/harnesses/shared.ts, cuyas líneas 26-33 re-exportan tipos de `./shared/contracts.js`. Y ese fichero —packages/adapter-sdk/src/harnesses/shared/contracts.ts— importa de TODAS las capas: línea 1 `../../sdk/durable-store.js`, línea 10 `../../shared-session/types.js`, línea 9 `../contexto-fijo.js`. O sea: NO es un detalle de implementación de los arneses, es EL CONTRATO entre el motor y cualquier arnés, y está guardado dentro de la capa más alta. Por eso el motor tiene que mirar hacia arriba.
    Además `harnesses/shared/contracts.ts` tiene exactamente UN importador: `harnesses/shared.ts:29`. Es un fichero suelto, no una red.

(2) `context/` es una QUINTA carpeta de primer nivel que el enunciado no menciona y que nadie ha ubicado en el apilado: 2 ficheros (perfil-a-contexto.ts, siembra-del-perfil.ts), con `sdk/client.ts -> ../context/siembra-del-perfil.js` (el sdk mira hacia arriba otra vez) y `context/perfil-a-contexto.ts:6 -> ../harnesses/contexto-fijo.js` (y encima hacia los arneses). Los dos se exportan en la superficie pública (src/index.ts:15,18). Está entre sdk y harnesses sin decidir de qué lado cae.

(3) CUATRO ficheros «fake» en tres niveles: `src/fake-harness.ts` (exportado en index.ts:29), `src/harnesses/fake.ts`, `src/bin/fake.ts`, `src/bin/fake-harness.ts`. Y `bin/` mezcla 6 shims de entrada de 4 líneas cada uno (claude/codex/hermes/openclaw/opencode/fake) con 701 líneas de maquinaria que no es punto de entrada de nada: bin/shared.ts (276), bin/config.ts (299), bin/shared-session.ts (126).

ÁRBOL OBJETIVO (para escribir en el plan, NO para ejecutar antes de FASE 3):
  src/index.ts                única superficie pública (hoy ya lo es: 29 líneas de re-export)
  src/contracts/harness.ts    <- desde harnesses/shared/contracts.ts. Tipos que cruzan capas, sin implementación
  src/sdk/                    núcleo durable; NO importa de harnesses/, shared-session/ ni context/
  src/shared-session/         tmux + pegado; importa sdk/ y contracts/
  src/harnesses/              un fichero por CLI + shared/; importa sdk/, shared-session/, contracts/
  src/context/                perfil -> contexto; importa contracts/ (no harnesses/)
  src/bin/                    los 8 shims, y bin/runtime/ para shared.ts, config.ts, shared-session.ts
- === ZONA 3 · adapter-sdk — QUÉ HACER Y QUÉ NO, CON EL COSTE MEDIDO ===

NO MOVER NINGUNA CARPETA DE packages/adapter-sdk/src ANTES DE FASE 3. Tres razones cuantificadas:
  - 39 ficheros de packages/adapter-sdk/ rompen el trinquete al moverse — con diferencia la peor de las cuatro zonas. Los peores: test/shared-session.test.ts (5455 líneas), test/engine.test.ts (2795 líneas + 3 fechas en comentarios), test/client.test.ts (1340), test/durable-store.test.ts (1101), src/shared-session/session.ts (950), src/harnesses/shared/prompt.ts y src/shared-session/{config,pane,rollout}.ts y src/sdk/engine/{recovery,delivery-context}.ts (todos al filo del 15%). Nueve ficheros de test/ tienen además fechas en comentarios (tope tras el mv = 0 -> rojo garantizado): artifact-inliner, bloque-gestionado, dialects, engine-session-queue, engine, fence, harness-turn-failure, output-parser-contract, output-parser-lost-delegations, output-parser-sobre-roto.
  - Es el 2º paquete del repo por referencias entrantes: 146 ficheros, y 3 de los 15 hubs de docs/grafo.md están dentro (sdk/types.ts con 73, sdk/durable-store.ts con 33, harnesses/index.ts con 24). Entrantes desde fuera: scripts(125), tests/unit(8), apps/console(7), packages/store(2), ops/scripts(2), tests/e2e(1), tests/integration(1), ops/tests(1), (raíz)(1).
  - `packages/adapter-sdk/package.json` clava 8 rutas de `bin` a `./dist/src/bin/<n>.js` y 2 de `exports` a `./dist/bridge/...`; `packages/adapter-sdk/scripts/chmod-bins.mjs:4` repite la lista de los 8 nombres a mano. Cualquier reorganización de src/bin/ arrastra los tres.

LO ÚNICO QUE VALE LA PENA, Y NO ES UNA MUDANZA: romper el ciclo sdk<->harnesses. Es una edición pequeña, deja los cuatro límites DICIENDO LA VERDAD, y no mueve carpetas.
  Paso 1 — `git mv packages/adapter-sdk/src/harnesses/shared/contracts.ts packages/adapter-sdk/src/contracts/harness.ts` (1 fichero).
  Paso 2 — arreglar sus 3 imports propios: línea 1 `../../sdk/durable-store.js` -> `../sdk/durable-store.js`; línea 10 `../../shared-session/types.js` -> `../shared-session/types.js`; línea 9 `../contexto-fijo.js` -> `../harnesses/contexto-fijo.js`.
  Paso 3 — apuntar los 4 consumidores del sdk directamente a `../contracts/harness.js` / `../../contracts/harness.js` en lugar de al barril de arneses (sdk/engine.ts:8, sdk/client.ts:9, sdk/engine/contracts.ts:2, sdk/engine/delivery-context.ts:3).
  Paso 4 — packages/adapter-sdk/src/harnesses/shared.ts:26-33: re-exportar desde `../contracts/harness.js` para no romper la superficie pública que consume src/index.ts:27.
  Efecto: `sdk -> harnesses` pasa de 4 a 0. El apilado bin -> harnesses -> shared-session -> sdk queda sin ciclos.
  CABO SUELTO HONESTO, decir en el commit: quedaría `contracts -> harnesses` por el tipo `SelloDeContextoFijo`, declarado en packages/adapter-sdk/src/harnesses/contexto-fijo.ts:12 y usado en contracts.ts:9,33 además de en harnesses/shared/adapter.ts:30,307, harnesses/shared/prompt.ts:3 y context/perfil-a-contexto.ts:6. Cerrarlo del todo exige extraer también ese tipo, y eso ya toca 4 ficheros más. Se deja escrito, no se hace en la misma tanda.
  Coste del trinquete de este cambio: `harnesses/shared/contracts.ts` tiene 43 líneas de comentario contra un tope por defecto de 18 -> SE PONDRÍA ROJO. Re-clavar la clave a `packages/adapter-sdk/src/contracts/harness.ts` en el mismo commit.
  Sector: packages/adapter-sdk/ NO figura en la tabla de 00-PROTOCOLO.md:22-29 con dueño explícito. Pedir asignación al integrador antes de tocar; es el fichero más central del paquete más central después de protocol/store.
- === ZONA 4 · ops/ — QUÉ NO REORDENAR ANTES DE FASE 3 (con el riesgo exacto, no genérico) ===

RIESGO 1 — HAY UNITS VIVAS APUNTANDO A RUTAS ABSOLUTAS DE ops/scripts/. Esto no es teoría: los timers `cauce-*` corren AHORA en esta máquina. Conteo de rutas absolutas escritas en units (hechas y generadas), con `git grep -ohE "(/opt/cauce-v3|\{install_prefix\}|%h/\.local/share/cauce-v3)/ops/[A-Za-z0-9._/-]+"`:
     30  %h/.local/share/cauce-v3/ops/scripts/container-adapter-supervisor.sh
     16  /opt/cauce-v3/ops/scripts/alias-runner.sh
      3  /opt/cauce-v3/ops/scripts/systemd-stack.sh
      3  /opt/cauce-v3/ops/scripts/container-adapter-supervisor.sh
      3  %h/.local/share/cauce-v3/ops/openclaw-gateway/openclaw-gateway-supervisor.sh
      2  {install_prefix}/ops/scripts/container-adapter-supervisor.sh
      2  /opt/cauce-v3/ops/scripts/guard-check.sh
      1 c/u  stack-health.sh · validate-manifests.py · generate-units.py · generate-container-units.py · container_ops_digest.py · quota-collector.py (%h) · pty-agent/cauce-pty-launcher.sh (%h) · observability/alertmanager.yaml
  Ejemplos literales: `ops/generated/systemd/cauce-v3-alias-zeus.service:30 ExecStart=/opt/cauce-v3/ops/scripts/alias-runner.sh zeus`; `ops/systemd/*.service: WorkingDirectory=/opt/cauce-v3/ops` + `ExecStart=/opt/cauce-v3/ops/scripts/stack-health.sh %i`.
  Consecuencia de mover CUALQUIERA de esos ficheros: la unit instalada en el host queda apuntando a una ruta inexistente. `/opt` y `/etc/cauce-v3` están en el NO TOCAR de AGENTS.md:32, así que la instancia NI SIQUIERA PUEDE reparar lo que rompió. El repo se quedaría verde y la flota rota — el peor modo de fallo posible aquí.

RIESGO 2 — 28 RUTAS ESCRITAS A MANO EN ops/scripts/container_ops_digest.py (STATIC_INPUTS, líneas 11-40). Incluye scripts/{container-adapter-supervisor.sh, alias-runner.sh, container_alias_lib.py, manifest_lib.py, container-alias-query.py, validate-container-mount.py, generate-container-units.py, pin-container-release.py, container_ops_digest.py, cutover.sh, create-inactive-override-manifest.py, migration-gate.mjs, validate.sh}, container-runtime/cauce-container-runtime.py, container-aliases.json, 9 de tests/ y 2 runbooks. Mover uno = `python3 ops/scripts/container_ops_digest.py --rootless --check` revienta por fichero ausente, y con él ops/scripts/validate.sh:66 y ops/tests/container-ops-evidence.test.mjs.
  Y aunque el fichero movido NO esté en esa lista, `OPERATIONAL_TREES` (líneas 45-61) recorre recursivamente scripts, tests, runbooks, schemas, systemd, observability, manifests, pty-agent, container-runtime, harness, guardias, openclaw-gateway, cli y generated/systemd: la cobertura no se pierde, pero la RUTA LÓGICA entra en el digest, así que el valor cambia y hay que regenerar `ops/generated/container-systemd/rootless/OPERATIONS.sha256` — que es lo que anclan los `BUNDLE_SHA256` de los .env instalados en el host.

RIESGO 3 — GENERADORES ANCLADOS POR PROFUNDIDAD. ops/scripts/generate-units.py:13, ops/scripts/generate-container-units.py:15 y ops/scripts/validate-manifests.py:7 hacen todos `root = pathlib.Path(__file__).resolve().parents[1]`, o sea «ops/ es mi abuelo». Meter un generador en `ops/scripts/generadores/` mueve `root` a `ops/scripts/` y todas las salidas (`root/generated/systemd`, `root/manifests`) se van a otro sitio SIN error, escribiendo en rutas nuevas. Y ops/scripts/manifest_lib.py:145 hace `sorted((root / "manifests").glob("*.yaml"))` con verificación de conjunto EXACTO (línea 150: `missing`/`extra` -> ManifestError): mover ops/manifests/ o añadirle/quitarle un fichero es un error duro.

RIESGO 4 — SALIDA GENERADA VERIFICADA BYTE A BYTE. ops/scripts/validate.sh:39-48 regenera las units en un tmp y hace `cmp -s` contra `ops/generated/systemd/`, luego `sha256sum -c SHA256SUMS`; :50-65 lo mismo para `generated/container-systemd/rootless/`. `ops/generated/**` (48 ficheros) no se toca a mano jamás.

LISTA NEGRA, EXPLÍCITA, PARA COPIAR EN LA ORDEN: ops/scripts/** · ops/systemd/** · ops/generated/** · ops/guardias/** · ops/pty-agent/** · ops/manifests/** · ops/schemas/** · ops/container-runtime/** · ops/openclaw-gateway/** · ops/cli/** · ops/harness/** (Dockerfile:106 y :113 lo COPYan a la imagen) · ops/observability/** · ops/tests/** · ops/container-aliases.json · ops/hermes-runtime.json · ops/compose.test.yaml · ops/Makefile · ops/config/** · ops/runbooks/** · ops/README.md · ops/INSTALLATION.md · ops/GATE_CONTRACT.md.
Eso es, en la práctica, TODO ops/ menos tres .md.
- === ZONA 4 · ops/ — QUÉ SÍ SE PUEDE REORDENAR HOY (es poco, y está bien que sea poco) ===

SÓLO TRES FICHEROS, y son los únicos de la raíz de ops/ que cumplen las DOS condiciones: cero referencias entrantes Y fuera del digest operativo.
  - ops/DELIVERABLES.md
  - ops/GATE_COLLECTOR_IMPLEMENTATION.md
  - ops/GATE_SNAPSHOT_EXAMPLES.md
Verificación (1) sin referencias: `git grep -l "<nombre>" -- ':!ops/<nombre>'` devuelve VACÍO para los tres. (DELIVERABLES.md sí menciona a GATE_CONTRACT.md, pero nadie menciona a DELIVERABLES.md.)
Verificación (2) fuera del digest: ops/scripts/container_ops_digest.py:62-73 `OPERATIONAL_ROOT_FILES` enumera exactamente Makefile, README.md, INSTALLATION.md, GATE_CONTRACT.md, compose.test.yaml, container-aliases.json, hermes-runtime.json, config/prod.env.example, config/host-backup.env.example. Los tres candidatos NO están, y como son ficheros de la raíz (no de un OPERATIONAL_TREE) no los pilla ningún rglob. Contraste: GATE_CONTRACT.md e INSTALLATION.md SÍ están en esa lista -> intocables.
Además son .md, así que ni siquiera entran en calidad.mjs (EXCLUIR incluye `\.md$`): trinquete cero.

DESTINO PROPUESTO: `ops/runbooks/` ya existe con 14 ficheros y es el hogar natural de documentación operativa. Alternativa si el dueño prefiere que ops/ no crezca: son 3 documentos de diseño de una entrega ya cerrada — la regla 0 (git es el archivo) admite `git rm` si ya no describen nada vivo. Decisión del dueño, no de la instancia.

ORDEN DE OPERACIONES (30 segundos, sin gate completo por ser sólo .md, 00-PROTOCOLO.md:35):
  1. git mv ops/{DELIVERABLES,GATE_COLLECTOR_IMPLEMENTATION,GATE_SNAPSHOT_EXAMPLES}.md ops/runbooks/
  2. python3 ops/scripts/container_ops_digest.py --rootless --check   (debe seguir VERDE: si se pone rojo, alguno de los tres sí estaba en el digest y hay que revertir en el acto)
  3. ./ops/scripts/validate.sh
  4. git commit ops/ -m "ops: mover 3 documentos sin referencias entrantes a runbooks/"

SECTOR: la higiene de docs es de OpenCode/MiniMax según 00-PROTOCOLO.md:27; ops/** operativo es de Codex (maquinaria de release) y del dueño. Estos tres .md caen en el lado de higiene, pero viven bajo ops/: confirmar con el integrador antes de mover, no asumir.

Y UNA COSA QUE NO ES REORDENAR PERO SÍ ES BARATA Y ARREGLA EL AGUJERO REAL DE ops/: ops/scripts/validate.sh:7 hace `node --check` sobre `"$ROOT"/scripts/*.mjs` — glob PLANO sobre 60 ficheros de ops/scripts/. Como eslint.config.js no cubre ops/ en absoluto, ese glob es la única verificación sintáctica de esos .mjs. Convertir los tres globs de las líneas 6, 7 y 197 en recursivos (`find ... -name '*.mjs'` o `shopt -s globstar` + `**/*.mjs`) es un cambio de 3 líneas que ELIMINA de raíz el modo de fallo silencioso de toda futura reorganización de ops/ y de deploy/. Hacerlo ANTES de cualquier mudanza, en su propio commit, y no dentro de la ventana de FASE 3.
- === TRAMPAS PARA LA SIGUIENTE SESIÓN FRESCA (esto es lo que las estaba contaminando) ===

(1) LOS «105 CANDIDATOS HUÉRFANOS» DE docs/grafo.md NO SON HUÉRFANOS. Al menos 27 de ellos se cargan por glob o por ruta construida en tiempo de ejecución, invisible para un analizador de imports. Un modelo fresco que lee la lista y aplica la regla 0 («código muerto se BORRA con git rm») rompe producción:
  - ops/manifests/{argos,atlas,dedalo,hegel,iza,janus,jarvis,kratos,midas,salva,seneca,socrates,vulcano,zeus}.yaml (15 en la lista) — los carga ops/scripts/manifest_lib.py:145 `sorted((root / "manifests").glob("*.yaml"))` Y la línea 150 EXIGE el conjunto exacto: borrar uno es ManifestError inmediato, y son la fuente de las units generadas que corren en el host.
  - ops/schemas/*.json (6 en la lista) — validación por ruta, mismo patrón.
  - packages/adapter-sdk/manifests/{claude,codex,fake,hermes,openclaw,opencode}.json (6) — los prueba packages/adapter-sdk/test/manifests.test.ts y los resuelve el runtime por nombre.
  - packages/adapter-sdk/scripts/{chmod-bins,copy-bridges,package-smoke}.mjs (3) — invocados desde packages/adapter-sdk/package.json (`build`, `smoke:package`), no importados por nadie.
  - ops/guardias/telegram-bridge.override.yaml, pnpm-lock.yaml, pnpm-workspace.yaml.
  El grafo dice literalmente «verificar antes de tocar»; la propuesta concreta es más fuerte: docs/grafo.md debería EXCLUIR de esa lista los ficheros de configuración/manifiesto (.yaml/.json/.schema.json) o marcarlos como «carga dinámica», porque hoy la lista invita al borrado. Eso es una edición de scripts/grafo.mjs, de una línea en `esFuente`/`huerfanos`.

(2) LA CONFUSIÓN QUE MÁS CUESTA: en deploy/ conviven rutas de ÁRBOL FUENTE y rutas DENTRO DE LA IMAGEN, escritas igual (`deploy/readiness-probe.mjs` en ambos casos). Un `git grep deploy/` devuelve ~60 puntos y el modelo asume que los 60 hay que cambiarlos. Son 11. El discriminador mecánico, que conviene escribir en el propio deploy/Dockerfile como comentario de una línea junto al COPY: si la ruta aparece dentro de un `docker run`, `docker exec`, `command:`, `entrypoint:`, `healthcheck.test`, un `RUN` del Dockerfile, o una URL `file:///app/...`, es IN-IMAGE y NO se toca; si aparece en `new URL(..., import.meta.url)`, `join(repositoryRoot, ...)`, `from '...'` o en el ORIGEN de un `COPY`, es del ÁRBOL y sí se toca.

(3) NINGUNO DE LOS TRES ÍNDICES POR RUTA (calidad-base.json, container_ops_digest.STATIC_INPUTS, los globs planos de validate.sh) está mencionado en AGENTS.md, CLAUDE.md, GEMINI.md ni en ordenes/00-PROTOCOLO.md. Una sesión fresca los descubre chocando contra ellos, a mitad de una mudanza, con el gate en rojo y la tentación de «arreglarlo» editando contenido. Propuesta concreta y acotada: añadir a AGENTS.md/CLAUDE.md/GEMINI.md (que hoy tienen 36 líneas y son idénticos salvo detalles) UNA regla junto a la regla 3 existente:
    «3b. Un `git mv` NO está terminado hasta re-clavar las claves de ruta: scripts/calidad-base.json (trinquete), ops/scripts/container_ops_digest.py STATIC_INPUTS, y los globs planos de ops/scripts/validate.sh:6,7,197. Van en el MISMO commit que el mv — no son ediciones de contenido, son la segunda mitad del movimiento.»
  Es la sección del protocolo que falta y, por lo que muestra la evidencia de las cuatro zonas, la causa mecánica de la contaminación que reporta el dueño.

(4) ORDEN RECOMENDADO ENTRE ZONAS, por relación valor/riesgo medida: primero el arreglo de los globs de validate.sh (3 líneas, quita el modo de fallo silencioso de todo lo demás); luego tests/terminal-pty/dobles/ (9 ficheros, sector acotado, sin producción de por medio); luego deploy/ (21 mv + 11 líneas, PERO sólo en la ventana de FASE 3 con el dueño, porque deploy/** es fila NADIE); luego el ciclo de tipos de adapter-sdk (5 líneas de import, sector por asignar); ops/ al final y casi entero congelado. La partición de tests/unit en unit/+politica/ y la unificación de los dos hogares de tests de ops se quedan FUERA: son las de mayor coste y menor urgencia, y compiten por la misma ventana que deploy/.

## purga

BACKLOG FINAL DE DESCONTAMINACIÓN — 20 acciones ordenadas por contaminación-por-esfuerzo (repo /datos/workspaces/zeus/cauce-v3, solo lectura, nada modificado).

REPARTO: 16 EJECUTABLES YA (P1–P16) · 4 BLOQUEADAS por decisión del dueño (P17–P20, que agrupan las 6 familias dudosas del censo + las 5 decisiones D1–D5 + los 2,3 GB de residuos de host).

LOS 4 HALLAZGOS QUE CAMBIAN EL ORDEN DE TODO (verificados con comandos, no heredados de reportes previos):

1) docs/grafo.md MIENTE, y es el documento que la orden vigente de minimax manda usar. Su generador (scripts/grafo.mjs línea 52) nunca prueba '.tsx' ni '/index.tsx' al resolver imports. Por eso apps/console/src/components/ui.tsx figura como "candidato huérfano" teniendo 47 ficheros que lo importan, y App.tsx y main.tsx figuran igual. Reejecuté la resolución con .tsx + import() + new URL() + rutas relativas: los candidatos caen de 105 a 39, y de esos 39 casi todos siguen siendo falsos positivos estructurales (tsconfig, manifests cargados por glob, fixtures). Coste del arreglo: ~30 líneas. Coste de no arreglarlo: una sesión fresca que "purgue huérfanos" borra la consola. → P1, y P5 detrás.

2) La decisión (3) que espera la firma del dueño está mal tasada 4,5×, y ejecutarla literalmente ROMPE /live. PENDIENTES-DEL-DUEÑO.md:33 pide aprobar 8 vistas por "~4.700 LOC"; medidos hoy, los 8 componentes suman 1.027 líneas de src. Y ordenes/reportes/gemini-vistas-sin-uso.md tasa `topology` en 1.780 LOC incluyendo hypergraph-layout.ts + layout-{geometry,labels,nodes}.ts (1.030 líneas) que importa features/live/LiveHypergraph.tsx — la vista VIVA. Además App.tsx:110 ya contiene `topology: 'live'`: la ruta ya está retirada y el dossier no se enteró. El único residuo real es HyperGraph.tsx (244 líneas, cero importadores). → P2 y P9.

3) Las 3 órdenes vivas están desincronizadas del árbol. ordenes/codex.md manda partir 8 ficheros ya partidos (durable-store.ts 2.060→90, paste-runner.ts 1.900→8, tmux.ts 1.529→59, deliveries.ts 1.493→27, fanin.ts 1.438→289…) y omite los 16 tests >800 que sí quedan. Codex es la única instancia en sesión larga persistente: la que menos revalida. Y los 22 tests rojos de claude-matriz-tests.md quedaron huérfanos al reescribir el §Cierre de codex.md — hoy no están en ninguna orden. → P3 y P6.

4) La tabla de sectores de 00-PROTOCOLO.md tiene 8 zonas sin dueño (packages/adapter-sdk, protocol, mcp-fleet-monitor, services/dispatcher, ops/pty-agent, ops/tests, tests/, scripts/, y packages/store/test). De los 36 ficheros >800 de hoy, 23 caen fuera de toda fila — incluido el mayor del repo (adapter-sdk/test/shared-session.test.ts, 5.454 líneas). minimax-foto-final.md se los asigna a Codex; el protocolo dice "prohibido tocar fuera de tu sector". Dos verdades contradictorias sobre quién manda = parálisis o colisión. → P4.

ESTADO MEDIDO DEL RESTO: 36 ficheros >800 (20 fuente + 16 test; apps/console ya está limpia en esa métrica) · ~1.800 de las 2.322 líneas de comentario borrables pendientes (ejecutado −514 en 901411f, 802d323, 6f7720a) · 13 enlaces muertos en los .md de arranque, entre ellos docs/bitacora/legado-indice.md citado 3 veces tras borrarse la bitácora en 2be4101 · 160K de reportes ya consumidos (ola2 100K + 46-commits 60K) sin cita activa real · ~20 MB regenerables en disco · 2 schemas de ops/schemas muertos incluso dentro de su propia familia.

UNA BUENA NOTICIA: el trinquete de scripts/calidad.mjs está tenso. Medí 19.412 líneas de comentario en el árbol contra 19.412 en scripts/calidad-base.json: holgura CERO en 0 ficheros, y lo mismo en el tope de líneas. Ningún fichero puede recrecer, y cada limpieza baja su propio techo sin intervención. El gate no es fuente de contaminación; las órdenes y los reportes desactualizados sí.

- P1 [EJECUTABLE — MÁXIMA CONTAMINACIÓN POR ESFUERZO] · QUÉ: arreglar el resolvedor de /datos/workspaces/zeus/cauce-v3/scripts/grafo.mjs y regenerar docs/grafo.md antes de que nadie use su lista de huérfanos. · EVIDENCIA: la línea 52 del generador prueba estos candidatos al resolver un import relativo: [ruta, ruta.replace(/\.js$/,'.ts'), ruta+'.ts', ruta+'/index.ts', ruta.replace(/\.js$/,'.mjs')]. NO prueba '.tsx' ni '/index.tsx'. Consecuencia medida: apps/console/src/components/ui.tsx figura como 'candidato huérfano' en docs/grafo.md:284 y tiene 47 ficheros que lo importan (`git grep -c "from '.*components/ui'" -- apps/console/src | wc -l` = 47); apps/console/src/App.tsx (docs/grafo.md:281) lo importan App.test.tsx, App.invariantes.test.tsx, AccountsPage.test.tsx, AuthGate.test.tsx y más; apps/console/src/main.tsx (docs/grafo.md:353) lo referencia apps/console/index.html:12 (`<script type="module" src="/src/main.tsx">`) y el generador ni siquiera lee .html (esFuente, línea 10). Reejecuté la resolución añadiendo .tsx, /index.tsx, import(), new URL() y rutas textuales relativas al directorio del fichero: la lista cae de 105 a 39 candidatos, y de esos 39 casi todos siguen siendo falsos positivos estructurales (7 tsconfig.json/package.json, 14 ops/manifests/*.yaml cargados por glob en ops/scripts/manifest_lib.py:load_manifests, 4 packages/adapter-sdk/manifests/*.json leídos por plantilla `manifests/${id}.json` en packages/adapter-sdk/test/manifests.test.ts:13, fixtures de test). Además apps/console/src/features/terminal/terminal.worker.ts (docs/grafo.md:349) lo instancia apps/console/src/features/terminal/pty-output.ts:29 con `new Worker(new URL('./terminal.worker.ts', import.meta.url))`. · ESFUERZO: S (≈30 líneas en un solo fichero + `pnpm grafo`). · ASIGNAR: claude (scripts/ no tiene fila en la tabla de sectores; el generador lo escribió el integrador en 1a9d1c0). · ELIMINA: la trampa más peligrosa del árbol. Hoy docs/grafo.md le dice a toda sesión fresca que App.tsx, main.tsx y 72 vistas de la consola son huérfanas. Una sesión que 'purgue huérfanos' obedeciendo ese documento borra la consola entera. De paso ahorra 105 verificaciones manuales de la Tarea 3 de minimax.
- P2 [EJECUTABLE — BLOQUEA CORRECTAMENTE UNA DECISIÓN DEL DUEÑO] · QUÉ: re-tasar el §(3) de PENDIENTES-DEL-DUEÑO.md (las 8 vistas a `_legado`) ANTES de que el dueño firme, porque la cifra con la que se le pide aprobar es falsa y la ejecución literal del dossier rompe /live. · EVIDENCIA: PENDIENTES-DEL-DUEÑO.md:33 pide aprobar la poda de 8 vistas con 'ahorro estimado ~4.700 LOC'. Medido hoy con wc -l, los 8 componentes a retirar suman 1.027 líneas de src: AuditPanel.tsx 168 + audit-summary.ts + JobsRetiredNotice.tsx 20 + ChainPanel.tsx 121 + RoleBriefTab.tsx 132 + role-brief.ts + FleetAgentDetailPage.tsx 90 + HarnessStrip.tsx 80 + HyperGraph.tsx 244. La diferencia hasta 4.700 son filas del dossier que el propio dossier marca 'conservar' (licenses 1.644 LOC, quotas 323, assignments 545, activity 525, SpaceWizard/AltaRapida 746, historial-rol 811, ack-inspector 186) y código compartido. PEOR: ordenes/reportes/gemini-vistas-sin-uso.md:14 tasa `topology` en 1.780 LOC, pero de ese directorio hypergraph-layout.ts (397), layout-nodes.ts (335), layout-geometry.ts (190) y layout-labels.ts (108) los importa apps/console/src/features/live/LiveHypergraph.tsx:19 y features/live/live-hypergraph/FlowArrow.tsx:1 — es decir, la vista VIVA. Y TenantCards.tsx / AclEdgeList.tsx, del mismo directorio y de la misma recomendación, los importa apps/console/src/features/live/LiveFleetLegend.tsx:2-3. Borrar 'topology' según el dossier deja /live sin hipergrafo. Además apps/console/src/App.tsx:110 ya contiene `topology: 'live'`: la ruta standalone YA está retirada, el dossier está desactualizado. · ESFUERZO: S (remedir con wc -l + git grep, reescribir 3 líneas del PENDIENTES y la fila `topology` del dossier). · ASIGNAR: claude (dueño de ordenes/, PENDIENTES y plan-reestructura). · ELIMINA: que el dueño apruebe una purga mal tasada 4,5× y que quien la ejecute rompa la consola siguiendo el documento al pie de la letra.
- P3 [EJECUTABLE] · QUÉ: reescribir ordenes/codex.md con la lista real de ficheros >800 medida hoy; la orden vigente manda hacer trabajo YA HECHO y omite el 55% del que queda. · EVIDENCIA: ordenes/codex.md:6-8 lista 16 ficheros. Medido con wc -l hoy, 8 de esos 16 ya no existen a ese tamaño: packages/adapter-sdk/src/sdk/durable-store.ts (orden dice 2.060 → hoy 90), shared-session/paste-runner.ts (1.900 → 8), shared-session/tmux.ts (1.529 → 59), sdk/engine.ts (1.322 → 786), sdk/output-parser.ts (1.189 → 27), harnesses/shared.ts (1.133 → 29), packages/store/src/repository/deliveries.ts (1.493 → 27), packages/store/src/repository/agents/fanin.ts (1.438 → 289). Los partió el commit 455c48b ('refactor(adapter-sdk): particiona estado, engine y sesiones') y la tanda de store. En paralelo, la orden OMITE los 16 ficheros de test >800 que sí existen hoy, incluido el mayor del repo (packages/adapter-sdk/test/shared-session.test.ts, 5.454 líneas). Cifra real hoy: 36 ficheros >800 (20 fuente + 16 test), coincide exactamente con scripts/calidad-base.json (36 entradas en `lineas`). · ESFUERZO: S. · ASIGNAR: claude. · ELIMINA: Codex trabaja en sesión larga persistente (00-PROTOCOLO.md:52) y su orden 'se mantiene estable hasta cerrarla' — es la instancia que MENOS revalida. Una lista obsoleta ahí se convierte en horas de re-partir ficheros ya partidos, o en la conclusión falsa 'ya está todo hecho' al ver 6 ficheros de 90/59/29 líneas.
- P4 [EJECUTABLE — REQUIERE VISTO BUENO DEL DUEÑO, 1 MINUTO] · QUÉ: cerrar los huecos de la tabla de sectores de ordenes/00-PROTOCOLO.md:22-29. Ocho zonas del árbol no aparecen en NINGUNA fila. · EVIDENCIA: la tabla cubre apps/console/**, services/terminal-relay/**, services/telegram-bridge/**, packages/store/src/**, services/gateway/src/** + maquinaria de release de ops/scripts, higiene/docs, plan-reestructura/ordenes, y la fila NADIE (migrations, deploy, /opt, contenedores). Quedan FUERA: packages/adapter-sdk/**, packages/protocol/**, packages/mcp-fleet-monitor/**, services/dispatcher/**, ops/pty-agent/**, ops/tests/**, tests/** (top-level), scripts/**, y packages/store/test/** (la fila dice src/**, no test/**). Impacto medido: de los 36 ficheros >800 de hoy, 23 caen fuera de toda fila — entre ellos el mayor del repo (packages/adapter-sdk/test/shared-session.test.ts 5.454) y packages/protocol/src/schemas.ts (1.115). Y hay contradicción activa: ordenes/reportes/minimax-foto-final.md asigna adapter-sdk y protocol a 'Codex', pero 00-PROTOCOLO.md:14 dice 'Prohibido tocar un fichero fuera de tu sector'. Una sesión que obedezca el protocolo NO puede tocarlos; una que obedezca el reporte sí. · ESFUERZO: S (4 filas nuevas). · ASIGNAR: claude + dueño (la tabla es decisión suya). · ELIMINA: exactamente el mecanismo de colisión que el protocolo existe para evitar. Dos fuentes de verdad discrepantes sobre quién manda en un fichero producen o parálisis o dos instancias editando lo mismo.
- P5 [EJECUTABLE — DEPENDE DE P1] · QUÉ: reescribir la Tarea 3 de ordenes/opencode-minimax.md una vez arreglado el grafo. · EVIDENCIA: ordenes/opencode-minimax.md:12 manda: 'El grafo lista 105 ficheros fuente sin referencia entrante. Para CADA uno (repártelos entre 6 subagentes): re-verifica con git grep...'. De esos 105, 71 son falsos positivos de un mismo bug de resolución ya demostrado en P1, y otros ~28 lo son de carga por glob/plantilla. El residuo real que merece verificación humana es de un puñado de ficheros. Tal como está, la orden quema 6 subagentes × 105 ficheros para redescubrir un bug de 30 líneas. Los 3 ficheros de salida que la orden promete no existen todavía: docs/mapa-de-ficheros.md, ordenes/reportes/minimax-huerfanos.md, ordenes/reportes/minimax-matriz-cd.md. · ESFUERZO: S. · ASIGNAR: claude. · ELIMINA: 6 subagentes trabajando sobre datos que ya se saben falsos, y el riesgo de que uno de ellos concluya 'huérfano confirmado' sobre una vista viva de la consola.
- P6 [EJECUTABLE] · QUÉ: reasignar los 22 tests rojos de ordenes/reportes/claude-matriz-tests.md — hoy no están en ninguna orden viva. · EVIDENCIA: el reporte documenta 2 rojos en tests/gateway-hardening (wake-outbox-routing.test.ts 'treats ackOutbox applied=false as an observable fenced result' — sospechoso 28c6c00, marcado 'posible regresión REAL'; y perfil-en-el-saludo.test.ts) y 20 rojos en 5 ficheros de tests/store-hardening (581/601), con la causa raíz del primer grupo diagnosticada: terminal-relay-instance-fencing-migration-postgres.test.ts baja la 036 para simular el mundo pre-034 y la guarda anti-downgrade lo rechaza desde que existe la 037 (c7345da). `git grep -l claude-matriz-tests -- '*.md'` devuelve UN solo fichero, y es ordenes/reportes/claude-censo-comentarios-basura.md (que solo lo cita como 'ACTIVO' en su tabla de zona 3). ordenes/codex.md ya NO lo cita: su §Cierre apunta a claude-revision-ola3.md. Es decir, la cita que hacía viva a la matriz se perdió al reescribir la orden de Codex. Ninguna de las dos suites entra en el gate diario (`pnpm typecheck && pnpm lint && pnpm test:unit`), así que nadie va a tropezarse con ellos. · ESFUERZO: S para reasignar; M para arreglar. · ASIGNAR: claude (reasignar) → codex (ejecutar; el reporte dice 'ambos sector CODEX'). · ELIMINA: deuda roja huérfana y una posible regresión real de contrato de ACK que nadie está mirando.
- P7 [EJECUTABLE] · QUÉ: reparar los 13 enlaces muertos de los .md que las sesiones frescas leen al arrancar. · EVIDENCIA: barrido de todas las rutas con '/' citadas entre backticks en `git ls-files '*.md'`, resolviendo contra cwd y contra el directorio del propio fichero. Rutas citadas que NO existen en el árbol: (1) docs/bitacora/legado-indice.md — citado por ops/README.md, ordenes/reportes/minimax-foto-final.md §(c).1 y claude-revision-ola2.md; la bitácora entera se borró en 2be4101. (2) ops/harness/CONTRACT.md — citado por plan-reestructura/censo-contingentes.md:23 y minimax-foto-final.md; borrado en ronda 6. (3) ordenes/ronda4/opencode-minimax.md — citado por censo-contingentes.md:3 como la ejecución de los 29 muertos; el directorio ordenes/ronda4/ no existe. (4) tests/unit/deploy-release.test.ts — citado por censo-contingentes.md:48 §e como prueba de que systemd-stack.sh está vivo. (5) ops/scripts/preflight.sh y (6) ops/scripts/restore.sh — citados por claude-revision-ola3.md y ops/runbooks/backup-restore.md. (7) schemas/gate-snapshot.schema.json — citado por ops/runbooks/alias-cutover.md y ola3; borrado en ronda 6. (8) deploy/shadow-guard.mjs — citado por plan-reestructura/fase3/compose-canonico.md (documento de FASE 3, el más caro de equivocar). (9) ops/compose.authentic.yaml y (10) ops/scripts/release-candidate.py — citados por docs/terminal-pty.md. (11) docs/POOL-SUSCRIPCIONES-Y-ALTA-AGENTES.md — citado por docs/adr/006. (12) ops/runbooks/dlq-causal-reconciliation.md — citado por services/telegram-bridge/CONFIGURATION.md. (13) ordenes/reportes/codex-terra-bugs-reales.md — citado por claude-revision-ola2.md. · ESFUERZO: S. · ASIGNAR: minimax (fila 'higiene de disco, docs/, residuos, verificaciones mecánicas'; su Tarea 5 ya pide 'enlaces sin rutas muertas'). · ELIMINA: el mecanismo de contaminación literal que reporta el dueño. Un agente abre el enlace, no lo encuentra, y RECONSTRUYE DE MEMORIA lo que creía que había — inventando contratos que nunca existieron. Los casos (3), (4) y (8) son especialmente tóxicos porque son la evidencia citada de decisiones abiertas.
- P8 [EJECUTABLE] · QUÉ: `git rm` de ordenes/reportes/claude-revision-ola2.md (100K) y ordenes/reportes/claude-revision-46-commits.md (60K) — 160K de narrativa ya consumida en el directorio que toda sesión abre. · EVIDENCIA: `git grep -l claude-revision-ola2 -- '*.md'` (excluyendo el propio fichero) devuelve SOLO ordenes/reportes/claude-censo-comentarios-basura.md, que lo lista como 'ACTIVO: la sección Cierre de ordenes/codex.md lista 5 tareas concretas aún abiertas que remitan a este' — afirmación FALSA hoy: el §Cierre de ordenes/codex.md apunta a claude-revision-ola3.md, no a ola2. claude-revision-46-commits.md tiene una única cita activa, docs/arquitectura.md:34, y solo para verificar 'SQL intacto en la mudanza (§store-1/store-2)': se sustituye por la conclusión en una línea. La doctrina vigente ya está escrita en censo-contingentes.md:5 ('git rm + evidencia en commit; no existen carpetas de cuarentena') y se aplicó a docs/bitacora en 2be4101 sin excepciones. Tamaños medidos con du -sh: ordenes/reportes/ ocupa 304K, de los cuales estos dos son 160K (53%). · ESFUERZO: S. · ASIGNAR: minimax (Tarea 5 ya dice 'reportes ya consumidos → git rm con evidencia'). · ELIMINA: volumen de lectura por sesión. Los reportes de revisión son narrativa de incidentes con cifras: exactamente el material del que las sesiones frescas sacan 'hechos' obsoletos.
- P9 [EJECUTABLE — NO ESPERA AL DUEÑO] · QUÉ: borrar apps/console/src/features/topology/HyperGraph.tsx (244 líneas) y su fixture, si aplica. Es código muerto verificado, no una decisión de producto. · EVIDENCIA: `git grep -n 'HyperGraph' -- apps/console/src`, filtrando los nombres distintos LiveHypergraph / live-hypergraph / hypergraph-layout, devuelve UNA SOLA LÍNEA en todo el árbol: apps/console/src/features/topology/HyperGraph.tsx:23 `export function HyperGraph({ snapshot }: ...)` — su propia declaración. Cero importadores. Y la ruta que lo montaba ya no existe: apps/console/src/App.tsx:110 contiene `topology: 'live'`, o sea /topology es hoy un alias a /live. Contraste importante para no sobrepasarse: sus dos vecinos de directorio SÍ están vivos — TenantCards.tsx y AclEdgeList.tsx los importa apps/console/src/features/live/LiveFleetLegend.tsx:2-3 y los renderiza en las líneas 27-28. Y hypergraph-layout.ts + layout-{geometry,labels,nodes}.ts (1.030 líneas) los importa LiveHypergraph.tsx:19. Solo cae HyperGraph.tsx. · ESFUERZO: S. · ASIGNAR: gemini (sector apps/console/**). · ELIMINA: la última pieza real de la vista `topology` que el dossier de vistas tasa en 1.780 LOC; deja el §(3) del PENDIENTES tasado en la verdad (ver P2) en vez de en una estimación de hace dos rondas.
- P10 [EJECUTABLE — CORRIGE AL CENSO] · QUÉ: borrar los 2 schemas de ops/schemas/ que están realmente muertos: telegram-manual-replay-request.schema.json y telegram-replay-inspect-request.schema.json. · EVIDENCIA: `git grep -l 'telegram-manual-replay-request'` devuelve exactamente 2 resultados: el propio fichero y docs/grafo.md (que solo lo lista como candidato huérfano). Idéntico para telegram-replay-inspect-request. Ni ops/scripts/dlq_cli.py los cita. Esto CORRIGE plan-reestructura/censo-contingentes.md:32 §b, que afirma que la familia DLQ trae 'sus 3 schemas vivos en ops/schemas/telegram-{manual-replay-request,replay-inspect-request,replay-inspect}.schema.json'. Medido: de esos 3, solo telegram-replay-inspect tiene citas reales (ops/scripts/dlq_cli.py y packages/store/migrations/030_dlq_causal_reconciliation.sql). Los otros 2 son huérfanos de la propia familia. · ESFUERZO: S. · ASIGNAR: minimax. · NOTA DE BLOQUEO: el §b entero SÍ está bloqueado por el dueño (P17), pero estos 2 ficheros no: ningún miembro de la familia los usa, así que su borrado no prejuzga la decisión '¿la familia DLQ se queda o se documenta en bitácora?'.
- P11 [EJECUTABLE] · QUÉ: borrar los ~20 MB regenerables presentes en el checkout. · EVIDENCIA: medido con du -sh sobre lo gitignorado presente hoy: .test-state 7,4M + packages/adapter-sdk/.test-state 2,3M + dist 4,2M + packages/adapter-sdk/dist 4,2M + packages/mcp-fleet-monitor/dist 1,0M + packages/protocol/dist 276K + ops/pty-agent/tests/__pycache__ 368K + ops/scripts/__pycache__ 44K = 19,8 MB. Actualiza el censo: los tres cachés que ordenes/reportes/claude-censo-comentarios-basura.md §ZONA BASURA 1 marcaba 'borrar' (.mypy_cache 4,4M, .pytest_cache 40K, .ruff_cache 32K) ya NO existen, y apps/console/dist tampoco. Conservar: .serena/ (668K, herramienta activa) y ops/artifacts/ (48K, evidencia operativa). · ESFUERZO: S. · ASIGNAR: minimax (su Tarea 4 lo pide literalmente). · ELIMINA: poco espacio, pero quita 238 subdirectorios de .test-state del campo de visión de cualquier barrido de ficheros — la fuente de ruido de todo `find`/`ls` que haga una sesión fresca.
- P12 [EJECUTABLE] · QUÉ: corregir plan-reestructura/censo-contingentes.md con lo medido, y en particular añadir a §e la evidencia que refina la decisión D2. · EVIDENCIA: (a) §b afirma 3 schemas vivos; son 1 vivo + 2 muertos (ver P10). (b) §e dice que alertmanager 'existe en código pero no se ejercita': medido, sí se ejercita a medias — ops/tests/test_provision_alertmanager_config.py (163 líneas) está referenciado por ops/scripts/validate.sh, es decir el gate valida la configuración de un servicio que no está desplegado en ningún compose ni contenedor. Ese dato cambia el coste de las dos ramas de D2: si se recortan las reglas, ese test cae con ellas. (c) §e cita tests/unit/deploy-release.test.ts como prueba de vida de systemd-stack.sh y ese fichero ya no existe (ver P7). (d) §f cita 'ops/harness/CONTRACT.md' que se borró en la propia ronda 6 que el documento describe. · ESFUERZO: S. · ASIGNAR: claude (plan-reestructura/ es su sector). · ELIMINA: que el dueño decida las 6 familias dudosas leyendo un inventario con 4 hechos caducados.
- P13 [EJECUTABLE] · QUÉ: dar veredicto explícito a services/gateway/src/routes/legado-candidato.ts (204 líneas) — un fichero cuyo nombre declara 'candidato a legado' y que está montado en producción. · EVIDENCIA: lo importa services/gateway/src/app.ts. Expone 3 endpoints: '/v3/console/chain-gates', '/v3/console/publish-intents' y '/v3/console/publish-intents/confirm'. Se cruza con la decisión (3) del dueño: 'chains' es una de las 8 vistas propuestas para retirar, y su endpoint documentado en el dossier es GET /v3/console/chains — no chain-gates, así que la relación no es la obvia y hay que medirla antes de tocar nada. `git grep -l legado-candidato` devuelve: app.ts, services/gateway/README.md, docs/arquitectura.md, scripts/calidad-base.json y 2 reportes. · ESFUERZO: M (hay que medir tráfico real de los 3 endpoints y su cobertura de test antes de decidir). · ASIGNAR: codex (sector services/gateway/src/**). · ELIMINA: un fichero que le dice a cada lector 'yo sobro' sin que nadie haya decidido si sobra; es una invitación permanente a que una sesión fresca lo borre por su cuenta.
- P14 [EJECUTABLE, GRANDE] · QUÉ: terminar la limpieza quirúrgica de comentarios. Quedan ~1.800 de las 2.322 líneas borrables del censo. · EVIDENCIA: ordenes/reportes/claude-censo-comentarios-basura.md:3 fija el total en 2.322. Ejecutado hasta hoy: −514 líneas en 3 commits (901411f −215 consola, 802d323 −248 consola, 6f7720a −51 telegram-bridge). Los focos que quedan, por líneas borrables de la tabla: apps/console/src/features/config/config-css.test.ts 160 (narrativo; 8 repeticiones de 'MEDIDO en Chrome' + cita textual del dueño), apps/console/src/styles.tipografia.test.ts 87, apps/console/src/features/live/agent-state.ts 75, styles.tipografia-montada.test.tsx 55, packages/store/src/repository/deliveries.ts 118 (incluye los `// S1`…`// S6` huérfanos sin definición dentro de lateTerminalSalvage — la evidencia dura de que la limpieza masiva 2a22107 mutiló frases), packages/protocol/src/schemas.ts 57, packages/store/src/repository/observability/policy.ts 55, packages/store/src/repository/agents/fanin.ts 54. PROHIBIDO: los comentarios `--` dentro del template literal SQL de fanin.ts:987-991 (el censo lo marca 'sql-string: no tocar sin verificar bytes'). El gate está a favor: el trinquete de comentarios de scripts/calidad.mjs está EXACTAMENTE al nivel actual (medido: 19.412 líneas de comentario en el árbol vs 19.412 en scripts/calidad-base.json, holgura 0 en 0 ficheros), así que cada borrado baja el techo solo y ningún fichero puede recrecer. · ESFUERZO: L. · ASIGNAR: gemini (consola + canales, ya es su Tarea 3) y codex (store/gateway/protocol, ya es su 'regla dos-en-uno'). · ELIMINA: la narrativa de incidentes con cifras medidas ('881 entregas', '197 entregas de producción', '148 repeticiones', '13 conectados') que las sesiones frescas leen como estado ACTUAL del sistema y no como historia. Es la contaminación de más alto volumen del repo.
- P15 [EJECUTABLE, GRANDE — DEPENDE DE P3 Y P4] · QUÉ: los 36 ficheros >800 líneas que quedan. · EVIDENCIA: medido hoy (`git ls-files | grep -E '\.(ts|tsx|mjs|py|sh)$' | xargs wc -l | awk '$1>800'`), coincide exactamente con las 36 entradas de `lineas` en scripts/calidad-base.json. Reparto: 20 fuente + 16 test. Los 5 mayores: packages/adapter-sdk/test/shared-session.test.ts 5.454, packages/adapter-sdk/test/engine.test.ts 2.794, packages/store/test/agent-output-postgres.test.ts 2.730, ops/pty-agent/cauce_pty_agent.py 2.667 (producción, NO tocar fuera de FASE 3), services/gateway/src/terminal.plugin.test.ts 2.020. De los 36, solo 13 caen dentro de una fila de la tabla de sectores (6 de packages/store/src, 7 de services/gateway/src): los 23 restantes no tienen dueño declarado hasta que se resuelva P4. Nota: apps/console ya no tiene NINGÚN fichero >800 — la consola está limpia en esta métrica. · ESFUERZO: L. · ASIGNAR: codex (store/gateway/adapter-sdk/protocol) + gemini (Tarea 5 vigente: tests de consola/canales, rollout-pty.py, update-alias-config.py) + dueño para ops/pty-agent y ops/container-runtime (fila NADIE hasta FASE 3). · ELIMINA: ficheros que ninguna sesión puede leer entera, que es la causa mecánica de que un modelo edite 'a ciegas' un tramo y rompa el resto.
- P16 [EJECUTABLE, NECESITA UN VEREDICTO] · QUÉ: decidir runner y dueño para ops/tests/container-supervisor.test.mjs (1.761 líneas) y para los tests huérfanos de ops/tests/ que el censo dejó en §f. · EVIDENCIA: ordenes/reportes/minimax-foto-final.md lo asigna a '**DUEÑO** (huérfano de ops/tests)' y es el 6º fichero más grande del repo. plan-reestructura/censo-contingentes.md:58 documenta el patrón para toda esa carpeta: 'los toca el genérico `node --check "$ROOT"/tests/*.mjs` de validate.sh (chequeo de sintaxis, no ejecución)' — es decir, ops/tests/ tiene ficheros que se compilan pero no se ejecutan nunca. Lista del §f afectada: ops/tests/gate-collector.test.mjs, provision-hermes-runtime.test.mjs, separar-config-alias.test.mjs, update-alias-config.test.mjs, y los 5 test_*.py (test_alias_lock_exec, test_config_por_alias_supervisor, test_container_runtime_zombies, test_schema_error_sanitization, test_verify_hermes_runtime) sin runner en validate.sh/Makefile/package.json/CI/pytest.ini/pyproject.toml. · ESFUERZO: M. · ASIGNAR: minimax para el censo mecánico (¿cuál tiene runner de verdad?) → dueño para el veredicto (enganchar al gate o git rm). · ELIMINA: tests que dan sensación de cobertura sin ejecutarse — el mismo patrón que ordenes/reportes/claude-revision-ola3.md ya documentó como crítico en la suite PTY ('225 pruebas que solo corren si alguien las teclea').
- P17 [BLOQUEADA POR EL DUEÑO — decisión (2)(a) y (2)(f) del PENDIENTES] · QUÉ: herramientas de otras máquinas + resto suelto. (a) ops/cli/cauce-portatil (único consumidor: ops/scripts/install-cauce-cli.sh rama 'portatil', para el portátil del operador, no aplica a zeus) y ops/cli/compilar-en-torre (cero referencias en todo el árbol: ni docs, ni package.json, ni Makefile, ni CI). (f) ops/private/CREDENTIAL-INVENTORY.local (no trackeado, nadie lo referencia, 'borrable seguro cuando el dueño autorice'), ops/openclaw-gateway/argos.env.example, ops/scripts/{generate-telegram-config.py, telegram-cutover-preflight.py} (solo desde ops/runbooks/telegram-cutover.md, sin invocación automática), ops/scripts/separar-config-alias.mjs + su test huérfano, ops/scripts/update-alias-config.py + su test huérfano, ops/scripts/fault-compose.test.sh (grep=0 de ejecución; solo listado, no ejecutado, en el manifiesto hardcodeado de source-digest.py:184), deploy/liveness-probe.mjs (0 referencias en compose*.yaml, no está en la lista COPY de deploy/Dockerfile, ni lo invocan runtime-package-smoke.mjs ni docker), y Makefile (`make <target>` aparece 0 veces fuera del propio Makefile; .github/workflows/ci.yml llama a `make validate` pero esa también la exporta `npm run validate`). · EVIDENCIA: plan-reestructura/censo-contingentes.md §a y §f. · ESFUERZO: S una vez decidido. · ASIGNAR: dueño (decide) → minimax (ejecuta). · DESBLOQUEO: 2 checkboxes en PENDIENTES-DEL-DUEÑO.md:22 y :27.
- P18 [BLOQUEADA POR EL DUEÑO — decisión (2)(b) y (2)(c) del PENDIENTES] · QUÉ: (b) familia DLQ manual — ops/scripts/dlq_cli.py + 5 wrappers (dlq-list.py, dlq-reconcile.py, resolve-dlq-without-replay.py, telegram-manual-replay.py, telegram-replay-inspect.py) + sus schemas; ¿herramientas vivas de emergencia del operador o documentadas en bitácora? (c) console-legibilidad — los 6 ficheros de ops/console-legibilidad/ (cdp.mjs, medir.mjs, medir-terminal.mjs, medir-tipografia.mjs, probe.mjs, servir-con-csp.mjs), con wiring interno solo entre sí, sin runner ni CI. · EVIDENCIA: plan-reestructura/censo-contingentes.md §b y §c. Matiz medido que ayuda a decidir (b): ningún runner automático los invoca, y 2 de sus 3 schemas están muertos incluso dentro de la propia familia (ver P10). Matiz para (c): docs/grafo.md registra que scripts → ops/console-legibilidad tiene 6 referencias y apps/console → ops/console-legibilidad tiene 3, o sea NO está tan aislado como dice el censo — hay que verificar esas 9 aristas antes de archivar. · ESFUERZO: S (b) / M (c, por las 9 aristas a verificar). · ASIGNAR: dueño (decide) → minimax (ejecuta). · DESBLOQUEO: PENDIENTES-DEL-DUEÑO.md:23 y :24.
- P19 [BLOQUEADA POR EL DUEÑO — decisión (2)(d), (2)(e) y D2] · QUÉ: (d) quota-collector — ops/scripts/quota-collector.py + 2 plantillas de ops/config/ + ops/scripts/ut-nexus-backup{,-verify}.py (UT_NEXUS_ENABLED=0 aquí, corren en otra máquina) + ops/tests/test_fleet_watchdog.py, test_quota_collector.py y sus 3 fixtures. (e) alertmanager — ops/observability/alertmanager.yaml + deploy/compose.alertmanager.yaml + ops/scripts/systemd-stack.sh. · EVIDENCIA: censo-contingentes.md §d y §e. (d) tiene un override explícito del integrador (censo:18): 'la base productiva escribe muestras de cuota frescas, NO se mueve hasta aclarar quién colecta'. (e) es la decisión D2 del dossier de FASE 3: compose.alertmanager.yaml declara el servicio pero no aparece en ninguna rama de ops/scripts/compose-files.sh ni en config_files de los contenedores productivos, y sin decidir, la alerta CauceAlertmanagerDown queda critical encendida para siempre. Dato nuevo medido que refina D2: ops/tests/test_provision_alertmanager_config.py (163 líneas) SÍ está enganchado a ops/scripts/validate.sh — el gate valida hoy la config de un servicio que no existe (ver P12). · ESFUERZO: M. · ASIGNAR: dueño (decide, y D2 tiene coste de 7 variables nuevas a aprovisionar si se despliega con receptor Telegram). · DESBLOQUEO: PENDIENTES-DEL-DUEÑO.md:11 (D2), :25 y :26.
- P20 [BLOQUEADA POR EL DUEÑO — decisiones D1/D3/D4/D5 y §(4) residuos de host] · QUÉ: lo que solo el dueño puede ejecutar a mano. (i) D1 flota declarada de la migración 029 (hoy tocaría deshabilitar Jhon/heraclito, Jhon/tales, Miguel/gaia); D3 origen del compose (repo vs /opt, cambia el source de 4 binds); D4 bloque B de pty-huérfanos (heraclito/tales, 2 de los 12 del kill-list); D5 censo de huérfanos en el OTRO host (el bucle de dedalo/salva viene de otra máquina). (ii) Los ~2,3 GB de residuos del host: 8 contenedores huérfanos (3 postgres Up-healthy sin identificar + 5 stopped), 13 árboles /opt/cauce-v3-release-* (~620 MB), 18 tags rc-<sha> del registry, 5 tags *-legacy, 5 imágenes locales del daemon (~1,3 GB), y el clon muerto /datos/workspaces/cauce-v3 (~366 MB, ya archivado en cauce-v3-archivo-completo-20260827.bundle 7,3 MB + cauce-rescate-worktrees-20260827.tar.gz 14,1 MB). · EVIDENCIA: PENDIENTES-DEL-DUEÑO.md §(1) y §(4); comandos exactos fila por fila en ordenes/reportes/minimax-residuos-host.md. · ESFUERZO: M (la ventana de FASE 3 se estima en 2–3 horas con el dueño presente). · ASIGNAR: dueño, en exclusiva — 00-PROTOCOLO.md:29 pone /opt, /etc/cauce-v3, contenedores, systemd y BD en la fila NADIE, y PENDIENTES:82 lo repite: 'los agentes no tocan /opt, /etc/cauce-v3, ni producción'. · ELIMINA: nada del repo, pero es lo único que desbloquea la ventana de FASE 3; mientras siga abierto, cada sesión que lee el PENDIENTES ve 5 decisiones sin marcar y no sabe si puede avanzar.

